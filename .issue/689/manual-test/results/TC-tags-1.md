# TC-tags-1: タグ行のチップUI（AC-1）
**結果**: PASS
## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | タグ付きノート編集を開く | ページが表示される | 編集画面が表示された | PASS |
| 2 | snapshot でタグ行を確認 | 既存タグが個別削除ボタン付きチップとして表示され、末尾に「新規タグ」textbox が同一 list 内に共存 | `list "タグ"` 内に listitem として `#test`+「test を削除」、`#guide`+「guide を削除」、`#design`+「design を削除」、最後に「新規タグ」textbox が共存 | PASS |
| 3 | 旧UIでないことを確認 | 「タグ（カンマ区切り）」ラベルの縦積みフォームでない | 該当ラベルなし。role=list 内のチップ + borderless 入力が1行に共存 | PASS |

## 失敗詳細（FAILの場合）
なし
