# TC-006: 自ノート検索で <mark> 生文字列が漏れない（AC-5）
**結果**: PASS
**セッション**: verify-779-own
## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | cookie 注入後 `http://localhost:8787/` を open（networkidle 待ち）| 認証済みアプリ画面（ノート一覧 P30）が表示される | サイドバーに「Dev Admin / dev-admin@example.com」、main にノート一覧（35件）が表示。ログインへのリダイレクトなし | PASS |
| 2 | `http://localhost:8787/?q=デザイン` を open（networkidle 待ち）→ snapshot | private ノート `デザイン草稿メモ` が1件ヒット | 見出し「「デザイン」の検索結果」、「1 件のノート」。結果に `デザイン草稿メモ`（snippet `デザインの検討メモ（非公開）`、非公開、2026年6月27日）が表示 | PASS |
| 3 | eval で生 `<mark>` 文字列・mark 要素の有無を確認 | body に生 `<mark>`/`</mark>` 無し、結果カードに mark 子要素無し（自ノートは highlight:false） | `bodyHasRawMark:false`, `markElementCount:0`(main mark), `mainHtmlHasMarkTag:false`, タイトル=`デザイン草稿メモ`（プレーン） | PASS |
| 4 | snapshot のタイトル/スニペット表示確認 | プレーンテキストで表示、生文字列・mark 要素一切無し | accessibility tree 上もタイトル `デザイン草稿メモ`・スニペット `デザインの検討メモ（非公開）` は StaticText のみ（mark ノード無し） | PASS |

## 失敗詳細（FAILの場合）
なし。

## 補足
- 自ノート検索ユースケースが `highlight:false` を渡す設計どおり、アダプターはタイトル・スニペットをマーカー無しのプレーン文字列で返却。生 `<mark>` 文字列の漏れ（AC-5 違反）・想定外の mark ハイライト、いずれも発生せず。
