# TC-003: XSS 安全性（AC-2）
**結果**: PASS
**セッション**: verify-779-public
## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `デザイン` 検索結果でノート C `<script>alert(1)</script>デザイン強化` のタイトル DOM を確認 | `<script>` がテキストとしてエスケープ表示、`デザイン` のみ mark | タイトル innerHTML = `&lt;script&gt;alert(1)&lt;/script&gt;<mark class="...">デザイン</mark>強化`（script はエスケープ、デザインのみ mark 要素） | PASS |
| 2 | alert 注入の有無を確認 | alert を含む script 要素が無い／ダイアログ出ない | `document.querySelectorAll('script')` 内に `alert(1)` を含むものは 0 件、アラートダイアログ発生なし | PASS |
## 失敗詳細（FAILの場合）
- なし
