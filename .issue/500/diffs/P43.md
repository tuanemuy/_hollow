# P43-admin-tokens 突き合わせ
対応: route=app/routes/admin/design.tsx, components=[admin/DesignTokensForm/Page.tsx, admin/DesignTokensForm/index.tsx], styles=[common/styles.ts (pillBtn/pillBtnGhostDanger/pillBtnSm)]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 管理シェルのヘッダー / 観点:component / 旧モック:menu-btn+search+通知 → 実装:logo+pill+avatar のみ / 修正内容:P40 と同じくヘッダー簡素化。検索プレースホルダ「トークン名を検索」も消失。
- [x] エディタ構成 / 観点:layout / 旧モック:左右2カラム（編集エディタ＋プレビューペイン）、グループ折りたたみ（カラー/タイポ/スペーシング/角丸）、上部 segmented toolbar、下部 diff パネル → 実装:1枚のカードに flat な行リスト（curated 27 キー＋任意追加行）、グループ分け・折りたたみ・プレビュー・diff・segmented いずれも無し / 修正内容:workspace 2カラム・group 折りたたみ・preview・diff・toolbar を削除し、`[220px キー入力][値入力＋swatch＋既定に戻す/削除]` の flat 行リストへ全面書き換え。「＋ トークンを追加」ボタン・「すべてリセット」/「保存」フッターを実装に合わせて配置。
- [x] トークン行の構造 / 観点:component / 旧モック:キーは静的テキスト、値入力に左内側 swatch、変更行は枠線を accent で強調 → 実装:キーも input（curated は disabled）、上書き行に「上書き中」バッジ＋「既定値: …」行、値入力の左に独立 swatch（24px）、行末に「既定に戻す」（curated）/「削除」（任意追加）ボタン / 修正内容:行を実装の grid `220px 1fr` 構造へ。バッジ・既定値表示・行アクションボタンを追加。
- [x] 入力欄スタイル / 観点:token / 旧モック:白地＋hairline 枠 / 実装:surface 塗り＋transparent 枠＋focus で bg+hairline-strong / 修正内容:`.token-input` を surface 塗りベースへ。disabled は opacity-disabled。
- [x] page-subtitle / 観点:component / 旧モック:「変更はすべてのユーザーの画面に反映されます。」 → 実装:「ビルトインの既定値が初期表示され…エクスポート時の CSS に注入されます。」 / 修正内容:subtitle を実装の文言へ。本文の説明段落（intro）も追加。
- [x] グループ折りたたみ JS / 観点:component / 旧モック:`.group-header` クリックで開閉する `<script>` → 実装:折りたたみ無し / 修正内容:該当 script を削除。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- ライブプレビューペイン（サンプルノートでトークン変更を視覚確認）/ 根拠:実装に編集中トークンのプレビュー機構は無い（index.md §10 でも P43 のプレビュー機構は別フェーズと明記）。/ 別Issue化候補:Yes（ただし index.md §10 で意図的スコープ外）
- 変更差分パネル（編集前→編集後の from/to 表示）/ 根拠:実装に diff 表示は無い（保存時に既定値同値行を落とすのみ）。/ 別Issue化候補:Yes
- トークンのカテゴリ別グループ化＋折りたたみ（カラー/タイポ/スペーシング/角丸）/ 根拠:実装は flat リスト。グルーピング UI は未実装。/ 別Issue化候補:Yes
- 「編集後 / 編集前との比較」segmented トグル / 根拠:プレビュー・diff が無いため segmented も未実装。/ 別Issue化候補:Yes

## C. 要判断（曖昧）
- プレビュー/diff の扱い / 論点:index.md §10 はトークン変更のプレビュー機構を「別フェーズ（スコープ外）」と明記。よってプレビュー/diff は「未実装で退化」ではなく意図的スコープ外。モックには残置（B）したが、§10 を根拠に別Issue化せず据え置く判断もあり得る。要確認。
