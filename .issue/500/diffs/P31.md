# P31-public-note 突き合わせ
対応: route=app/routes/u/$username/$noteSlug.tsx (+ app/routes/notes/public/$noteId.tsx), components=[public/PublicNoteDetail, public/PublicLayout], styles=[public/styles.ts, app/styles/index.css(.note-detail-content)]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 公開ヘッダーのハンバーガー `menu-btn` を削除 / 観点:component / 旧モック:`.menu-btn`（max-sm 表示）→ 実装:公開レイアウトは drawer 無し / 修正内容:マークアップ・CSS・レスポンシブルールを削除。
- [x] top-bar を「breadcrumb の上にグリッド横並び author-mini」から「breadcrumb → author-mini の縦積み」へ / 観点:layout / 旧モック:`.top-bar { display:grid; grid-template-columns:1fr auto }`（breadcrumb と author-mini を左右配置）→ 実装:breadcrumb（`mb-4`）の下に author-mini ブロック（`mb-4.5`）を縦積み / 修正内容:`.top-bar` を単純ブロックにし、breadcrumb に `margin-bottom:16px`、author-mini に `margin-bottom:18px` を付与。
- [x] author-mini の「他のノートを見る →」リンクを削除 / 観点:component / 旧モック:`.author-mini-link`「他のノートを見る →」→ 実装:`AUTHOR_MINI` はアバター + 表示名 + @username のみ / 修正内容:`.author-mini-link` のマークアップと CSS を削除。
- [x] breadcrumb のリンク/現在地の色とセパレータを実装に合わせる / 観点:token,component / 旧モック:リンク=accent、`.current`=ink-secondary、セパレータ=chevron svg → 実装:リンク=`text-ink-secondary hover:text-ink`、現在地=`text-ink font-medium`（省略付き max-w-[30rem]）、セパレータ=`›` テキスト / 修正内容:色を ink 系へ、セパレータを `›` に、`.current` に省略表示を追加。breadcrumb リンク文言も「遠山ゆう (@tuanemuy)」へ（実装は `{displayName} (@{username})`）。
- [x] pub-pill を「塗りつぶし緑+白文字+白ドット」→「success-surface 背景 + success 文字 + success ドット」 / 観点:variant,token / 旧モック:`background: var(--color-status-public); color:#fff`、白ドット → 実装:`PUB_PILL`=`bg-success-surface text-success text-[11px]`、`PUB_PILL_DOT`=`bg-success`（Apple Calm の淡色ピル）/ 修正内容:背景=success-surface、文字=success、font-size=`var(--text-xs)`、ドット 6px=success、gap/padding を実装値（gap-1/px-2 py-0.5）相当に。
- [x] action-toolbar（エクスポート / 共有 ボタン）を削除 / 観点:component / 旧モック:note-head 内に owner 向けアクションボタン → 実装:匿名閲覧の `PublicNoteDetail` はアクションツールバー無し（export/share は所有者操作で公開ビューに出さない）/ 修正内容:`.action-toolbar` マークアップと CSS を削除。
- [x] note-meta-inline / author-mini 系の font-size を px 直値→token / 観点:token / 旧モック:`13px`/`12px`→ 実装:`text-sm`/`text-xs` / 修正内容:`var(--text-sm)` / `var(--text-xs)`。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- バックリンクセクション（`.section-block`「バックリンク（公開ノート）」+ `.backlink-list`）/ 根拠:`PublicNoteDetail` は本文 article のみ描画。`BACKLINKS` 定数は `public/styles.ts` に存在するが未使用 / 別Issue化候補:Yes
- 「同じ著者の他のノート」関連カードグリッド（`.section-block` + `.related-grid`/`.related-card`）/ 根拠:実装に関連ノート表示が無い / 別Issue化候補:Yes
- 本文末尾の bottom-meta ブロック（タグ + 公開/更新日の再掲）/ 根拠:実装は note-meta-inline のみで末尾再掲が無い / 別Issue化候補:No
- 本文内 wikilink / hashtag の見た目（`.wikilink` ピル+先頭ドット、`.hashtag`）/ 根拠:本文は `dangerouslySetInnerHTML`(.note-detail-content) 経由で、これらの装飾は `app/styles/index.css` 側の責務。モック本文の質感は spec/design/index.md §6 の規約に沿うため正として残す（別途 note-detail-content と突き合わせ要だが本バッチのコンポーネント対象外）/ 別Issue化候補:No

## C. 要判断（曖昧）
- なし（本文 Markdown 質感は §6 規約準拠でモック正、B に記録）
