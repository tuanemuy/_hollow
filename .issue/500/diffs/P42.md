# P42-admin-prompts 突き合わせ
対応: route=app/routes/admin/prompts.tsx, components=[admin/PromptsForm/Page.tsx, admin/PromptsForm/index.tsx (PromptsForm, PromptCard)], styles=[common/styles.ts]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 管理シェルのヘッダー / 観点:component / 旧モック:menu-btn+search+通知 → 実装:logo+pill+avatar のみ / 修正内容:P40 と同じくヘッダー簡素化。
- [x] プロンプトカードの構成 / 観点:component / 旧モック:3カード（構造化/タイトル/ディレクトリ）、各カードに textarea＋サンプル入出力ブロック＋「実行プレビュー」＋「履歴を見る」＋「エラー率」 → 実装:5カード（structure/title/directory/metadata/ocr_assist）、各カードに「上書き中」バッジ・説明・既定値行・「分析の指示（任意）」textarea・「期待するプレースホルダ」入力・カード単位の「この項目をリセット」/「保存」ボタン / 修正内容:カードを5枚（実装の PROMPT_DESCRIPTORS SSOT）へ。サンプル/プレビュー/履歴/エラー率を削除し、変数入力＋カード内 save/reset ＋ badge ＋ 既定値表示に置換。未上書きカードは save/reset を disabled、既定値「（追加の指示なし）」を表示。
- [x] 入力欄スタイル / 観点:token / 旧モック:textarea が surface-elevated＋hairline 枠 → 実装:surface 塗り＋transparent 枠＋focus で bg+hairline-strong / 修正内容:`.textarea`/`.input` を surface 塗りベースへ（mono text-xs・min-h 140px）。
- [x] フッター / 観点:component / 旧モック:「初期値にリセット」＋「キャンセル」＋「変更を保存」 / 実装:「すべてのプロンプトをリセット」（ghost-danger）1つのみ＋ConfirmDialog / 修正内容:フッターを単一の「すべてのプロンプトをリセット」へ。グローバル「変更を保存」/「キャンセル」は廃止（保存はカード単位）。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- プロンプトのサンプル入力 / 実行プレビュー出力ブロック / 根拠:実装にプロンプト実行プレビュー機能は無い。/ 別Issue化候補:Yes
- 「実行プレビュー」ボタン / 根拠:プレビュー実行 usecase は未実装。/ 別Issue化候補:Yes
- 「履歴を見る」リンク（プロンプト変更履歴）/ 根拠:実装に履歴 UI/永続化は無い。/ 別Issue化候補:Yes
- 「直近24h エラー率」表示 / 根拠:プロンプト別エラー率の集計・表示は未実装。/ 別Issue化候補:Yes

## C. 要判断（曖昧）
- カードに「本文プロンプト全文を編集する textarea」か「分析の指示（任意）だけ補足する textarea」か / 論点:旧モックはシステムプロンプト全文を直接編集する想定。実装は ADR-002 で「空の既定＝追加指示なし」とし、ユーザーは追加意図のみを補足する設計に変わっている。モックを実装（補足意図）に寄せたが、全文編集に戻す設計判断があれば再検討。今回は実装を正とした。
