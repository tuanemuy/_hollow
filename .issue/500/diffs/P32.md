# P32-public-search 突き合わせ
対応: route=app/routes/search.tsx, components=[public/PublicSearch, public/PublicLayout], styles=[public/styles.ts]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 公開ヘッダーのハンバーガー `menu-btn` を削除 / 観点:component / 旧モック:`.menu-btn`（max-sm 表示）→ 実装:公開レイアウトに drawer 無し / 修正内容:マークアップ・CSS・レスポンシブルールを削除。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- レート制限バナー（`.rate-banner`「レート制限: 5 分間に 30 リクエストまで…」）/ 根拠:`PublicSearch` にレート制限の可視化 UI が無い / 別Issue化候補:Yes
- フィルターバー（`.filter-bar`: 件数 + `フィルター`ボタン(バッジ付き) + `関連度順`ソート）/ 根拠:実装は検索フォーム + 結果サマリ(件数のみ) のみ。フィルター/ソート UI 無し / 別Issue化候補:Yes
- アクティブフィルターチップ列（`.active-chips`: ユーザー/タグ/期間 chip + すべて解除）/ 根拠:実装にアクティブフィルター chip の描画が無い / 別Issue化候補:Yes
- フィルタードロワー一式（`.drawer` + facet-section: ユーザー/タグの token-input combobox、期間 radio、`すべてリセット`/`N 件を表示`）/ 根拠:実装にファセット絞り込みドロワーが無い（検索は q/username/cursor/limit のみ）/ 別Issue化候補:Yes
- 検索結果の `<mark>` ハイライト（result-title / result-snippet 内）/ 根拠:`SEARCH_HIT_TITLE`/`SEARCH_HIT_SNIPPET` はプレーン表示でヒット語ハイライト無し / 別Issue化候補:Yes
- 結果メタの 2 行スニペット（`-webkit-line-clamp:2`）/ 根拠:実装 `SEARCH_HIT_SNIPPET` は clamp 無し（`leading-normal mb-1.5`）。差は軽微だが未対応として記録 / 別Issue化候補:No

## C. 要判断（曖昧）
- ヒーローの体裁差 / 論点:旧モック=中央寄せ・`max-width:720px`・サブタイトル「このインスタンス全体の…横断検索できます」・⌘K kbd ヒント・送信ボタン無し（padding 64px 0 24px、input height 56px）。実装=左寄せ `SEARCH_HERO`(`py-12 pb-6`)・サブタイトル無し・⌘K 無し・input 内に「検索」送信ボタンあり（`SEARCH_FORM_INPUT` h-12=48px + border + `SEARCH_FORM_BUTTON`）。「実装の簡素な左寄せ＋送信ボタン形が最終意図」か「モックの中央ヒーロー＋⌘K が正で実装が退化」かの判断待ち。decisions-pending に登録。
- 空状態 `SEARCH_EMPTY`（#231 既知論点）/ 論点:実装は「キーワードを入力してください」「該当ノートが見つかりませんでした」をアイコン無しで表示。公開側 `EMPTY_LIST`/空状態はアイコン無しが意図的（#231 スコープ外）。モックは P32 で明示的なアイコン付き空状態を描いていないため現状差分は無いが、公開側空状態のアイコン要否は判断待ちとして decisions-pending に登録。
