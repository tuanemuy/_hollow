# P30-user-public-top 突き合わせ
対応: route=app/routes/u/$username/index.tsx, components=[public/UserPublicTop, public/PublicLayout], styles=[public/styles.ts, common/styles.ts]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 公開ヘッダーのハンバーガー `menu-btn` を削除 / 観点:component / 旧モック:`header-left` 先頭に `.menu-btn`（max-sm で表示）→ 実装:`PUBLIC_HEADER_LEFT` はロゴのみ（公開レイアウトに drawer/サイドバー無し）/ 修正内容:`menu-btn` のマークアップ・CSS・レスポンシブ表示ルールを全削除。
- [x] プロフィール統計から「タグ数」を削除 / 観点:component / 旧モック:`<strong>42</strong>公開ノート · <strong>18</strong>タグ · 2024年4月から`→ 実装:`PROFILE_STATS` は公開ノート数 + 参加年月のみ（タグ数表示なし）/ 修正内容:`<strong>18</strong>タグ` とその区切りドットを削除。
- [x] プロフィール bio の最大幅を token 化 / 観点:token / 旧モック:`max-width: 60ch`→ 実装:`max-w-[var(--content-max)]`(760px) / 修正内容:`max-width: var(--content-max)`。
- [x] ノート行タイトル横の `internal-icon`（右上矢印）を削除 / 観点:component / 旧モック:各行タイトル右に装飾的な内部リンクアイコン → 実装:`NOTE_TITLE_ROW` はタイトルのみ（アイコン無し。§7.1 の「装飾の賑やかしを増やさない」に整合）/ 修正内容:8 行分の svg と `.internal-icon` CSS を削除。
- [x] note-snippet の font-size を px 直値→token / 観点:token / 旧モック:`font-size: 14px`→ 実装:`text-sm`（#461 ADR-001、fluid・デスクトップ実効13px）/ 修正内容:`font-size: var(--text-sm)`（14px→実効13px へ値が変わる点に注意）。
- [x] note-meta / note-tags / note-date の font-size を px 直値→token / 観点:token / 旧モック:`13px`→ 実装:`text-sm` / 修正内容:`var(--text-sm)`。
- [x] header-search / user-search の input font-size を px 直値→token / 観点:token / 旧モック:`14px`→ 実装:`PUBLIC_HEADER_SEARCH_INPUT` / `USER_SEARCH_INPUT` ともに `text-sm` / 修正内容:`var(--text-sm)`。
- [x] search-icon の left を実装に合わせる / 観点:token / 旧モック:`.search-icon { left: 13px }` + `.user-search .search-icon { left: 14px }`→ 実装:共有 `SEARCH_ICON` が `left-[11px]` / 修正内容:`left: 11px` に統一し user-search 個別オーバーライドを削除。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- ユーザートップのフィルター chip 群（`.filter-row`: すべて / #タグ / タグを追加 / 期間）/ 根拠:`UserPublicTop` は検索 input とノート一覧のみで、タグ・期間フィルター UI を一切持たない / 別Issue化候補:Yes
- 表示モード切替セグメント（`.toolbar .segmented`: リスト / タイル / カレンダー）+ ソートボタン（`.sort-btn` 公開日順）/ 根拠:公開トップに `DisplayModeSwitch` 相当・ソート UI が未実装（認証側 P10 にはあるが公開側には無い）/ 別Issue化候補:Yes

## C. 要判断（曖昧）
- 空状態 `EMPTY_LIST`（#231 既知論点）/ 論点:P30 モックは populated list のみ描き空状態アイキャッチは描いていないが、公開側 `EMPTY_LIST`（`public/styles.ts`）はアイコン無しが意図的（#231 スコープ外）。将来モックに公開側空状態を足す場合もアイコン付きにしない方針か要確認。decisions-pending に登録。
