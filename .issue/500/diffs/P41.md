# P41-admin-llm 突き合わせ
対応: route=app/routes/admin/llm.tsx, components=[admin/LLMSettingsForm/Page.tsx, admin/LLMSettingsForm/index.tsx], styles=[common/styles.ts (pillBtn)]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 管理シェルのヘッダー / 観点:component / 旧モック:menu-btn+search+通知 → 実装:logo+pill+avatar のみ / 修正内容:P40 と同じくヘッダー簡素化。
- [x] プロバイダセクション / 観点:component / 旧モック:Anthropic 固定の locked-display カード（「固定 (ADR-004)」） → 実装:3択 select（Anthropic / OpenAI-compatible / Google Gemini）＋環境変数ロックバッジ＋OpenAI 時の Base URL フィールド / 修正内容:locked-display を `<select>` ベースのプロバイダ選択へ置換。Base URL フィールドはコメントで条件表示を明示。「現在の保存値」ヒントを追加。
- [x] 入力欄スタイル / 観点:token / 旧モック:`border:1px solid hairline; background:bg`（白地＋枠線） → 実装:`bg-surface border-transparent`（surface 塗り、focus で bg+hairline-strong 枠）/ 修正内容:`.input/.select` を surface 塗りベースに変更（#461 入力ノーマライズと整合、h-10=40px 維持）。disabled の opacity-disabled も追加。env-lock バッジ用の `.lock-badge` を追加。
- [x] API キーセクション / 観点:component / 旧モック:「テスト用 API キー」＋保存されない旨＋success/error 2枚並置 → 実装:「新しい API キー」＋「未入力で保存すると現在のキーが維持」＋接続テスト結果は単一 banner / 修正内容:ラベル・ヒント・環境変数名（ANTHROPIC_API_KEY → ADMIN_LLM_API_KEY）を実装に合わせ更新。note-card のアイコンを除去し現在状態テキストを簡素化。
- [x] モデルセクション / 観点:component / 旧モック:select ドロップダウン＋コスト目安テーブル → 実装:自由入力 text フィールド（例示ヒント付き） / 修正内容:select+cost-table を text 入力へ置換。
- [x] フッター / 観点:component / 旧モック:「キャンセル」＋「変更を保存」 / 実装:「保存しました」フィードバック＋「変更を保存」primary のみ（キャンセル無し） / 修正内容:キャンセルを削除、保存成功フィードバックを追加。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- クォータセクション（1ユーザー/24h・インスタンス全体・アラート閾値・レート制限の4入力）/ 根拠:実装にクォータ設定 UI/usecase は存在しない。コスト上限制御は未実装。/ 別Issue化候補:Yes
- モデルのコスト目安テーブル（input/output 単価・用途）/ 根拠:実装はモデルを自由入力 text にしており、コストテーブルや候補リストを持たない。/ 別Issue化候補:Yes
- 接続テストの success/error 詳細（「モデル N 件取得」「認証失敗(401)」等の構造化結果）/ 根拠:実装の test 結果は `接続成功 · 応答 ${ms}ms` / `接続失敗 · ${error}` の簡易表示のみ。モデル件数・HTTP ステータス分解は未実装。/ 別Issue化候補:No

## C. 要判断（曖昧）
- プロバイダ固定（ADR-004）の扱い / 論点:旧モックは「Anthropic 固定」を仕様としていたが、実装は 3 プロバイダ選択に進化済み。ADR-004 のスコープが MVP 限定だったかを確認の上、モックを実装（多プロバイダ）に合わせたが、ADR が依然有効ならモック側が正の可能性も残る。今回は実装を正とした。
