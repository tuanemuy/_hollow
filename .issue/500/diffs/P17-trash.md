# P17-trash 突き合わせ
対応: route=`app/routes/_app/trash/index.tsx`, components=[`trash/TrashList`,`TrashRowActions`], styles=[`layout/styles.ts`,`common/styles.ts`]

共通シェルは SHELL.md A 反映済み。

## A. モック修正（実装に寄せた＝書き換えた）
- [x] ヘッダー shell（アップロード pill 追加 / 新規作成 / placeholder） / 観点:component。
- [x] page-subtitle 文言を実装に差し替え（「削除されたノートは 30 日間保管されます。期限を過ぎたものは…」→「3 件のノートがゴミ箱にあります。保存期間を過ぎたものは自動的に完全削除されます。」） / 観点:component / 実装:`{count} 件のノートがゴミ箱にあります。…`。
- [x] 行アクションをアイコンのみ（`.icon-action` 復元/完全削除）→ ラベル付きピルに変更（実装 TrashRowActions：`復元`(RotateCcw)・`完全削除`(Trash2, danger=error-surface)） / 観点:component+variant / 修正:`pill-btn` / `pill-btn danger`（`.pill-btn.danger` 追加）にラベル併記。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- フィルタツールバー（削除日 select / タイトル検索）。実装 TrashList にフィルタ無し。根拠: 実装に無い。別Issue化候補:Yes。
- 一括選択（行チェックボックス + 選択を復元 / 選択を完全削除 / ゴミ箱を空にする）。実装 TrashList に一括操作・選択 UI 無し（行ごとの復元/完全削除のみ）。根拠: 実装に bulk 無し。別Issue化候補:Yes（ゴミ箱の一括操作）。
- テーブルヘッダ行（タイトル/場所/削除日/残日数）。実装はヘッダ行を持たない 2 カラム（コンテンツ / アクション）リスト。別Issue化候補:No（テーブル化は上記 bulk と一体で検討）。
- 「場所（ディレクトリ）」列 + 「残日数（残 N 日）」countdown ピル。実装の行は タイトル + excerpt + 「削除日 …」のみで、場所・残日数を持たない。根拠: 実装に無い。別Issue化候補:Yes（残日数表示）。
- 空状態（ゴミ箱が空）: 実装は Trash2 アイコン + 「ゴミ箱は空です」+ 「すべてのノートに戻る」CTA。モックは空状態を描いていない（行ありの状態）。実装にあってモックに無い（モックは行あり状態を見せている）。別Issue化候補:No。

## C. 要判断（曖昧）
- **行の構成差（中）**: モックは 6 カラムテーブル（チェック/タイトル/場所/削除日/残日数/アクション）。実装は 2 カラム（タイトル+excerpt+削除日 / 復元・完全削除ピル）。今回は行アクションのみ実装へ寄せ、テーブル全体（チェック列・場所列・残日数列）は B（未実装）として残置。テーブル → 2 カラムリストへの全面書き換えは bulk/filter 未実装と一体のため要判断。
