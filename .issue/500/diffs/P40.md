# P40-admin-dashboard 突き合わせ
対応: route=app/routes/admin/index.tsx, components=[admin/Dashboard/index.tsx (AdminDashboard), admin/route.tsx (シェル)], styles=[—]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 管理シェルのヘッダー / 観点:component / 旧モック:menu-btn + search 入力 + 通知 icon-btn + avatar → 実装:logo + admin-pill + avatar のみ（中央列は空 spacer、ハンバーガー無し・検索無し・通知無し） / 修正内容:`<header>` から menu-btn・search・通知ボタンを削除し、中央列を空 `<div>` に。実装の `route.tsx` は常時水平 nav ストリップ方式で drawer を持たず、ヘッダーは最小構成。
- [x] page-subtitle の文言 / 観点:component / 旧モック:「… · 最終更新 2026-05-16 14:32 JST」 → 実装:「Hollow インスタンス全体の状態」（更新時刻の表示なし） / 修正内容:subtitle から最終更新時刻を削除。
- [x] ステータスバナー領域 / 観点:variant / 旧モック:2-up グリッドで operational + degraded の2枚常時表示＋上部に warning banner（ストレージ80%） → 実装:アラートが無いときは単一の「All systems operational / 重大アラートはありません」バナーのみ（alerts がある場合のみ banner を積む） / 修正内容:warning banner と degraded バナーを削除し、既定状態の単一 operational バナーに統一。meta 文言も実装に合わせて「重大アラートはありません」へ。
- [x] メトリクスカードの sub テキスト / 観点:component / 旧モック:delta 表示（+3 今日 / +12% vs 昨日 / エラー率0.4% 等） → 実装:delta 無し、固定の補助文言（現在の登録数 / R2 … · DO … / 件 / 24h / 回 / 24h） / 修正内容:metric-sub を実装の固定文言へ。delta-positive/negative 表現を削除。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- 上部 warning banner（ストレージ閾値超過などの運用アラートを CTA「詳細」付きで表示）/ 根拠:実装の alerts は AlertDTO ループでバナー表示するが、CTA ボタンや「ストレージ80%」のような具体的閾値アラートの発火元は未実装。アラート種別が揃うまでモックの warning banner を仕様として残す。/ 別Issue化候補:Yes
- 「直近24時間」チャートセクション（アップロード数 / LLM呼び出しの area チャート×2）/ 根拠:実装は時系列チャートを一切持たず「管理メニュー」セクションに置換。チャート描画は未実装。/ 別Issue化候補:Yes
- 「最近のアクティビティ」テーブル（時刻・種類・対象・詳細、tag バリアント付き、モバイルでカード化）/ 根拠:実装にアクティビティ/監査ログのテーブルは存在しない。/ 別Issue化候補:Yes
- メトリクスカードの hover 浮上（surface-elevated）と delta 増減色（success/error）/ 根拠:実装の metric-card は hover 効果・delta を持たない。チャート/アクティビティと併せて将来の充実時に判断。/ 別Issue化候補:Yes

## C. 要判断（曖昧）
- ヘッダーのグローバル検索・通知の扱い / 論点:本Issueでは実装に寄せて削除したが、検索・通知は admin の標準的な affordance であり「未実装で退化」とも読める。シェル全体（P40〜P46 共通）の方針として、削除（実装が正）と維持（モックが正の未実装）のどちらを SSOT とするか要判断。暫定は実装に合わせ削除。
- 実装の「管理メニュー」セクション（各設定への導線説明）/ 論点:モックには無い実装独自セクション。モックへ追記すべきか（実装が正）、それともダッシュボードはチャート/アクティビティを正とし管理メニューは暫定UIとみなすか要判断。今回はモックへ未反映（チャート/アクティビティを残置）。
