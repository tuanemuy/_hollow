# P24-settings-account-delete 突き合わせ
対応: route=`app/routes/_app/settings/account-delete.tsx`(+`settings/route.tsx`), components=[`identity/AccountDeleteForm/Page.tsx`, `identity/AccountDeleteForm/index.tsx`, `identity/SettingsSidebarNav.tsx`, `common/ConfirmDialog`], styles=[`identity/styles.ts`, `common/styles.ts`]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 設定 nav 項目 / 観点:component / 旧モック: …/カスタムプロンプト/言語・タイムゾーン(準備中)/通知(準備中)/アカウント削除(active) → 実装: プロフィール/セキュリティ/プロンプト/アカウント削除(active) / 修正内容: 「カスタムプロンプト」→「プロンプト」、disabled 2項目を削除。
- [x] 入力欄・ラベルのトークン / 観点:token / 旧モック: bordered 白背景 input ＋ label 13px/ink → 実装 `fieldControl`/`fieldLabel`: surface 背景・透明ボーダー、label text-sm/ink-secondary / 修正内容: P21 と同じトークン適用（残存の input-group は B 参照のため CSS は残置）。
- [x] 削除フロー全体 / 観点:component / 旧モック: 「削除によって失われるもの」warning-block ＋ インライン 4 ステップ確認（同意チェック/`DELETE`入力/ユーザー名 input-group/パスワード）＋ disabled「アカウントを削除する」ボタン → 実装(`AccountDeleteForm`): SECTION_DESC ＋ 単一 danger ピル「続けて削除する」→ `ConfirmDialog`（ユーザー名入力で確認）の 2 段フロー / 修正内容: warning-block と confirm-steps を削除し、実装の SECTION_DESC 文言＋「続けて削除する」danger ボタンに置換。
- [x] danger ボタンの配色 / 観点:token / 旧モック: `.pill-btn.danger` = 塗りつぶし `--color-error` 背景＋白文字 → 実装 `pillBtnDanger`(data-danger): `error-surface` 背景＋`error` 文字のフィルドチップ / 修正内容: `.pill-btn.danger` を error-surface 背景＋error 文字に、disabled を `--opacity-disabled` に。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- 「削除によって失われるもの」詳細一覧（ノート件数/メディア容量/公開 URL 410 Gone/限定公開リンク失効数/構造情報/復元不可）/ 根拠: 実装は SECTION_DESC の 1 段落（「ノート、メディア、公開リンク、進行中のエクスポートジョブ…取り消せません」）のみで、件数・容量・410 Gone 等の詳細リストを持たない。/ 別Issue化候補: Yes
- 4 ステップのインライン確認（同意チェックボックス / `DELETE` 入力 / パスワード再入力）/ 根拠: 実装の確認は `ConfirmDialog` 内のユーザー名入力のみ（同意チェック・DELETE 語・パスワード再入力は無し）。/ 別Issue化候補: Yes
- ユーザー名 input の prefix（`hollow.example/`）/ 根拠: 実装はダイアログ内の素の input でユーザー名照合のみ。prefix UI は無い。/ 別Issue化候補: No

## C. 要判断（曖昧）
- 確認フローの厳格度（4 ステップ inline vs modal でユーザー名のみ）/ 論点: モックは同意チェック＋DELETE 語＋ユーザー名＋パスワードの 4 重確認、実装は modal でユーザー名一致のみ。破壊的操作の確認強度として、実装の簡素フローが正か、モックの多段確認を採るべきか（#221 フィードバック原則・破壊操作の確認設計と併せて判断）。今回はモックを実装の 2 段フローに寄せたが、確認強度の設計判断自体は要検討。
- 設定サブナビの配置（コンテンツ内 列 vs 左サイドバー）/ 論点: P21 と同一論点。判断保留（P21 C と統合）。
