# P22-settings-security 突き合わせ
対応: route=`app/routes/_app/settings/security.tsx`(+`settings/route.tsx`), components=[`identity/SecurityForm/Page.tsx`, `identity/SecurityForm/index.tsx`, `identity/SettingsSidebarNav.tsx`], styles=[`identity/styles.ts`, `common/styles.ts`]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 設定 nav 項目 / 観点:component / 旧モック: …/カスタムプロンプト/言語・タイムゾーン(準備中)/通知(準備中)/… → 実装: プロフィール/セキュリティ/プロンプト/アカウント削除 / 修正内容: 「カスタムプロンプト」→「プロンプト」、disabled の2項目を削除（P21と同一）。
- [x] フィールド入力欄・ラベルのスタイル / 観点:token / 旧モック: bordered 白背景 input ＋ label 13px/ink → 実装 `fieldControl`/`fieldLabel`: surface 背景・透明ボーダー・h-10、label `text-sm`/ink-secondary / 修正内容: P21 と同じトークン適用。
- [x] パスワード変更フォーム / 観点:component / 旧モック: 現在/新規/新規（確認）の3 input ＋ パスワードを変更/キャンセル → 実装(`SecurityForm`): 現在/新規の2 input ＋「他の端末からはログアウトする」チェックボックス ＋ パスワードを変更のみ / 修正内容: 「新しいパスワード（確認）」フィールド（error 状態表示含む）を削除、チェックボックス行を追加、キャンセルボタンを削除。新規 PW の placeholder/ヘルプ（強度要件文）は実装に無いため除去。
- [x] メールアドレス変更フォーム / 観点:component / 旧モック: 現在のアドレス preview-line ＋ 新しいアドレス のみ → 実装: `現在: email` 行 ＋ 新しいメールアドレス ＋ **現在のパスワード**（本人確認）/ 修正内容: 現在アドレスを `現在: …` 体裁に、ラベルを「新しいメールアドレス」に、現在のパスワード入力を追加。24時間ヘルプ文は実装に無いため除去。
- [x] セッションセクションの見出し・操作ボタン / 観点:component / 旧モック: 見出し「アクティブセッション」＋ danger-text 小ボタン「他のすべてからログアウト」→ 実装: 見出し「セッション」＋ ACTION_ROW 内 surface ピル「他のすべてのセッションをログアウト」/ 修正内容: 見出しを「セッション」、ボタンを surface ピル＋文言一致に。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- アクティブセッション一覧（端末ごとの行: アイコン/端末名/OS・ブラウザ・地域・IP/最終アクセス/行単位ログアウト, 「このセッション」バッジ）/ 根拠: 実装 `SecurityForm` のセッション機能は「他のすべてのセッションをログアウト」一括操作のみで、個別セッションの列挙・行単位失効を持たない（成功時は無効化件数のみ表示）。/ 別Issue化候補: Yes
- 新しいパスワードの強度要件ヘルプ（12文字以上・3種類以上）/ 根拠: 実装にクライアント側の強度ヘルプ表示が無い（`maxLength` のみ）。/ 別Issue化候補: Yes
- メール変更リンクの有効期限ヘルプ（24時間）/ 根拠: 実装は成功時に「確認メールを送信しました。リンクをクリックすると変更が確定します。」のみ表示。/ 別Issue化候補: No

## C. 要判断（曖昧）
- 設定サブナビの配置（コンテンツ内 列 vs 左サイドバー）/ 論点: P21 と同一論点。実装は `/settings` で左サイドバーを `SettingsSidebarNav` に差し替える方式で、モックのコンテンツ内サブナビ列・`settings-header` は実装に無い。構造的相違はリスクを避け nav 項目・スタイル・フォーム構成の寄せに留めた。判断保留（P21 C と統合）。
