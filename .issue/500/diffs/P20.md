# P20-views 突き合わせ
対応: route=`app/routes/_app/views/index.tsx`, components=[`view/SavedViewsList/Page.tsx`, `view/SavedViewsList/index.tsx`, `view/SavedViewsList/NewViewButton.tsx`], styles=[`view/SavedViewsList/styles.ts`, `layout/styles.ts`, `common/styles.ts`]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] ヘッダー検索プレースホルダ / 観点:component / 旧モック:「ノート、タグ、本文を検索」→ 実装:「ノートを検索」(`Header.tsx`) / 修正内容: placeholder を実装文言に統一。
- [x] ヘッダー「新規」ボタン文言 / 観点:component / 旧モック:`新規` → 実装:`新規作成`(`Header.tsx` の Plus+ラベル) / 修正内容: label を `新規作成` に。
- [x] ヘッダーのアップロードボタン欠落 / 観点:component / 旧モック:新規ボタンの後に無し → 実装:`UploadButton`(Upload アイコン+「アップロード」、`pillBtn`) が常設 / 修正内容: `.pill-btn`（surface）でアップロードボタンを追加。
- [x] サイドバー nav 項目の構成 / 観点:component / 旧モック: ライブラリ(すべてのノート/最近更新/お気に入り)＋ディレクトリ(Research/日記/プロジェクト/雑記)＋管理(タグ/ビュー/ゴミ箱/エクスポート) → 実装(`Sidebar.tsx`): ライブラリ(すべてのノート/保存ビュー)＋ディレクトリ(動的ツリー)＋管理(タグ/ゴミ箱/エクスポートジョブ/アップロード) / 修正内容: 実装に存在しない「最近更新」「お気に入り」を削除、ライブラリに「保存ビュー」を追加（active）、管理を タグ/ゴミ箱/エクスポートジョブ/アップロード に揃える。
- [x] サイドバー nav のアイコン / 観点:component / 旧モック: 各 nav-item に line-icon SVG ＋ count バッジ → 実装: nav-item は `<span>` テキストのみ（§7.1「サイドバーのセクションタイトル・nav はアイコンを使わない」）/ count も無し / 修正内容: nav-item からアイコン SVG と `.count` を除去しテキストのみに。
- [x] セクション見出し「公開ビュー」 / 観点:component / 旧モック:「公開ビュー」＋ `/yumenaut` を含む説明 → 実装(`Page.tsx`):「共有ビュー」＋「公開プロフィールに表示されるビュー。読者が…」/ 修正内容: 見出しを「共有ビュー」、説明文を実装と一致させ `/yumenaut` の code を除去。
- [x] 行アクションの数・種類 / 観点:component / 旧モック: 適用/編集/複製/削除（4つ）→ 実装(`index.tsx`): 適用/編集/名前変更/複製/既定にする(or 既定を解除)/削除（6つ）/ 修正内容: 全行に「名前変更」「既定にする」を追加。既定ビュー（未公開の下書き）行は「既定を解除」を表示。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- 保存ビューのクエリ条件チップ（公開状態/更新範囲/ソート/タグ/ディレクトリ）/ 根拠: 実装(`index.tsx`)の `view-chips` は「表示モードラベル」チップ＋「壊れた条件: N 件」チップのみで、保存ビューのクエリ条件（フィルタ内容）をチップ表示しない。モックの条件チップは保存ビューの中身を一覧で確認できる有用な情報設計であり、実装が未対応。/ 別Issue化候補: Yes
- 「既定」マークのアイコン / 根拠: 実装は lucide `Star`、モックは star polygon。視覚はほぼ同等のため微差として残置。/ 別Issue化候補: No
- 壊れた条件バナーの詳細文言 / 根拠: 実装は条件種別ごとに「削除済み{種別} `名前` を参照しています。」を箇条書き表示し、`修復` ボタンの aria-label を持つ。モックは具体的な参照名・補足説明（ブラックリスト等）を直書き。文面はサンプルでありデータ依存のため残置。/ 別Issue化候補: No
- 表示モードを示すチップ / 根拠: 実装は `view-chips` 先頭に表示モードラベル（リスト表示/タイル表示/カレンダー表示）チップを必ず描画する一方、モックは表示モードを `view-icon-wrap` タイルの `title` で表現する。両者は表示モードの提示方法が異なる（実装はチップ＋タイル両方）。条件チップ（上記）の対応時にまとめて整理するのが妥当。/ 別Issue化候補: Yes（条件チップと同一Issue）

## C. 要判断（曖昧）
- 空状態の体裁 / 論点: 実装は `emptyState`（`px-3 py-12 text-center text-ink-secondary text-sm`）の素テキスト「保存ビューはまだありません。」のみ。モックは空状態ブロックを持たない（常にデータあり）。§7.1 の `EMPTY_STATE`（破線枠＋アイキャッチ）は認証済み側対象だが、本ページの空状態は破線枠もアイコンも持たない簡素版。実装通り素テキストで良いか、`EMPTY_STATE` 体裁に揃えるべきかは #231 と併せて判断。モック側は空状態を描いていないため今回は書き換えなし。

## シェル一次判定（P20 で実施・P21〜P24 へ展開）
- ヘッダー: logo は `max-sm:hidden`（base〜sm では非表示、それ以外は表示）。検索 placeholder「ノートを検索」、右側は `新規作成`(primary pill)＋`アップロード`(surface pill)＋`UserMenu`(アバター)。avatar はクリックでドロップダウン（UserMenu）。
- サイドバー: ライブラリ/ディレクトリ(動的)/管理 の3セクション。nav-item はテキストのみ・カウント無し・アイコン無し。`lg` 未満は drawer、`lg` 以上は sticky 常時表示。
- `settings` 配下では `AppShellDrawer` が左サイドバーを `SettingsSidebarNav`(設定セクション=プロフィール/セキュリティ/プロンプト/アカウント削除) に差し替える。→ P21〜P24 はモックの「コンテンツ内 settings-nav 列」ではなく「左サイドバーに設定 nav」が実装の正（各 P2x の diff 参照）。
