# P45-admin-users 突き合わせ
対応: route=app/routes/admin/users.tsx, components=[admin/UsersTable/Page.tsx, admin/UsersTable/index.tsx (UsersTable, UserRow)], styles=[common/styles.ts (pillBtn/pillBtnSm), Icon]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 管理シェルのヘッダー / 観点:component / 旧モック:menu-btn+search+通知 → 実装:logo+pill+avatar のみ / 修正内容:P40 と同じくヘッダー簡素化。
- [x] フィルタ行 / 観点:component / 旧モック:検索＋状態＋ストレージの3フィルタ、白地 h-36 入力 → 実装:検索＋状態の2フィルタ、surface 塗り h-40 入力（ストレージフィルタ無し）/ 修正内容:ストレージフィルタを削除。入力を surface 塗りへ。状態 option を実装の値（すべて/アクティブ/未確認/一時停止/削除済み）に統一。
- [x] テーブル列構成 / 観点:component / 旧モック:[チェックボックス, ユーザー, 登録日, 最終ログイン, ストレージ, 状態, アクション] → 実装:[ユーザー, 登録日, ロール, 状態, アクション] / 修正内容:checkbox 列・最終ログイン列・ストレージ列を削除し、ロール列（管理者/メンバー tag）を追加。登録日は `YYYY/MM/DD` 形式へ。
- [x] 行アクション / 観点:component / 旧モック:一時停止/強制ログアウト/削除（destructive 赤）→ 実装:状態・ロールに応じて 一時停止/復帰/管理者に昇格/管理者を解除（いずれもアイコン+ラベルの pill-sm。強制ログアウト・削除は無い）/ 修正内容:アクションをロール管理系（promote/demote/suspend/reinstate）へ全面置換。アイコン+ラベル形態に。削除済み行はアクション無し。
- [x] ステータス tag / 観点:variant / 旧モック:アクティブ(ドット付き)/一時停止/削除予定 → 実装:アクティブ/未確認/一時停止/削除済み（ドット無し、tone は success/accent/warning/error）/ 修正内容:tag バリアントを実装の4状態へ。ドットを除去。role-admin tag（accent-surface）を追加。
- [x] アバター配色 / 観点:token / 旧モック:行ごとに異なるグラデーション → 実装:全行 `linear-gradient(135deg,#c9d3df,#8e99a8)` 単一 / 修正内容:行ごとのグラデ差を削除し単一グラデへ。
- [x] 一括操作バー（bulk-bar） / 観点:component / 旧モック:選択数＋一時停止/強制ログアウト/削除 → 実装:行選択 UI 自体が無い / 修正内容:bulk-bar とチェックボックスを削除。
- [x] 空状態 / 観点:variant / 旧モック:無し → 実装:検索0件で「該当するユーザーが見つかりません。」行 / 修正内容:空状態行をコメントで併記。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- 行の複数選択＋一括操作（bulk-bar: 一時停止/強制ログアウト/削除）/ 根拠:実装に行選択・一括操作は無い（§10.5 はテーブルはネイティブ checkbox=A案だが、そもそも選択機構が未実装）。/ 別Issue化候補:Yes
- 行展開（ノート数・アップロード履歴・公開ノート数の統計）/ 根拠:実装に行展開・ユーザー統計表示は無い。/ 別Issue化候補:Yes
- ユーザー削除（ハンドル入力確認付きモーダル、即時削除/削除取消）/ 根拠:実装の admin アクションは suspend/reinstate/promote/demote のみ。ユーザー削除 UI/usecase は無い。/ 別Issue化候補:Yes
- 「強制ログアウト」アクション / 根拠:実装に強制ログアウト（セッション失効）アクションは無い。/ 別Issue化候補:Yes
- ストレージ列・最終ログイン列・ストレージフィルタ / 根拠:実装の UserDTO はストレージ/最終ログインを表に出さない。/ 別Issue化候補:Yes

## C. 要判断（曖昧）
- ユーザー管理の主眼 / 論点:旧モックは「ストレージ・削除・強制ログアウト」運用が主眼。実装は「ロール管理（昇格/解除）＋停止/復帰」が主眼で、削除・ストレージは扱わない。プロダクト方針として削除/ストレージ運用を将来戻すか、ロール管理に絞るかで未実装項目（B）の別Issue化要否が変わる。今回は実装を正とした。
