# P23-settings-prompts 突き合わせ
対応: route=`app/routes/_app/settings/prompts.tsx`(+`settings/route.tsx`), components=[`identity/PromptsForm/Page.tsx`, `identity/PromptsForm/index.tsx`, `identity/SettingsSidebarNav.tsx`], styles=[`identity/styles.ts`, `common/styles.ts`]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 設定 nav 項目 / 観点:component / 旧モック: …active「カスタムプロンプト」/言語・タイムゾーン(準備中)/通知(準備中)/… → 実装: プロフィール/セキュリティ/プロンプト(active)/アカウント削除 / 修正内容: active 項目を「プロンプト」にリネーム、disabled 2項目を削除。
- [x] 入力欄・ラベルのトークン / 観点:token / 旧モック: bordered 白背景 ＋ label 13px/ink / `.prompt-textarea` も bordered 白背景＋shadow-focus → 実装 `fieldControl`/`PROMPT_TEXTAREA`: surface 背景・透明ボーダー、focus で白背景＋アクセントボーダー / 修正内容: `.input/.textarea/.prompt-textarea` を surface 背景・透明ボーダー・h 揃え、label を text-sm/ink-secondary に。
- [x] セクション説明文 / 観点:component / 旧モック:「LLM の挙動をカスタマイズします。…変数は {{input}}/{{title}}/{{existing_directories}} が利用できます。」→ 実装(`PromptsForm`):「各用途について、あなたの分析の意図を補足できます。空欄のままなら システム既定の動作が適用されます。」/ 修正内容: 実装の文言に統一。
- [x] カード説明文 / 観点:component / 旧モック: 用途固有の説明（「アップロードしたメモを Markdown に…」等）→ 実装: 状態説明「個別オーバーライドが有効」/「インスタンスデフォルトを使用中」/ 修正内容: 各カードの desc を実装の状態文言に置換。
- [x] `<details>` の summary と 想定変数 / 観点:component / 旧モック:「インスタンスのデフォルトを表示」、想定変数行なし → 実装:「デフォルトプロンプト」＋ 下部に「想定変数: …」/ 修正内容: summary を「デフォルトプロンプト」に統一、各 details に想定変数の `<small>` を追加。
- [x] textarea ラベルと placeholder / 観点:component / 旧モック: ラベルなし＋placeholder「カスタムプロンプトを入力」/「空欄の場合はデフォルトが使われます」→ 実装: ラベル「あなたの分析の指示（任意）」＋placeholder「あなたの分析の意図を記入（空欄ならシステム既定の動作）」/ 修正内容: 各カードに field-label を追加、placeholder を実装文言に統一。
- [x] 保存・リセットの配置 / 観点:component / 旧モック: ヘッダーの reset-link「デフォルトに戻す」＋末尾グローバル action-row（変更を保存/キャンセル/未保存）→ 実装: 各カード末尾に per-card action-row（保存 ＋ デフォルトに戻す※オーバーライド時のみ）/ 修正内容: ヘッダー reset-link を削除し、各カード末尾に保存ボタン（＋override カードのみデフォルトに戻す）を追加。グローバル action-row を削除。
- [x] プレビューパネル削除 / 観点:component / 旧モック: 2 カードに preview-panel（入力/出力サンプル・「サンプルで実行」）→ 実装: 該当 UI なし / 修正内容: preview-panel ブロックを削除（B にフォローアップ記録）。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- プロンプトのプレビュー機能（入力サンプル → 出力サンプル・「サンプルで実行」）/ 根拠: 実装 `PromptsForm` にプレビュー実行 UI が無く、保存/デフォルトに戻すのみ。モックの preview-panel は有用な機能だが未実装のためモックでは削除し、ここに記録。/ 別Issue化候補: Yes
- 「メタデータ抽出」「OCR 補正」用途のカード / 根拠: 実装は 5 用途（structure/title/directory/metadata/ocr_assist）をループ描画。モックは代表 3 用途のみ。残り 2 用途のカードは未描画。/ 別Issue化候補: No（モックの代表サンプル方針の範囲）
- グローバルの「未保存の変更があります」表示・キャンセル / 根拠: 実装は各カード独立保存で、未保存差分の集約表示やキャンセルを持たない。/ 別Issue化候補: No

## C. 要判断（曖昧）
- 設定サブナビの配置（コンテンツ内 列 vs 左サイドバー）/ 論点: P21 と同一論点。判断保留（P21 C と統合）。
