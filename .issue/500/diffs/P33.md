# P33-share-link 突き合わせ
対応: route=app/routes/share/$token.tsx, components=[public/ShareLinkGate, public/PublicLayout], styles=[public/styles.ts(GATE_*, LOCKOUT, SHARE_*), common/styles.ts(pillBtn, pillBtnTall, pillBtnPrimary)]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] gate-card の背景を surface-elevated→白(bg) / 観点:token / 旧モック:`background: var(--color-surface-elevated)`→ 実装:`GATE_CARD`=`bg-bg`（白）+ border + shadow-xs / 修正内容:`background: var(--color-bg)`。
- [x] gate-submit を実装の tall pill 構成へ / 観点:component,token / 旧モック:`height:44px; font-size:15px`（padding 指定なし）→ 実装:`GATE_SUBMIT`=`pillBtn + pillBtnTall + pillBtnPrimary + w-full`（h-12=48px / px-8 / text-md / justify-center）/ 修正内容:`height:48px; padding:0 32px; font-size:var(--text-md); justify-center` を反映（合成内容をコメントで明記）。
- [x] gate-submit のボタン文言「アクセスする」→「閲覧する」 / 観点:component / 実装:`{isPending ? "確認中..." : "閲覧する"}` / 修正内容:ラベルを「閲覧する」に。
- [x] gate-sub / gate-label / gate-error の font-size を px 直値→token / 観点:token / 旧モック:`14px`/`13px`/`13px`→ 実装:`text-sm` 各所 / 修正内容:`var(--text-sm)`。
- [x] STATE1 の gate-foot 文言を実装に合わせる / 観点:component / 旧モック:「共有リンクは /share/… から開かれています」→ 実装:`GATE_FOOT`「パスワードはサーバー側でのみ検証され、保存されません。」/ 修正内容:foot 文言を差し替え（インライン code 装飾も除去）。
- [x] STATE3(expired) のタイトル/サブ/foot を実装に合わせる / 観点:component / 旧モック:「このリンクは無効です」/「リンクの有効期限が切れている…問い合わせてください。」/「エラーコード: 410 Gone」→ 実装:`isExpiredOrGone` 時「リンクは無効です」/「リンクが失効しているか、削除された可能性があります。」/ foot は常時 `GATE_FOOT`（パスワード未保存文言）/ 修正内容:タイトル・サブ・foot を実装文言へ。
- [x] page の上下 padding を実装に合わせる / 観点:token / 旧モック:`padding: 48px …`→ 実装:`SHARE_PAGE`=`py-14`(56px) / 修正内容:`padding: 56px var(--container-padding)`。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- STATE2「ロック解除後のノート本文インライン表示」一式（`.unlocked` セクション: note-banner / doc-title / author-mini / note-meta-inline(link-pill) / action-toolbar(個別エクスポート・一括エクスポート不可) / content）/ 根拠:実装 `ShareLinkGate` はロック解除に成功すると `router.navigate({ to:"/notes/public/$noteId" })` で **P31(PublicNoteDetail) へ遷移**し、ゲート画面内に本文をインライン描画しない。`SHARE_NOTE_BANNER` 定数は `public/styles.ts` に存在するが未使用。モックのインライン本文表示は実装と構造が異なるため正として残す / 別Issue化候補:Yes（インラインバナー/限定公開アクションの扱いは要検討）

## C. 要判断（曖昧）
- expired/gone 状態の「トップへ戻る」CTA / 論点:旧モック STATE3 は gate-submit 風の「トップへ戻る」リンクを描くが、実装 `ShareLinkGate` の `isExpiredOrGone` 分岐はアイコン+タイトル+サブ+foot のみで CTA を出さない。実装に寄せて CTA を消すか、UX 上有用なのでモック据え置き（実装側に CTA 追加をフォローアップ）かは判断待ち。モックには CTA を残しコメントで明示。decisions-pending に登録。
- STATE2 のロック解除後挙動（インライン描画 vs P31 へ遷移）/ 論点:B に記録のとおり実装は遷移方式。モックのインライン表現を「設計意図（同一URLで本文を出す）」として残すか、実装の遷移方式に合わせてモックを差し替えるか判断待ち。decisions-pending に登録。
