# P46-admin-jobs 突き合わせ
対応: route=app/routes/admin/jobs.tsx, components=[admin/Jobs/Page.tsx, admin/Jobs/index.tsx (JobsBoard, IngestionRow, ExportRow, CleanupSection, SearchIndexSection, InternalLinkBackfillSection, SecretRotationSection)], styles=[common/styles.ts (pillBtn/pillBtnSm), Icon]

## A. モック修正（実装に寄せた＝書き換えた）
- [x] 管理シェルのヘッダー / 観点:component / 旧モック:menu-btn+search+通知 → 実装:logo+pill+avatar のみ / 修正内容:P40 と同じくヘッダー簡素化。
- [x] page-subtitle / 観点:component / 旧モック:「取り込み・エクスポート・クリーンアップ等のバックグラウンドジョブ」 → 実装:「全ユーザーの取り込み・エクスポートジョブの状況と、定期クリーンアップの概要。」 / 修正内容:subtitle を実装文言へ。
- [x] 画面構成 / 観点:layout / 旧モック:状態チップ＋自動リフレッシュトグル＋タブ（すべて/取り込み/…/DLQ）＋単一の混在テーブル＋失敗展開行（スタックトレース）→ 実装:ジョブ種別ごとのセクション（取り込みジョブ / エクスポートジョブ）＋保守オペレーション4セクション（検索インデックス再構築 / 内部リンクバックフィル / マスターキー再暗号化 / クリーンアップ概要テーブル）/ 修正内容:topbar(chips+refresh)・tabs・単一テーブル・展開行を削除し、アイコン付きセクション見出し＋2ジョブテーブル＋4保守セクションへ全面書き換え。
- [x] 取り込みテーブル列 / 観点:component / 旧モック:[ジョブID, タイプ, 対象ユーザー, 状態, 開始時刻, 経過, 再試行, アクション] → 実装:[ジョブ(ID短縮+ファイル名), 状態, 種別, 所有者, 更新, エラー(code+reason), アクション] / 修正内容:列を実装の IngestionRow へ。経過・再試行回数列を削除。エラーは行内に errorCode+errorReason 表示。アクションは失敗時のみ「再実行」（pill-sm + RefreshCw）。
- [x] エクスポートテーブル列 / 観点:component / 旧モック:取り込みと同一の混在行 → 実装:[ジョブ(format·scope), 状態, 進捗(processed/total), 所有者, 作成, エラー, アクション] / 修正内容:エクスポート専用セクション＋進捗列へ。
- [x] ステータス tag / 観点:variant / 旧モック:実行中(pulse ドット)/失敗/リトライ中(2/3)/DLQ/完了 → 実装:取り込み=待機中/処理中/プレビュー中/保存済/失敗/破棄、エクスポート=待機中/処理中/完了/失敗/キャンセル/期限切れ（tone:info/success/warning/error、pulse 無し）/ 修正内容:tag を実装の状態語彙へ。pulse アニメ・ドットを削除。
- [x] 行アクション / 観点:component / 旧モック:詳細＋キャンセル/再実行 → 実装:失敗時の「再実行」のみ（詳細・キャンセルは無い）/ 修正内容:詳細・キャンセルを削除、再実行のみに。
- [x] 制御 JS / 観点:component / 旧モック:auto-refresh トグル・タブ切替の `<script>` → 実装:無し / 修正内容:該当 script を削除。

## B. 実装フォローアップ（モックを正に残した＝未実装）
- 状態サマリーチップ（実行中/失敗/DLQ の件数バッジ）/ 根拠:実装にジョブ状態の集計チップは無い（各セクションの件数のみ）。/ 別Issue化候補:Yes
- 自動リフレッシュトグル（5s ポーリング ON/OFF）/ 根拠:admin/Jobs は client polling を持たず routerInvalidate 手動更新のみ。自動リフレッシュ UI は未実装（index.md のジョブ進捗 polling は ingestion/IngestionQueue 側の話で admin 監視盤は別）。/ 別Issue化候補:Yes
- ジョブ種別タブ（すべて/取り込み/エクスポート/メディアクリーンアップ/ゴミ箱自動パージ/Outbox リレー/DLQ）/ 根拠:実装はセクション分割で固定表示、タブ絞り込みは無い。/ 別Issue化候補:Yes
- 失敗展開行（失敗理由＋スタックトレース pre）/ 根拠:実装はエラーを errorCode+errorReason の行内テキストで表示し、スタックトレース展開は無い（§9 内部 stack を UI に出さない方針とも整合）。/ 別Issue化候補:No（stack 非表示は意図的。理由テキスト拡充のみ将来検討）
- 経過時間・再試行回数列、ジョブのキャンセル/詳細アクション / 根拠:実装の Job DTO は elapsed/retryCount を表に出さず、キャンセル/詳細導線も無い。/ 別Issue化候補:Yes
- メディアクリーンアップ/ゴミ箱パージ/Outbox リレー等を「ジョブ行」として一覧する表示 / 根拠:実装はこれらを cron 駆動の概要テーブル（実行履歴非永続）として扱い、個別ジョブ行にしない。/ 別Issue化候補:Yes（実行履歴永続化は実装コメントでも別Issue予定と明記）

## C. 要判断（曖昧）
- ジョブ監視の粒度 / 論点:旧モックは全ジョブ（クリーンアップ・Outbox 含む）を1つの監視テーブルに混在表示＋自動更新する「リアルタイム運用盤」想定。実装は取り込み/エクスポートのみテーブル化し、cron 系は概要のみ＋保守オペレーションボタンを並べる構成。リアルタイム監視盤に寄せるか、現実装（保守オペレーション盤）を正とするかで B の別Issue化方針が大きく変わる。今回は実装を正とした。
