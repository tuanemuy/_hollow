# 実装計画 — Issue #250: spec/design: index.md「max-width メディアクエリ不使用」規約と実態の乖離

**Issue:** #250
**作成日:** 2026-05-28
**複雑度:** 小規模

---

## 目的

`spec/design/index.md` セクション9 の「`max-width` メディアクエリは使わない」という規約と、`spec/design/pages/` 配下の実態（19ページ・84箇所で `@media (max-width: ...)` を使用）の乖離を解消する。

Issue では Option A（規約を実態に合わせる）が現実的とされている。review 001 由来の mobile fix（44px tap target 等）と特定ブレークポイントを狙ったスタイル絞り込みは意図的なものであり、全ページの書き換えコスト・既存モックの完成度を踏まえると、規約側を緩めるのが妥当。

## スコープ

### 含まれるもの

- `spec/design/index.md` セクション9（実装上の取り決め）の `max-width` メディアクエリに関する記述を、実態に合わせて修正する

### 含まれないもの

- `spec/design/pages/` 配下の各HTMLファイルの修正（Option B 不採用）
- セクション3（レスポンシブ戦略）の「モバイルファースト」記述（こちらは方針として妥当なため変更しない）

## 実装ステップ

### 1. index.md セクション9 の該当行を修正

- **対象ファイル:** `spec/design/index.md`
- **変更内容:** 138行目「レスポンシブは `min-width` ベース。`max-width` メディアクエリは使わない。」を、Issue提案の通り、グローバル mobile fix や特定範囲のみに適用したいスタイルでは `max-width` も許容する旨に書き換える
- **理由:** 実態（84箇所/19ページ）と規約の乖離を解消し、review 001 で導入された mobile fix（44px tap target）を規約上も正当化するため

## 設計判断

- Option A（規約を実態に合わせる）を採択。Issue 本文で「A が現実的」と提案されている通り、書き換えコストと既存モックの意図性（mobile fix 等）を踏まえての判断。`adr.md` を起こすほどのトレードオフではないため未作成。

## リスクと注意点

- ドキュメント1行の修正のため技術リスクはなし。
- 将来 `max-width` の濫用に繋がる懸念はあるが、修正文言で「グローバル mobile fix や特定範囲のみ」と用途を限定することで抑制する。

## テスト方針

- Markdown レンダリング崩れがないかの目視確認のみ。
