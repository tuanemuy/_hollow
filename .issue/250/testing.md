# 動作確認計画 — Issue #250: spec/design: index.md「max-width メディアクエリ不使用」規約と実態の乖離

**Issue:** #250
**作成日:** 2026-05-28

---

## 確認環境

ドキュメント（`spec/design/index.md`）1ファイルの文言修正のみ。アプリケーションの起動・ビルドは不要。

### 検証環境の起動

不要（Markdown 表示確認のみ）。

### デプロイ方法

なし（spec ドキュメントは GitHub 上で参照される）。

## 確認項目

### 1. 修正後の文言が実態と整合している

- **目的:** `spec/design/index.md` セクション9 の修正文言が、`spec/design/pages/` 配下の `@media (max-width: ...)` 使用実態と矛盾しないことを確認する
- **手順:**
  1. `spec/design/index.md` セクション9 の該当行を目視で確認
  2. 「グローバル mobile fix」「特定範囲のみに適用したいスタイル」のような限定的用途を明示していることを確認
  3. `grep -rn "@media (max-width" spec/design/pages/` の結果が、修正文言の許容範囲内に収まることを確認
- **期待結果:** 修正文言と既存の `max-width` 使用箇所（44px tap target、特定ブレークポイントの絞り込み）が論理的に矛盾しない
- **確認ポイント:** 「`min-width` 優先」のニュアンスは残しつつ、例外用途を明確化していること

### 2. Markdown のレンダリング崩れがない

- **目的:** Markdown 表記が壊れていないことを確認する
- **手順:**
  1. GitHub 上で `spec/design/index.md` をプレビュー（または VS Code の Markdown プレビュー）
  2. セクション9 のリスト構造、コードスパン、リンクが崩れていないか確認
- **期待結果:** 修正前と同じレイアウトでレンダリングされる
- **確認ポイント:** バッククォート、リスト記号、改行の崩れがないこと

## エッジケース・異常系

なし（ドキュメント修正のため該当なし）。

## 既存機能への影響確認

- なし。`spec/design/pages/*.html` の挙動・コードには影響しない。Issue 関連 PR (#249) も既存パターンに準拠しているため整合性は変わらない。

## 確認チェックリスト

- [ ] 修正後の文言が `spec/design/pages/` の実態と整合している
- [ ] Markdown のレンダリング崩れがない
- [ ] セクション9 の他の行（インラインスタイル等）は変更されていない
