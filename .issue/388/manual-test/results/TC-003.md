# TC-003: cyclic 除外の維持（MoveDirectoryDialog）

**結果**: PASS
**セッション**: verify-388

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 子孫を持つ Documents を移動対象に MoveDirectoryDialog を開く | 候補に Documents 自身と子孫が出ない | 候補5件: （ルート）, Personal, existing-dir-1779991904395, dup-dir-290, valid-dir-290-ok。Documents / Work / 2024 は出ない | PASS |
| 2 | 「（ルート）」の重複表示確認 | （ルート）が1回だけ | （ルート）が listbox 先頭に1件のみ。root の name=""/path="/" 行は重複表示されない | PASS |

## スクリーンショット

- `screenshots/tc003-move-documents-dialog.png`

## 備考

`getDescendantIds(tree, Documents)` で自身＋子孫（Work, 2024）を除外し、
さらに root を `includeRootOption` として別途「（ルート）」で1件だけ提示する設計通りに動作。
バックエンドの `assertNotCyclicMove` が SSOT で、UI はそのミラー。
