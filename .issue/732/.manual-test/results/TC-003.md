# TC-003: セッション失効後に未認証ランディングへ収束する

**結果**: PASS
**セッション**: verify-tc-003

## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `open` → cookie 注入 → 再 `open` → `wait networkidle` → `snapshot` | 認証済み HomePage | サイドバー「ライブラリ」、ノート一覧21件、「Dev Admin」メニューを確認 | PASS |
| 2 | `cookies clear`（CDP 経由でセッション失効を再現） | cookie 削除 | `✓ Cookies cleared`、`cookies get` の出力が空 | PASS |
| 3 | `open http://localhost:3000/`（leaf 再評価で `authenticated:false` を観測）→ `wait networkidle` → `snapshot` | 未認証 LandingPage へ収束 | banner に「ログイン」「アカウント作成」、hero「散らかった頭の中に、静かな置き場所を」、「WHY HOLLOW」機能紹介を確認。サイドバー/Dev Admin メニューは消失。無表示/HomePage 残存/エラーではない | PASS |

## 所見
セッション失効後、leaf 再評価を経て最終的に未認証 LandingPage へ正しく収束した。無表示固着や HomePage 残存などの退行は発生していない。

## 備考
1 フレームのちらつき（過渡フレームで未認証 UI が一瞬見える問題）は agent-browser では原理的に検証不能（snapshot 時点では過渡フレームは過ぎている）。本 TC では Issue #732 の修正対象である「収束状態の正しさ」を確認した。
