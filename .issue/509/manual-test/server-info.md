# サーバー情報

- URL: http://localhost:3000
- PID: `/tmp/mt-509-server.pid`
- ログ: `/tmp/mt-509-server.log`
- 起動コマンド: `pnpm dev`（vite dev / cloudflare config / port 3000）
- 認証: cookie `__Host-session` = `dev-admin-session-token`（CDP 経由注入）
