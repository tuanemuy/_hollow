# TC-003: P16 ジョブ詳細 /exports/$jobId

**結果**: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|---|---|---|---|
| 1 | failed ジョブ（...113）を開き networkidle 待ち | 200 描画 | 200。heading「エクスポートジョブ詳細」 | PASS |
| 2 | meta グリッド・status・fail summary・failed note ID 確認 | `<dl>` meta グリッド、status、errorReason、failed note ID リスト表示 | `<dl>`（DescriptionList）に ステータス=失敗 / 形式・スコープ=markdown・multiple / 進捗=1/3 / 作成日時 / 完了日時 を term-definition で表示。エラー内容 alert「一部ノートの変換に失敗しました」。失敗したノート ID リスト（b, c）。`<dl>`=`grid grid-cols-2 gap-3 pt-3 mt-1 border-t border-hairline lg:grid-cols-[auto_1fr]`、dt=`text-xs text-ink-tertiary uppercase tracking-[0.05em]`、dd=`text-sm text-ink`、error alert=`bg-error-surface text-error rounded-sm`、h1=`text-3xl font-normal tracking-tightest`（page-title 相当） | PASS |
| 3 | completed ジョブ（...112）を開き artifact/進捗確認 | 崩れず表示 | 200。同 meta グリッド意匠。ステータス=完了 / pdf・single / 進捗=1/1 / 作成日時 / 完了日時 / 有効期限 / ファイルサイズ=200.0 KB。ダウンロード button・一覧へ戻る link。レイアウト崩れなし | PASS |

## 総評

meta グリッド（`<dl>`）はモックの意匠どおり grid + hairline 上罫線でスタイルされ、status バリアントは一覧と一貫。failed の fail summary（error-surface alert）・failed note リスト、completed の artifact 情報（有効期限・ファイルサイズ）がいずれも崩れず描画される。
