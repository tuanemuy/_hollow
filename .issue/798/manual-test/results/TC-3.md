# TC-3: HTML / インラインモードには書式ツールバーも画像ボタンも無い（AC-5）
**結果**: PASS（HTML）／ インラインは対象外（現UIに当該モードが存在しない）
**セッション**: verify-tc-798
## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | エディタのモードタブ一覧を確認 | HTML/インライン/WYSIWYG | 実在タブは「WYSIWYG」「HTML」の2つのみ。「インライン」タブは存在しない | 注記 |
| 2 | モード切替で「HTML」を選択 | HTMLモードに切替 | selectedTab="HTML" | PASS |
| 3 | HTMLモードで書式ツールバー（aria-label="書式"）の有無 | 存在しない | formatToolbarPresent=false | PASS |
| 4 | HTMLモードで `aria-label="画像"` ボタンの有無 | 存在しない | imageButtonPresent=false | PASS |
| 5 | HTMLモード本文下のファイル入力（input[type=file]）の有無 | 存在する | fileInputCount=1（ラベル「画像・動画をドラッグ&ドロップ…」） | PASS |
| 6 | モード切替で「インライン」を選択 | インラインモードに切替 | インラインタブが現UIに存在しないため実行不可 | 対象外 |

## 注記
- 現在の編集モード切替（EditorModeSwitch）の tablist は「WYSIWYG」「HTML」の2モードのみで、テストソースが想定する「インライン」モードのタブは UI 上に存在しない。
- HTML モードについては受け入れ基準（書式ツールバー無し・画像ボタン無し・本文下ファイル入力健在）をすべて満たし PASS。
- インラインモード分はモード自体が現UIに無いため自動検証は対象外（FAIL ではない／実装側でモードが削除または非提供の可能性。要メイン確認）。

## 失敗詳細（FAILの場合）
なし（インライン部分はモード非存在のため対象外）
