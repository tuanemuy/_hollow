# TC-001: UploadDialog のタイトルが SR で読み上げられる（A11y-H1）

**結果**: PASS
**実行時間**: 約 40 秒
**セッション**: verify-tc-001

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/login` を開き existing@example.com / Password123! でログイン | ホームへリダイレクト | `http://localhost:3000/?page=1&limit=20` に遷移 | PASS |
| 2 | ホームのヘッダー「アップロード」ボタンをクリック | UploadDialog が開く | `<h2>アップロード</h2>` が表示される | PASS |
| 3 | `document.querySelector('[role=dialog]')` から `aria-labelledby` 値を取得 | `<h2 id>` と一致 | `aria-labelledby="_r_9_"`、`<h2 id="_r_9_">` で一致 | PASS |
| 4 | `<div role="status" aria-live="polite" class="sr-only">` が dialog 内に存在 | 常設、初期 select view では textContent が空 | role=status, aria-live=polite, class="sr-only", textContent="" | PASS |
| 5 | `aria-modal="true"` の確認 | true | "true" | PASS |

## DOM 検証結果（eval 出力）

```json
{
  "ariaLabelledBy": "_r_9_",
  "ariaModal": "true",
  "idsMatch": true,
  "labelId": "_r_9_",
  "labelTag": "H2",
  "labelText": "アップロード",
  "statusAriaLive": "polite",
  "statusClass": "sr-only",
  "statusExists": true,
  "statusRole": "status",
  "statusText": ""
}
```

## スクリーンショット

- Step 2 (Dialog open): `screenshots/tc-001/step-01-dialog-open.png`

## スキップ項目

- VoiceOver / NVDA による実際の SR 読み上げ確認 — SR ツール依存のため agent-browser ではカバー不可。DOM 上の `aria-labelledby` ↔ `<h2 id>` の一致は検証済み。
