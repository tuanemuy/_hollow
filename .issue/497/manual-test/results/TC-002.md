# TC-002 内部リンク参照トリガーの統一（問題2・3）

- **結果:** PASS
- **対応確認項目:** testing.md 確認項目2（内部リンク参照トリガーの高さ・見た目が期間・公開状態と揃う）

## 手順と観察

1. `/` を未フィルタ状態（通常幅 1280×900）で開いた。
2. 「期間 ▾」「公開状態 ▾」「内部リンク参照 ▾」の3トリガーが破線アウトラインのゴーストチップ（同一高さ h-7、▾付き）で横並びになっている。
3. 以前の大きい「ノートを選ぶ…」ボタン（高さ違い）は存在しない。snapshot 上も内部リンク参照は他2つと同じ `button [expanded=false]` で、ゴーストチップとして描画される。

## 証拠スクショ

- `screenshots/tc-002/triggers-normal.png`
- `screenshots/tc-001/normal-width.png`（3トリガーの高さ揃いが視認できる）

## ソース確認

`app/components/note/list/FilterBar.tsx`：未選択時の内部リンク参照は `className={filterChipGhost}`。`styles.ts` の `filterChipGhost` は `h-7 ... border-dashed`。期間・公開状態の未選択トリガーも同じ `filterChipGhost` を使用しており、3つで完全に一致。
