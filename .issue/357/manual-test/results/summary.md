# テスト実行サマリー — Issue #357

**実行日時**: 2026-05-31
**テストソース**: .issue/357/testing.md
**サーバー**: http://localhost:3000

| TC | テスト名 | 種別 | 結果 | 失敗ステップ |
|----|---------|------|------|-------------|
| TC-003 | ディレクトリ空状態の文言短縮 | 正常系 | PASS | - |
| TC-001 | ディレクトリツリーのハイライト幅統一 | 正常系 | PASS | - |
| TC-002 | ライブラリ/管理ナビの構成 | 正常系 | PASS | - |

**合計**: 3 件（PASS: 3 / FAIL: 0）

## 要点

- **TC-003**: 空状態文言は「ディレクトリがありません」のみ。簡潔で旧来の長文ではない。作成導線は見出し横「+」に残る。
- **TC-001**: 選択・ホバーともハイライトは行ラッパー `div.group.flex...`（left=16, right=243, width=227px）に乗り、Link（167px）ではなく caret 列・「︙」アクション列を含む行全体を覆う。選択フォルダAとホバーフォルダBの left/right/width/色が完全一致。`:has()` による行ラッパー塗りが意図どおり機能していることを実測で確認。
- **TC-002**: ライブラリに「保存ビュー」、管理に「エクスポートジョブ」が追加済み。`/views?kind=personal`（H1「保存ビュー」）・`/exports?offset=0`（H1「エクスポートジョブ」）ともパラメータ不足エラーなく正常表示。デッドリンクなし。

## 備考（アプリ不具合ではない／スコープ外）

- signup 後にメール確認が必須。dev は実メール未配信のため、ローカル D1 の `verifications` トークンを使い `/verify-email?token=...` で確認完了させた（テスト環境都合）。
- 「アップロード」リンクの href は `/#upload`（ホーム内アップロードモーダルの URL hash、spec P13 準拠）で `/upload` 専用ルートへの直リンクではない。デッドリンクではないため PASS。本Issueでは変更していない既存リンク。
