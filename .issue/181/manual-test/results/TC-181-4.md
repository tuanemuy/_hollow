# TC-181-4: ディレクトリを削除でき、UI 上でエラーなく完了する

**結果: BLOCKED（TC-181-2/3 が未達のため未実行）**

## 目的
作成したディレクトリを削除でき、UI 上でエラーなく削除が完了することを確認する。
（削除後、ディレクトリ名 / directory ID / SavedView 名を D1 検証用にメモする想定だった）

## 状況
削除対象のディレクトリが存在せず（TC-181-2 BLOCKED）、認証済みアプリ画面に到達できないため未実行。
記録すべきディレクトリ名・ID・SavedView 名は取得できなかった。

テスト方針に従い BLOCKED として未実行。
