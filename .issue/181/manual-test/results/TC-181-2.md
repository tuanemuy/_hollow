# TC-181-2: ディレクトリを作成できる

**結果: BLOCKED（前提のログインがメール認証必須で達成できないため未実行）**

## 目的
ディレクトリ（例: "TestDir181"）を作成できることを確認する。

## 状況
ディレクトリ作成 UI（ノート一覧のディレクトリツリー）は認証済みのアプリ画面内にある。
TC-181-1 で作成したユーザーはメール認証が未完了でログインできず、`/`（未認証ではランディング表示）
および `/views`（"エラーが発生しました"）に到達できないため、ディレクトリツリーへアクセスできない。

テスト方針（DB 直接操作はしない）に従い、本 TC は BLOCKED として未実行。
