# Round 3（最終）レビュー — Issue #701 計画（観点: 要件カバレッジ・スコープ整合性）

レビュー対象: `.issue/701/plan.md` / `.issue/701/adr.md`
観点: 受け入れ条件7項目の AC への網羅・検証可能性、Round 1・2 指摘の解消、スコープ整合性。本ラウンドは「ブロッカーが残っているか」の最終確認のみ。

## Round 1・2 指摘の最終解消確認

| 指摘 | 最終状態 | 判定 |
|---|---|---|
| R1 P-001（AC-6「失敗の明示」が観測不能） | ADR-005 Accepted。AC-6 が「`class="ingestion-failure-note"` 段落＋空相当本文＋`previewing` 到達＋本文追記して commit 可」の観測可能文に確定。 | 解消 |
| R1 P-002（AC-1 接続テスト境界が曖昧） | AC-1 に「probe でモデル存在/認証まで確認が合格境界」明記。ADR-006 Accepted、transcribe 疎通は AC-3／手動へ割当。 | 解消 |
| R1 S-001 / S-002（AC-3 導線・AC-5 回帰） | ステップ15に `accept` 確認、ステップ16・テスト方針に AC-5 メタデータ欠落回帰を紐付け。 | 解消 |
| R2 P-001（未設定 Stub の catch 漏れ） | ADR-005 に「未設定時の扱い」節を追加。縮退対象を `SpeechFailureError` のみに限定し、Stub の `unsupported_format` は従来どおり `markFailed`。テスト方針に境界1ケース追加。 | 解消 |
| R2 S-001（AC-6 commit まで観測） | ステップ16・テスト方針 手動節に「縮退 preview → 本文追記 → commit 成功（AC-6 最終節、coverage S-001）」を追加。 | 解消 |
| R2 arch S-001/S-002（env.ts 不要・suggestMetadata 共通経路） | ステップ10で env.ts を「変更不要」に、ステップ5・ADR-005 に early-return で metadata ブロック回避を明記。 | 解消 |

Round 1・2 の coverage 指摘（P-001/P-002/S-001/S-002 ×2ラウンド）と arch 指摘はすべて plan.md レビュー履歴・本文・ADR に反映済み。蒸し返す点なし。

## 要件カバレッジの最終確認

Issue 受け入れ条件7項目 → AC-1〜7 の写像を最終照合した。

- 条件1（`/admin/speech` 設定・env>db・暗号化・接続テスト）→ AC-1。probe 境界が明示され検証可能。
- 条件2（DI 注入で Stub 置換、request/consumer 両パス）→ AC-2。ステップ10で両パスの波及箇所を行番号付き列挙。
- 条件3（音声アップロード→文字起こし→構造化→preview→保存）→ AC-3。ステップ15で `accept` 受理確認も担保。
- 条件4（録音→取り込み→ノート化、状態・権限拒否）→ AC-4。ステップ14・15。
- 条件5（`MediaAsset(kind='source')` 保存・元ファイル閲覧、#452 踏襲）→ AC-5。既存フロー合流＋録音由来 Blob メタデータ回帰 TC。
- 条件6（失敗時も空テキストで preview 保存・失敗明示）→ AC-6。ADR-005 で観測可能文に確定。
- 条件7（spec/ADR 更新）→ AC-7。ステップ16・17で対象ドキュメント列挙。

7項目すべてが AC に1対1で写像され、各 AC が検証可能な合否境界を持つ。

## スコープ整合性の最終確認

「含まれないもの」節で `UserSpeechOverride` / locale 可変化 / 第2プロバイダ / Workers AI 経由 / 話者分離・タイムスタンプを根拠付きで除外。いずれも Issue 明記のスコープ外宣言・ユーザー方針1〜3と整合。スコープ外混入なし、スコープ縮小による要件欠落もなし。

---

## 問題点（要修正）

問題点ゼロ。

受け入れ条件7項目すべてが検証可能な AC に写像され、Round 1・2 の全指摘（coverage・arch とも）が反映済み。スコープ整合性も保たれている。要件カバレッジ・スコープ整合性の観点でブロッカーは残っていない。

## 改善提案（検討推奨）

なし。Round 2 の S-001（AC-6 commit までの観測）は既にステップ16・テスト方針 手動節へ取り込まれており、追加で挙げるべき改善はない。

## 良い点

- AC 表の各行が「由来（Issue 条件番号）」「対応ステップ」を持ち、要件→AC→ステップ→テストのトレーサビリティが完結している。
- AC-6 が「縮退 preview の挙動」だけでなく「未設定（Stub）時は縮退対象外で `markFailed`」という除外境界まで含めて確定しており、誤実装を防ぐ合否境界になっている。
- スコープ外項目を「なぜ今やらないか」の根拠付きで明示し、将来拡張の余地（registry・`baseURL` 追加・`IngestionPreview` failure フィールド）も記録されている。

---

## 返答

- 問題点: 0 / 改善提案: 0
- （指摘なし。受け入れ条件7項目すべて検証可能な AC に写像済み、Round 1・2 指摘は全件反映、スコープ整合性も保たれており、ブロッカーは残っていない）
