# TC-002 bio 文字数カウンタ（B-2）

**確認項目:** testing.md 2（自己紹介の文字数カウンタ）+ エッジケース2（bio 上限 500 / maxLength）
**結果:** PASS

## 操作ログ

| # | 操作 | 期待 | 結果 |
|---|------|------|------|
| 1 | `/settings/profile` を開く | bio textarea 直下に `37 / 500` 表示 | OK（カウンタ=`37 / 500`、textarea 値長=37、`maxlength=500`） |
| 2 | bio に5文字追記 | カウンタ増加（500 上限） | OK（`42 / 500`、値長=42） |
| 3 | Backspace で7文字削除 | カウンタ減少 | OK（`35 / 500`、値長=35） |

## スクリーンショット

- 初期 `37 / 500`: `../screenshots/tc-002/step-01-bio-counter.png`
- 追記後 `42 / 500`: `../screenshots/tc-002/step-02-after-type.png`
- 削除後 `35 / 500`: `../screenshots/tc-002/step-03-after-delete.png`

## 備考

- カウンタは `N / 500`（160 ではない）で表示され、入力・削除にリアルタイム追従。
- textarea の `maxlength` 属性 = `500` を確認（エッジケース2: 上限制御が効いている）。500 文字フルの入力検証は省略（手順どおり maxLength の存在確認で可）。
