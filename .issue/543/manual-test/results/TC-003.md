# TC-003 ユーザー名 URL prefix・プレビュー・レート制限ヘルプ（B-3）

**確認項目:** testing.md 3
**結果:** PASS

## 操作ログ

| # | 操作 | 期待 | 結果 |
|---|------|------|------|
| 1 | `/settings/profile` を開く | username input(`newUsername`) の左に prefix 表示 | OK（prefix=`http://localhost:8787/u/`） |
| 2 | 初期プレビュー確認 | 実 appUrl ベース、ダミードメインなし | OK（プレビュー行に `http://localhost:8787/u/`、`hollow.example` 不在） |
| 3 | レート制限ヘルプ確認 | 「30日に1回」、90日でない | OK（`ユーザー名は30日に1回まで変更できます。`、`90日` 不在） |
| 4 | 「次に変更できる日付」算出値の有無 | 出ていないこと | OK（次回変更可能日の算出表示なし） |
| 5 | input に `new-name` を入力 | プレビューが `http://localhost:8787/u/new-name` に更新、`//u/` 二重スラッシュなし | OK（プレビュー=`http://localhost:8787/u/new-name`、二重スラッシュ検出なし） |

## スクリーンショット

- 初期（prefix・空プレビュー・rate help）: `../screenshots/tc-003/step-01-username-initial.png`
- `new-name` 入力後プレビュー: `../screenshots/tc-003/step-02-username-typed.png`

## 備考

- prefix・プレビューとも実 appUrl（`http://localhost:8787`）ベース。ダミー `hollow.example` は不在。
- レート制限ヘルプは **30日**。90日表記なし。「次に変更できる日付」のような算出値も非表示（虚偽表示回避）。
- プレビュー結合で `//u/` の二重スラッシュは発生せず。
- エッジケース1（APP_URL 未設定の相対 `/u/` フォールバック）は別 TC で SKIP 記録（サーバー再起動が必要・ユニットテストで担保）。
