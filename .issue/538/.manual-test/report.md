# ブラウザ検証レポート — Issue #538

**実行日**: 2026-06-13
**テストソース**: .issue/538/testing.md
**サーバー**: http://localhost:3100（pnpm dev、ローカル D1 / dev-admin セッション）
**結果**: 14 件すべて PASS（FAIL: 0、起票 Issue: なし）

## 概要

アップロード導線の「投げっぱなし＋キューで編集・保存」再設計を、受け入れ基準 AC-1〜AC-6 由来の確認項目 8 件・エッジケース 5 件・回帰確認 1 式でブラウザ検証した。詳細は `results/` 配下の各ファイルを参照。

- モーダルは送信後すぐ `queued` 結果ビューに着地し、推論待ち・編集をモーダル内で抱えない（TC-001〜003, 007）
- `previewing` 行の「編集」から開く編集ダイアログでタイトル・ディレクトリ・タグ・Front Matter を編集して保存・破棄・再生成できる（TC-004, 005）
- ヘッダーバッジは両アップロード経路・保存・破棄の操作後にリロードなしで追随し、0 件で非表示（TC-006, EDGE-004, 005）
- クロスタブ競合・アップロード失敗系は許容トレードオフ（adr.md ADR-001/002）どおりに振る舞い、クラッシュなし（EDGE-001〜003）
- 既存の行アクション・`#upload` ハッシュ開閉・カスタムプロンプト・ノート一覧反映に回帰なし（REGRESSION）

## 制限事項

- サーバー側の部分失敗（EDGE-001）は静的ファイルでは再現不能のため、表示はユニットテストのカバーで代替確認。
- visible 復帰（EDGE-005）は `visibilitychange` のプログラム発火による準実機確認。

## フォローアップ候補（本 Issue 範囲外）

- 編集フォームでタグを削除しても Front Matter 由来タグが保存後に残存する（`commitIngestionPreview` の既存マージ挙動、回帰ではない）。
