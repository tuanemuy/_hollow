# TC-006: ヘッダーのキュー件数バッジ（確認項目 6 / AC-5）

**結果**: PASS

**セッション**: verify-tc-006 / **日時**: 2026-06-13 / **URL**: http://localhost:3100

## 実行ログ

| # | 手順 | 操作 | 結果 |
|---|---|---|---|
| 0 | 前提整備 | 過去テスト残存の previewing ジョブ 3 件を `/upload` から破棄して未処理 0 件にした | OK |
| 1 | 0 件でバッジ非表示 | `/upload` をリロードしヘッダー確認 | アクセシブルネームが `アップロード`（件数なし）、バッジ非表示 — OK |
| 2 | モーダル経由で +1 | `/` でヘッダーのアップロードボタン → `#upload` モーダル → `test-note-1.md` を送信 | queued ビュー表示と同時に（リロードなしで）ヘッダーが `アップロード（未処理 1 件）` に — OK |
| 2' | aria-label に件数 | `get attr` で確認 | `aria-label="アップロード（未処理 1 件）"` — OK |
| 3 | `/upload` の UploadForm 経由で +1 | `/upload` ページのドロップゾーンから `test-note-4.md` を送信（モーダルでない経路） | リロードなしで `アップロード（未処理 2 件）` に — OK |
| 4 | 保存（commit）で -1 | previewing 行の「編集」→ ダイアログの「登録」 | `/notes/019ebe3e-...` に遷移し、バッジが `未処理 1 件` に減少 — OK |
| 5 | 破棄で 0 件 → 非表示 | 残り 1 件を「破棄」→ 確認ダイアログ「破棄」 | バッジ消失、aria-label も件数なしの `アップロード` に — OK |

## 確認ポイント

- 各操作直後（ページリロードなし）にバッジ件数が追随した。
- モーダル経路・UploadForm（`/upload`）経路の両方で更新を確認。
- 0 件時はバッジ非表示かつ aria-label から件数表記が消える。
