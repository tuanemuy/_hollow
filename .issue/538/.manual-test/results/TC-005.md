# TC-005: 編集ダイアログからの破棄・再生成

**対応:** testing.md 確認項目 5（AC-4）
**セッション:** verify-tc-005
**実行日:** 2026-06-13
**結果**: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|---|---|---|---|
| 1 | クッキー注入後 `/upload` を開く | `previewing` 行が表示される | 「プレビュー可能」行（test-note-1 / test-note-4 ほか）を確認 | PASS |
| 2 | test-note-1.md（previewing）の「編集」を押す | 編集ダイアログが開く | 行を viewport 内にスクロールしてからクリックでダイアログ「プレビューを編集: test-note-1.md」が開いた | PASS |
| 3 | ダイアログ内の「破棄」を押す | 破棄が実行できる | 確認ダイアログ「ジョブを破棄 / このジョブを破棄しますか？」が表示された | PASS |
| 4 | 確認ダイアログで「破棄」を確定 | ダイアログが閉じ、行が破棄済み表示になる | 編集・確認ダイアログとも閉じ、行はデフォルト一覧から消え、「破棄済みを表示」トグルで「破棄済み」バッジ付きで表示された。ヘッダーバッジも 3→2 に減少 | PASS |
| 5 | test-note-4.md（previewing）の編集ダイアログを開き「再生成」を押す | ダイアログが閉じ、キュー行で進捗追跡（モーダル内待機にならない） | クリック直後にダイアログが閉じ、キュー一覧に戻った。モーダル内のスピナー待機は発生しなかった | PASS |
| 6 | 再生成後のキュー表示を観察 | `pending/processing` 進捗を経て `previewing` に復帰 | 数秒以内に行が「プレビュー可能」となり、提案タグが「#週次レビュー #プロジェクト管理 #開発作業」→「#implementation」に変化（再生成の実行を確認）。pending/processing の中間表示は LLM 完了が速くポーリング間隔内に終わったため目視できず | PASS |
| 7 | `previewing` 復帰後に再度「編集」を開く | 新しい提案値が見える | ダイアログのタグ欄が「implementation」（再生成後の新提案）になっていた | PASS |

## 備考

- 手順 6 の `pending / processing` 中間表示は再生成完了が約 2〜3 秒と速く、ポーリング描画を捕捉できなかった（最終状態と新提案値で再生成の成功は確認済み）。
- テストツールの制約: 「編集」ボタンが viewport 外にあると `click` が成功扱いのままダイアログが開かない事象が再現した。`scroll down` で行を画面内に入れてからクリックすると確実に開く。アプリ側のバグではなくキーボード/実マウス操作では問題にならない見込みだが、Playwright 系の自動操作では注意。
- 上記調査中の誤クリックで test-note-2.md が「ノートとして保存」（commit）された（キュー行アクションが正常動作している副次確認。バッジ 4→3、ノート数 15→16 に追随）。
