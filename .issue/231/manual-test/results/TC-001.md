# TC-001: ヘッダー検索アイコン（#217 同時解消）

**結果**: PASS
**実行時間**: 約60秒

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | existing@example.com でログイン後、`/` を開く | ログイン後ホーム表示 | `/?page=1&limit=20` 表示成功 | PASS |
| 2 | ヘッダー検索ボックス領域の `<svg>` を取得 | `lucide-search` SVG が存在、`currentColor` で stroke、`aria-hidden="true"` | class=`lucide lucide-search ... text-ink-tertiary ...`、stroke=`currentColor`、aria-hidden=`true`、16x16 | PASS |
| 3 | 描画形状確認（path,circle 構成） | 虫眼鏡（円+ハンドル） | `<path>` + `<circle>` 構成 → 虫眼鏡形状で描画 | PASS |
| 4 | 親要素のテキストでフォームバインドを確認 | 「ノート検索」フォーム内に配置 | parentTag=FORM, parentText=「ノート検索」 | PASS |

## スクリーンショット
- `.issue/231/manual-test/screenshots/tc-001.png`

## 備考
- mask-image 撤去 → lucide `<Search />` への置換が反映済み。#217「豆腐」現象は解消。
