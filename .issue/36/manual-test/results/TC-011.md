# TC-011 — HTML モードでは Suggest が起動しない

**Result:** PASS
**Session:** `verify-tc-011`
**Date:** 2026-05-19

## Steps

1. Login, open note A edit, leave the HTML tab selected (default).
2. Focus the body `<textarea>` and type ` [[Hel`.

## Observations

- 0 listbox elements in DOM.
- 0 option elements.
- Plain `[[Hel` characters appear in the textarea value.

The Mention/Suggestion extension is wired only into `WysiwygEditor.tsx`; the `HtmlEditor.tsx` plain textarea has no such behaviour. This matches the scope decision in plan.md ("含まれないもの: WYSIWYG タブ + suggestion プラグインの…テスト") and is the correct behaviour for S-002.

## Evidence

- screenshots/tc-011/step-html-mode.png

## Verdict

PASS — popup is scoped to WYSIWYG mode only.
