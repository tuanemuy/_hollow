# TC-013 — Editor 再生成リスク（P-004）/ autosave との共存

**Result:** PASS
**Session:** `verify-tc-013`
**Date:** 2026-05-19

## Steps

1. Login, open note A edit, WYSIWYG.
2. Tag the live `.ProseMirror` element (`window.__editorRef = el; el.dataset.testId = …`).
3. Perform 3 consecutive `[[xxx` → Enter suggest operations.
4. After each operation, compare:
   - `cur === window.__editorRef` (same React instance / same DOM node?)
   - `cur.dataset.testId === window.__editorId` (custom attribute survived?)
   - `document.querySelectorAll(".ProseMirror").length` (still exactly 1?)
   - autosave indicator present?

## Observations

After 3 suggest operations (insertions: `[[Helsinki]]`, `[[Hello World]]`, `#draft`):

| Check | Value |
| --- | --- |
| `sameRef` | true |
| `sameId` (custom dataset attr survived) | true |
| `.ProseMirror` count | 1 |
| autosave "保存" indicator visible | true |

No re-mount; autosave continues to run. `lastEmittedHtmlRef` guard is effective.

## Evidence

- screenshots/tc-013/step-multiple-inserts.png

## Verdict

PASS — repeated suggest insertions do not unmount/remount the TipTap editor; autosave keeps running.
