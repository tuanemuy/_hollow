# TC-012 — 連続入力時のリクエスト抑制（debounce / abort）

**Result:** PASS
**Session:** `verify-tc-012`
**Date:** 2026-05-19

## Steps

1. Login, open note A edit, WYSIWYG.
2. Clear network request log.
3. Type ` [[Helsinki` rapidly (10 characters total including space + `[[` + 7 letters).
4. Inspect captured network requests.

## Observations

After the rapid typing, only **1 POST** to `searchInternalLinkTargetsFn` (decoded from base64 path: `searchInternalLinkTargetsFn_createServerFn_handler`) was logged, instead of ~7 (one per matching keystroke).

```
[21948.570] POST /_serverFn/…searchInternalLinkTargetsFn_createServerFn_handler (Fetch) 200
[21948.571] POST /_serverFn/…saveNoteDraftFn_createServerFn_handler (Fetch) 200
```

This is consistent with the planned 100 ms debounce + AbortController in `internalLinkExtension.ts`.

## Evidence

- screenshots/tc-012/step-after-rapid-type.png

## Verdict

PASS — typing 8 query characters in quick succession resulted in 1 server-fn call. Debounce + abort is effective.
