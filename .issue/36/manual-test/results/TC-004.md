# TC-004 — キーボード操作 ↑↓ / Enter / Esc（受入条件 3）

**Result:** PASS
**Session:** `verify-tc-004`
**Date:** 2026-05-19

## Steps

Open note C, WYSIWYG mode, type ` [[Hel` to bring up 2-option popup, then:

1. Initial state — index 0 (`NHello World`) selected.
2. ArrowDown → index 1 (`NHelsinki`) selected.
3. ArrowDown (wrap test) → index 0 selected.
4. ArrowUp from index 0 → index 1 (`NHelsinki`) selected — last item by wrap.
5. Press Escape → popup closes, editor still contains `[[Hel` text.

## Observations

| Step | aria-selected before | aria-selected after | Verdict |
| --- | --- | --- | --- |
| ArrowDown from 0 | [true, false] | [false, true] | PASS |
| ArrowDown from 1 (wrap) | [false, true] | [true, false] | PASS — wrapped to first |
| ArrowUp from 0 (wrap) | [true, false] | [false, true] | PASS — wrapped to last |
| Escape | listbox present | listbox removed (count=0) | PASS |
| After Esc | — | editor text unchanged (`[[Hel` remains) | PASS |

Enter selection was already exercised in TC-002 (ArrowDown + Enter inserted `[[Helsinki]] `) and TC-003 (Enter inserted `#draft `).

aria-selected is set on exactly one option at a time, confirming the requirement.

## Evidence

- screenshots/tc-004/step-esc-closed.png

## Verdict

PASS — all 4 keys (↑ ↓ Enter Esc) behave as required.
