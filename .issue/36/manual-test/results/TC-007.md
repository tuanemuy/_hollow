# TC-007 — trashed ノートは候補に出ない

**Result:** PASS
**Session:** `verify-tc-007`
**Date:** 2026-05-19

## Steps

1. Mark note B (`Helsinki`) as trashed via DB:
   `UPDATE notes SET status='trashed', trashed_at='…' WHERE id='{noteB.id}'`.
2. Login, open note A edit, WYSIWYG, type ` [[Hel`.
3. Restore note B (`status='active'`, `trashed_at=NULL`).
4. Re-type `[[Hel` in the same session.

## Observations

| Phase | Popup options |
| --- | --- |
| Note B trashed | `["NHello World"]` |
| Note B restored | `["NHello World", "NHelsinki"]` |

Trashed note is excluded; restoration brings it back.

## Test-harness note

The first attempt used the in-UI delete button which triggers a JS `confirm()` dialog that froze agent-browser. To avoid the dialog issue I trashed the row directly in D1 (the adapter's exclusion clause `WHERE status='active'` is what the test actually verifies). Restoration was likewise done in DB.

## Evidence

- screenshots/tc-007/step-1-trashed-excluded.png

## Verdict

PASS — `searchByTitlePrefix` excludes trashed notes (consistent with the JSDoc contract in the port).
