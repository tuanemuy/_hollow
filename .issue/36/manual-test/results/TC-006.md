# TC-006 — オーナー隔離（受入条件 4）

**Result:** PASS
**Sessions:** `verify-tc-001` (main user direction) + `verify-tc-006` (user X direction)
**Date:** 2026-05-19

## Direction 1 — main user (TestUser) does not see X's notes

(Captured in TC-001.)

- Main user types `[[Hel` in WYSIWYG editor of note A.
- Popup options: `NHello World`, `NHelsinki`.
- `Heliopolis` (owned by X) is NOT in the list.

## Direction 2 — user X does not see TestUser's notes

- Login as `test-x@example.com`, open `/notes/{x.heliopolisNoteId}/edit`, WYSIWYG.
- Type `[[Hel`.
- Popup options: `["NHeliopolis"]` only.
- `Hello World`, `Helsinki` (owned by TestUser) are NOT in the list.

## Evidence

- screenshots/tc-006/step-1-x-only.png

## Verdict

PASS — owner scoping holds in both directions. The `actorUserId === ownerId` enforcement at the repository port signature is observed end-to-end.
