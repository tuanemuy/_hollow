# TC-003 — タグ候補を選択すると `#tagname` 形式で挿入される（ADR-002）

**Result:** PASS
**Session:** `verify-tc-003`
**Date:** 2026-05-19

## Steps

1. Login as `test-user@example.com`
2. Open `/notes/{noteC.id}/edit` → WYSIWYG tab
3. Focus editor, type `[[dra`
4. Press Enter (single option `draft` is auto-selected)
5. Submit to save

## Observations

- Popup shows single option `#draft` with `#` icon (kind=tag).
- After Enter, editor body contains `#draft ` (no brackets, trailing space).
- After save, `note_tags` row:
  ```
  note_id=019e3c61-297d-713f-98dd-9f44e4f6c1c0 (note C)
  tag_id=019e3c61-7ee1-768a-80ac-02255ed5772d (draft)
  ```

## Evidence

- screenshots/tc-003/step-1-tag-popup.png
- DB query

## Verdict

PASS — tag candidates insert as `#tagname` and the existing `TagService.extractFromHtml` pathway picks them up exactly as ADR-002 specifies.
