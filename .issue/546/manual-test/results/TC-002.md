# TC-002: P03 ログイン — 失敗サマリーの案D 表示 + 未認証 alert 回帰

**結果**: PASS
**セッション**: verify-tc-auth

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|----------|-----------|------|
| 1 | `/login` を open → networkidle → snapshot | ログインフォーム表示 | メール(ref=e5)/パスワード(ref=e7)/保存チェック(e11)/ログインボタン(e9)/「パスワードを忘れた方」リンクを確認 | PASS |
| 2 | メール=`dev-admin@example.com`, パスワード=`WrongPassword999!`(誤り)で送信 | 認証失敗 | `form.requestSubmit()` で送信、認証失敗 | PASS |
| 3 | エラー出現待ち → `tc-002-login-fail.png` 撮影 | スクショ取得 | `wait --text "ログインできませんでした"` 成功、撮影成功 | PASS |
| 4 | 「ログインできませんでした。」title のアイコン付き枠線アラート（案D）確認 | 案D サマリーアラート表示 | `role="alert"`、`bg-bg`（白地）+ `border border-[color-mix(...alert-accent 30%...)]`（ヘアライン枠）+ `[--alert-accent:var(--color-error)]`（error色）、AlertCircle アイコン(svg)あり、title「ログインできませんでした。」、body「認証が必要です」。スクショでも白地+赤ヘアライン枠+赤アイコン+赤title を視認 | PASS |
| 5 | （補足）未認証ユーザー向け status alert（確認メール再送導線）の仕組み | 仕組みの有無を軽く観察 | コード上 `isUnverified`（`unauthorized` かつ `code==="unverified"`）のとき `role="status"` の status alert（MailWarning アイコン+「メールアドレスの確認が未完了です」+「確認メールを再送」ボタン）が出る実装を確認。今回は検証済みアカウント+誤パスワードのため summary alert が出る（正しい分岐） | PASS（観察のみ） |

## スクリーンショット

- `/Users/hikaru/github.com/tuanemuy/hollow3/.issue/546/manual-test/screenshots/tc-auth/tc-002-login-fail.png`
  （白地 + error色ヘアライン枠 + AlertCircle アイコン + 「ログインできませんでした。」title + 「認証が必要です」body の案D アラート）

## 検証メモ

- アラート DOM 実測値:
  - `role="alert"`
  - class: `flex items-start gap-3 p-4 rounded-lg bg-bg shadow-xs text-left border border-[color-mix(in_oklab,var(--alert-accent)_30%,transparent)] ... [--alert-accent:var(--color-error)]`
  - `hasSvg: true`（AlertCircle）
  - title: 「ログインできませんでした。」 / body: 「認証が必要です」
- 塗りつぶし箱ではなく**白地 + error色の細い枠線**であることをクラス（`bg-bg` + `border ... color-mix 30%`）とスクショの両方で確認。
- 判定基準「『ログインできませんでした。』が出てアイコン併用の枠線アラートなら PASS」を満たす → **PASS**。
