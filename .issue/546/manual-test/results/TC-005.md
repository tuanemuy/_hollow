# TC-005: エッジケース

**結果**: PASS
**セッション**: verify-tc-error

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `open "/error?kind=invalid"` → wait → snapshot | notFound(404) バリアントとして表示。検索ボックス +「一つ前に戻る」（validateSearch の catch("notFound")） | URL が `/error?kind=notFound` に正規化された。heading「ページが見つかりません」/ link「ホームへ戻る」+「検索ページを開く」/ searchbox「ノートを検索」あり / button「一つ前に戻る」あり / Error code: 404 Not Found | PASS |
| 2 | 新規セッション（close→再open）で `/error?kind=notFound` に直接到達 →「一つ前に戻る」click | クラッシュせず標準の history.back 挙動。エラー・白画面にならない | クリック後 url = `about:blank`（ブラウザ初期ページへの標準 history.back）。例外スローなし、コンソールに error なし（Vite/React dev info と無関係な tanstack code-split warning のみ）。クラッシュせず標準挙動を確認 | PASS |

## 補足

- ステップ1: `kind=invalid` は route の `validateSearch` で notFound にフォールバックされ、URL も `kind=notFound` に書き換えられた。アクション構成は TC-004 の notFound と完全一致。
- ステップ2: 新規セッションで直接到達したため app 内履歴は無し（`window.history.length` = 2、ブラウザ初期 about:blank を含む値）。「一つ前に戻る」は標準の history.back を呼び、about:blank に遷移。アプリ側のクラッシュ・白いエラー画面・JS 例外は発生せず、標準挙動どおり。

## スクリーンショット

- /Users/hikaru/github.com/tuanemuy/hollow3/.issue/546/manual-test/screenshots/tc-error/tc-005-invalid-kind.png
- /Users/hikaru/github.com/tuanemuy/hollow3/.issue/546/manual-test/screenshots/tc-error/tc-005-backlink-nohistory.png

## 失敗詳細（FAIL時）

なし。全ステップ期待通り。
