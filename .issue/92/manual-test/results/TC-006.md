# TC-006 (LIKE エスケープ)

- 結果: **PASS**
- URL: `http://localhost:3100/search?q=%` (→ `?q=%25&limit=20`)
- 期待: 「セール情報」（body に "50%"）がヒットし、全件ヒットにはならない（ワイルドカード暴発しない）。

## 実行ログ

| ステップ | 操作 | 結果 |
| --- | --- | --- |
| 01 | open `/search?q=%25`（`%` 1文字） + wait networkidle | OK |
| 02 | snapshot で結果確認 | 1 件ヒット: 「セール情報」（body "本日のセールは50%オフ。お得です"）。全 7 ノートのうち 1 件のみ |
| 03 | screenshot | step-01.png 保存 |

## スクリーンショット

- `.issue/92/manual-test/screenshots/tc-006/step-01.png`

## 判定理由

`%`（LIKE ワイルドカード文字）がエスケープされ、リテラル `%` を含む「セール情報」のみがヒット。エスケープが効かなければ全 7 件がヒットするはずだが 1 件のみで、ワイルドカード暴発しないことを確認。`50%25` 代替試行は不要（`%` 単体で正常動作）。PASS。
