# TC-5: 既存アップロード系列・他カードの回帰（AC-7）

結果: **PASS**

## 手順と結果

| # | 手順 | 結果 |
| --- | --- | --- |
| 1 | アップロード sparkline（aria-label「アップロード数の直近 24 時間の推移」）の描画確認 | SVG path として存在・描画。合計 2 件表示 |
| 2 | 他 scalar（userCount / storage / uploadsToday）確認 | 後述（値は記録のみ） |
| 3 | 「直近 24 時間」セクションが 2 カラム（md 以上）か確認 | `grid grid-cols-1 ... md:grid-cols-2` を確認 |

## 他 scalar 実測値（記録のみ）

| metric | 表示 | 備考 |
| --- | --- | --- |
| ユーザー数 | `—` / 取得失敗 | null 固定のまま（挙動不変）。今回は実数ではなく取得失敗表示 |
| ストレージ消費 | `—`（R2 — · DO —） / 取得失敗 | null 固定のまま（挙動不変） |
| 当日アップロード | `—` / 取得失敗 | null 固定のまま（挙動不変） |
| LLM 呼び出し (24h) | `7` | 本 Issue で追加・実数 |

## 判定根拠

- アップロード sparkline は snapshot に `image "アップロード数の直近 24 時間の推移"` として存在し、SVG path で描画（distinctY=[12,128,140]、合計 2 件）。系列は壊れていない。
- 2 系列の aria-label が正しく出し分けられている:
  - 「アップロード数の直近 24 時間の推移」
  - 「LLM 呼び出し数の直近 24 時間の推移」
- セクションのカード並びコンテナは `grid grid-cols-1 gap-4 max-sm:gap-3 md:grid-cols-2` で、md 以上 2 カラム横並び。
- 他 scalar（userCount / storage / uploadsToday）は従来どおり「取得失敗」表示で挙動不変。

## 期待 vs 実測

- 期待: アップロード系列が壊れていない / LLM カードと 2 カラムで並ぶ / aria-label が 2 系列で正しく出し分け。
- 実測: すべて一致。PASS。
