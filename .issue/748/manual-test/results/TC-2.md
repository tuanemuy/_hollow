# TC-2: scalar「LLM 呼び出し (24h)」が実数 7 で表示（AC-3,4）

結果: **PASS**

## 手順と結果

| # | 手順 | 結果 |
| --- | --- | --- |
| 1 | /admin の「LLM 呼び出し (24h)」metric-card を確認 | scalar = **7** を確認 |

## 判定根拠

snapshot の region "主要メトリクス" 内:

```
- StaticText "LLM 呼び出し (24h)"
- StaticText "7"
- StaticText "回 / 24h"
```

- 値は実数 **7**（「取得失敗」ではない、0 件でもない）。
- 直近24h窓の COUNT = 7（シード合計 7 件、48h 超過の古い 2 行は窓外で不算入）と一致。
- 「直近 24 時間」セクションの LLM カード（LLM 呼び出し数）も合計 **7** を表示し整合。

## 期待 vs 実測

- 期待: 実数 7（直近24h窓 COUNT）/「取得失敗」でない / 0 件でない。
- 実測: 7 で一致。PASS。
