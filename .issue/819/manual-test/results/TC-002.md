# TC-002: 編集ルートが RSC 再構成後も正常描画（AC-2 機能面）

**結果**: PASS
**セッション**: verify-819

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/notes/01950779-...e1/edit` を開く | 編集画面が描画される | title「ノートを編集 — hollow」・URL 一致 | PASS |
| 2 | エディタ本体の存在を検査 | contenteditable/ProseMirror 等のエディタ要素 | `hasEditor=true` | PASS |
| 3 | not-found でないこと | 「見つかりません」非表示 | `notFound=false` | PASS |

## 備考
- `renderNoteEditor` を await せず `NoteEditorSection`（Suspense）を即返す RSC 再構成後も、エディタが正常に描画されることを確認（詳細ルートと同型への収束が機能面で回帰していない）。
