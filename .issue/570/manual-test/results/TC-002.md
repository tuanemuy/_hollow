# TC-002（エッジ）: 失効リンクには QR が出ない
**結果**: PASS

## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|----------|------------|------|
| 1 | 同モーダル内で revoked(失効済み)リンク行を確認 | 失効リンク2件(`...031`/`...032`)が表示 | 2件とも「失効済み」バッジ付きで表示 | PASS |
| 2 | 失効リンク行に QR ブロックが描画されないことを確認 | QR 画像・キャプションが無い | snapshot 上で両 listitem は `StaticText "失効済み"` + `code`(URL) + 「リンクをコピー」ボタン + status のみ。`image "QR コード"` も キャプション paragraph も無し。スクショでも QR 不在を確認 | PASS |

## スクリーンショット
- /Users/hikaru/github.com/tuanemuy/hollow3/.issue/570/manual-test/screenshots/tc-002/revoked-links.png

## 失敗詳細（FAILの場合）
なし
