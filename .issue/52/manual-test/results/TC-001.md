# TC-001: saved_views 行を含むシードでホームが 200 を返す

**結果**: PASS
**実行時間**: 約 35 秒

## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/` を開く | 200 / ランディングまたはログイン画面 | 200 で landing が表示 (`Hollow` ヘッダー、`ログイン` リンク表示) | PASS |
| 2 | `ログイン` リンクをクリック | ログインフォーム表示 | `/login` でメール / パスワード入力フォームが表示 | PASS |
| 3 | `test-user-001@example.com` / `TestPassword123!` を入力してログイン | ホーム `/` に遷移 | `http://localhost:3000/?page=1&limit=20` に遷移 | PASS |
| 4 | ホームに 500 エラー画面が出ていないこと | 500 なし、Note 一覧表示 | `すべてのノート` 見出し / `10 件のノート` / 10 件のリスト / タグチップ (`#design 2`, `#todo 4`, `#work 4` …) / `保存ビュー: 作業中のタスク` セレクトが表示。500 表示なし | PASS |

## スクリーンショット
- Step 1 (landing): screenshots/tc-001/step-01.png
- Step 2 (login form 入力済み): screenshots/tc-001/step-02.png
- Step 3 (login 後のホーム): screenshots/tc-001/step-03.png

## サーバーログ確認
`/tmp/manual-test-server.log` の末尾 50 行を確認。

- `DataIntegrityError` なし
- `Saved view ... query_json.directoryId is not a string|null` なし
- `decodeQueryJson` / `decodeSortJson` 関連の失敗ログなし
- 500 / stack trace の出力なし

修正後の `query_json` (フラット構造、`directoryId/tagIds/dateRange/keyword/referencingNoteId/visibilityFilter` を持つ) と `sort_json` (`{by, direction}`) で `renderHome` が SavedView を rehydrate できることを確認。
