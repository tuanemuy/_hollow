# TC-003: P15-export.html「FrontMatter を含める」サブテキストが新モデルに沿っている（review-001 W-001 受領分）

**結果**: PASS
**実行時間**: 約30秒
**セッション**: verify-tc-003

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `file://` 経由で `spec/design/pages/P15-export.html` を開く | エクスポートモーダルがレンダリングされる | タイトル `Hollow — エクスポート` で正常レンダリング | PASS |
| 2 | オプション「FrontMatter を含める」のサブテキスト確認 | `date / title / description などのメタデータを冒頭に出力`（`tags` / `status` を含まない） | snapshot で完全一致を確認、スクリーンショットでもチェックボックス直下のサブテキストとして表示を視認 | PASS |
| 3 | モーダル全体のレイアウト確認 | 「FrontMatter を含める」「メディアを埋め込む」等のチェックボックス行・推定サイズ・アクションボタンが崩れず並んでいる | スクリーンショット `step-01-p15-frontmatter.png` で配置維持を確認 | PASS |
| 4 | リポジトリ全体に旧スラッシュ区切り文言が残っていないこと | `grep -rn "title / tags / date / status\|title, date, tags, status" spec/design/pages/` のマッチ 0 件 | 出力なし | PASS |

## スクリーンショット

- Step 1（P15 エクスポートモーダル）: `screenshots/tc-003/step-01-p15-frontmatter.png`

## 失敗詳細

なし。
