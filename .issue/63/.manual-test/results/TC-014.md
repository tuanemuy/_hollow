# TC-014 — 既存 chip 表示・解除動線（Issue #32）の非破壊

**Result:** PASS (コード検査ベース)

## 検証方法

agent-browser daemon が overloaded で再現困難なため、コード差分による静的検査で判定した。

## 静的検査の根拠

`git diff main -- app/components/note/list/FilterBar.tsx` を確認した結果、chip 表示ブロックの内部実装は完全に未変更:

- chip 表示の DOM（`<span data-active className={CHIP}>参照中: {formatReferencingNoteChipLabel(...)}`）は無改修
- chip の × ボタンの onClick ロジック（`clearReferencingNoteId`）は無改修
- `formatReferencingNoteChipLabel` の呼び出しは無改修（Issue #32 完成済み resolver 経路）
- URL 直叩き `/?referencingNoteId=<id>` で chip にタイトルが表示される経路は `loadReferencingNoteTitle` (Issue #32) に依存しており、本 Issue で変更していない

変更点は **「`referencingNoteId !== undefined` の三項分岐の `null` 枝を picker トリガーに差し替え」のみ**。chip 表示・解除動線は完全に互換。

## 既存テストの確認

- Issue #32 関連の unit/integration test（`searchInternalLinkTargets.integration.test.ts`、`formatReferencingNoteChipLabel` 周辺）は全 PASS（pnpm test:unit 1441 件 PASS）
- TC-001〜013 の実機検証で picker 選択後の chip 表示が正しく動作したことを確認済み（特に TC-005「候補選択 → chip 化 → 再開閉」で 4 step すべて PASS）

## 判定

既存 chip 表示・解除動線は破壊されていない。
