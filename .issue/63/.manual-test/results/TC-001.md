# TC-001 — picker トリガーの初期表示

**Result:** PASS

## 期待結果との照合

- `referencingNoteId` 未指定状態（URL `/?page=1&limit=20`）でホームを表示
- FilterBar に「内部リンク参照」ラベルが表示される
- ラベル直後に「ノートを選ぶ…」ボタンが表示される（`aria-expanded="false"`、`aria-haspopup="dialog"` 想定）
- chip は表示されない
- 他フィルタ（タグ、期間、公開状態）と同じ FilterBar 内で水平整列している

## 実際のスナップショット

```
- StaticText "内部リンク参照"
- button "ノートを選ぶ…" [expanded=false, ref=e23]
```

FilterBar の他要素（タグ 9 個、開始日 / 終了日 input、公開状態 select）と同じセクション内に並ぶことを確認。

## スクリーンショット

- `/Users/hikaru/github.com/tuanemuy/hollow2/.issue/63/.manual-test/screenshots/tc-001/step-01-home.png`

## 備考

事前準備として、テスト開始前に `saved_views` テーブルに残っていた古いスキーマの行 (`01938f00-0000-7000-8000-00000000d071`、`query_json` が `{"filters":{...}}` 形式) を削除。これがないと home loader が `Saved view ... query_json.directoryId is not a string|null` で SystemError を投げる。詳細は summary.md 参照。
