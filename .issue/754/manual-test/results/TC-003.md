# TC-003: 件数バッジ（AC-6）

**結果**: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|----------|-----------|------|
| 1 | フィルター未適用でトリガーを確認 | 件数バッジ無し/0 | トリガーは「絞り込み」のみ（件数バッジ無し）。全クリア× も非表示 | PASS |
| 2 | シートを開きタグ #design を選択 | トリガーに件数バッジが付き 1 になる / 一覧が絞り込まれる | トリガーが「絞り込み 1」に変化。一覧が「3 件のノート」に絞り込み。シートは開いたまま(expanded=true) | PASS |
| 3 | さらに公開状態=公開を選択 | 件数バッジが 2 に増える | トリガーが「絞り込み 2」に変化。一覧が「2 件のノート」に絞り込み | PASS |
| 4 | 「すべてのフィルタをクリア」で全クリア | 件数バッジが 0（消える）/ 一覧が全件に戻る | トリガーが「絞り込み」のみに戻る（バッジ消失=0）。一覧が「31 件のノート」に復帰 | PASS |

## 備考

- 件数バッジは適用中フィルター数（タグ・公開状態など）を正しく加算反映。
- 「すべてのフィルタをクリア」ボタンが TanStack Router Devtools ボタン（fixed 左下）と重なり agent-browser のネイティブクリックがブロックされたため eval 経由 click で実行。これは dev ツールの重なりであり実装の問題ではない（本番ビルドでは devtools は出ない）。
