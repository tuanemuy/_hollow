# TC-004（エッジ）: 存在しないノートへのリンクは未解決のまま

**結果: PASS**

## 実行ログ

| 操作 | 期待 | 実際 | 判定 |
| --- | --- | --- | --- |
| ノート `Broken Linker` 本文に `[[存在しないノートXYZ]]` を入力・保存 | エラーにならず保存できる | `/notes/019e721b-f8a4-7438-ae0b-6c240fd7f72a` へ遷移、エラーなし | PASS |
| D1 確認 | 該当行が `resolved_note_id IS NULL` | `ref_kind='title'`, `ref_target='存在しないノートXYZ'`, `resolved_note_id=null` | PASS |

## 内部リンク挿入方法

直接タイプ。`[[存在しないノートXYZ` 入力後、サジェストは Escape で閉じ `]]` を手入力。

## スクリーンショット

- `screenshots/tc004-broken.png`

## D1 実値

```
ref_kind: title
ref_target: 存在しないノートXYZ
resolved_note_id: null  (broken link 維持、保存は成功)
```
