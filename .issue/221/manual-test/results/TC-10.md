# TC-10: spec / design 反映の目視確認
**結果**: PASS
**スクリーンショット**: なし（bash の grep 確認）

## 実行ログ

### A. `spec/pages/index.md` — フィードバックポリシー反映

```
$ grep -i 'フィードバック\|aria-live\|polling\|errorDisplay' spec/pages/index.md
```

ヒット箇所（抜粋）:
- `- 対応外形式 / サイズ超過 / LLM 失敗のフィードバック`
- `- フィードバックポリシー（#221）:`
  - `アップロード操作直後にモーダル内でローディング状態（skeleton + aria-live="polite"）を提示し、推論完了までフィードバックを途切れさせない`
  - `/upload 取り込みキュー画面は client polling（active job がある間は 4 秒間隔、無い間は 16 秒間隔）で自動更新し、フルリロードを要求しない`
  - `すべての完了・失敗・対応外通知は aria-live="polite" 領域でスクリーンリーダーに伝える`
  - `サーバーから返る BusinessRuleError.code（unsupported_format / daily_upload_quota_exceeded / regeneration_limit_exceeded 等）は app/core/presentation/errorDisplay.ts のマッピングテーブルを経由してユーザー向け文言に変換する`

実装と完全に一致。**PASS**

### B. `spec/design/index.md` — 10b. フィードバック・エラー表示原則

```
$ grep -in 'フィードバック\|10b\|エラー表示' spec/design/index.md
```

ヒット箇所:
- `143:## 10b. フィードバック・エラー表示原則（#221）`

セクション 10b が新設されている。**PASS**

### C. `spec/scenario/ingest.md` — FrontMatter JSON エラーの errorDisplay 経由表示

```
$ grep -in 'FRONT_MATTER_JSON_INVALID\|errorCode' spec/scenario/ingest.md
```

ヒット箇所:
- L69: `FrontMatter JSON が parse 不可 / オブジェクトでない: presentation 層の errorDisplay.ts 経由で「FrontMatter の JSON が不正です。形式を確認してください」のようなユーザー向け文言をモーダル内に表示する（内部の BusinessRuleError("FRONT_MATTER_JSON_INVALID") は UI には露出させない）`

`errorDisplay.ts:64-65` の case 文と一致。**PASS**
