# TC-004: ホワイトスペース有意要素・インライン語間スペース・内部リンクが保持される (AC-4, AC-5, AC-6)

**結果**: PASS

## 実行ログ
| # | 操作 | 期待 | 実際 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 整形表示された textarea で `<pre><code>` のインデント・改行 | 元のまま保持（AC-4） | `<pre><code>function greet(name) {\n    if (name) {\n        return ...` 4スペースインデント保持 | PASS |
| 2 | 隣接 `<a>` の語間スペース | 消えない（AC-5） | `</a> と <a` / `</a> の語間` のスペース保持 | PASS |
| 3 | 内部リンク `[[別のノート]]` | 保持（AC-6） | `[[別のノート]]` 保持 | PASS |
| 4 | 往復（保存→リロード→HTMLタブ再整形）後の再確認 | すべて保持 | preIndent=true / adjacentSpace=true / internalLink=true / 再整形 `<ul>\n  <li>` も成立 | PASS |

## 備考
- minify→reformat の往復後も 3 要素すべて非破壊。ユニットテスト (`htmlFormat.test.ts`) の `minifyHtml(formatHtml(m)) === m` と実画面挙動が一致。
