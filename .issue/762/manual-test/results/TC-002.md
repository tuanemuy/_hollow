# TC-002: 手動保存で整形済み HTML が minify されて永続化される (AC-2, AC-9)

**結果**: PASS

## 実行ログ
| # | 操作 | 期待 | 実際 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | HTML タブ（整形表示）で本文を一部編集（`内部リンクのプレースホルダ`→`内部リンクのプレースホルダ（手動保存編集）`） | textarea 内で編集反映 | len 446→454 | PASS |
| 2 | 「保存」ボタンで手動保存 | 保存成功・遷移 | 保存成功 | PASS |
| 3 | DB の content_html を確認 | 編集反映 かつ minified（改行・インデント無し） | `（手動保存編集）` 反映、`<ul><li>項目その1</li><li>項目その2` とブロック間インデント除去 | PASS |

## 備考
- textarea は整形表示（`\n  <li>` インデント有り）だが、保存実体は minify 済み。表示限定不変条件を確認。
- `<pre><code>` 内部の有意ホワイトスペース（`function greet(name) {\n    if`）は保存実体でも保持。手動保存も snapshotForSubmit 経路を通り自動保存と同じ minify ルールが適用されることを実挙動で確認。
