# ブラウザ検証レポート — Issue #762: HTML 編集モードで本文を整形表示し保存時に minify する

**実行日:** 2026-06-21
**テストソース:** `.issue/762/testing.md`
**サーバー:** http://localhost:3000（`pnpm dev`）
**検証ノート:** noteId=`019ee7b2-4cd2-740a-a7a0-dfc22b87d147`

## サマリー

| TC | 確認項目 | 対応AC | 結果 |
|----|---------|--------|------|
| TC-001 | HTML タブで minified 本文が整形表示される | AC-1 | PASS |
| TC-002 | 手動保存で整形済み HTML が minify されて永続化 | AC-2, AC-9 | PASS |
| TC-003 | 自動保存でも minify 済み HTML が永続化 | AC-3 | PASS |
| TC-004 | WS有意要素・語間スペース・内部リンク保持 | AC-4,5,6 | PASS |
| TC-005 | HTML ⇄ WYSIWYG ⇄ inline 往復で本文非破壊 | AC-7 | PASS |
| TC-EDGE | 壊れた HTML でも入力継続 + 既存機能影響 | AC-8 | PASS |

**合計:** 6 件（PASS: 6 / FAIL: 0）

## 根拠の要点

- **表示と保存実体の分離を DB 直読み（`db:execute:local`）で実証**: HTML タブ表示は整形済み（446 文字・`\n  <li>` インデント有り）、DB 保存実体は minified（439 文字・ブロック間インデント除去）。
- **手動・自動保存とも minify**: 編集内容が DB に反映され、いずれも minified。自動保存インジケータ「未保存の変更があります → 保存中… → 保存しました」を確認。
- **非破壊**: `<pre><code>` の 4 スペースインデント、隣接 `<a>` の語間スペース、`[[別のノート]]` が往復後も保持。
- **モード往復**: HTML⇄WYSIWYG / HTML⇄inline、および HTML 編集を挟んだ切替でも内容消失・余分改行混入なし。編集中の切替は確認ダイアログで保護（仕様どおり）。
- **エッジ**: `<div><p>未完成` 入力でも textarea が空にならずクラッシュなし。WYSIWYG 往復後 HTML に戻すと整形・自動補完され編集継続可。素の inline 編集→保存も従来どおり動作。

## 起票した Issue

なし（全 PASS）。
