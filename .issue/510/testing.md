# 動作確認計画 — Issue #510: design: 各画面の UX 最適化（デザインモック改善）

**Issue:** #510
**作成日:** 2026-06-06

---

## 確認環境

本 Issue の成果物は `spec/design/pages/*.html`（静的 HTML モック）と `spec/design/index.md` / `tokens.md`（設計言語）のみ。アプリ実コード（app/）は変更しないため、`pnpm dev` / `typecheck` / `lint` は本 Issue の確認対象外（HTML モックは TypeScript/Biome の対象ではない）。

各モックは index.md §9 の通り**単一ファイルでブラウザ表示可能**（CSS はインライン、外部依存なし）。

### 検証環境の起動

依存のないローカル静的サーバーで `spec/design/pages/` を配信し、ブラウザで各 HTML を開く:

```bash
# リポジトリルートで実行（空きポートを使う。例: 4173）
npx serve spec/design -l 4173
# → http://localhost:4173/pages/P10-home.html などを開く
```

ファイルを直接開く場合（macOS）:

```bash
open spec/design/pages/P10-home.html
```

### デプロイ方法

なし（静的モックの目視確認のみで完結。ステージング・本番へのデプロイは行わない）。

## 確認項目

### 1. 領域1 中核体験（P10/P11/P12/P20）

- **目的:** ノートを書く・読む・探す体験が UX 向上方針通りに描けているか。
- **手順:**
  1. P11-note-detail を開き、**右メタレールが廃止され単一カラム + 本文下メタ + バックリンクカード1箇所**になっているか確認。
  2. P12-editor を開き、モードタブ骨格 + borderless タイトル入力 + 書式ツールバーの折衷構成を確認。
  3. P10-home の一覧・フィルタ・選択インジケータ見本を確認。
  4. P20-views の空状態が認証側 EMPTY_STATE（破線枠 + 24px アイキャッチ）になっているか確認。
- **期待結果:** 各画面が確定方針通り。横スクロール非発生。
- **確認ポイント:** P11 が index.md §2.1 改訂（単一カラム）と整合しているか。

### 2. 領域2 取り込み・書き出し（P13/P15/P16）

- **目的:** 取り込みキューがカード型基調 + 進捗バー/形式バッジ/保存先/抽出タイトル取り込みになっているか。
- **手順:**
  1. P13-upload / P13a-upload-modal を開き、カード内の進捗可視化を確認。
  2. P13a の `:root` が tokens.md 最終形に揃い、font-size リテラルが fluid トークンへ置換されているか確認。
  3. P15-export / P16-export-jobs の体裁を確認（実装スタイリングは #509/#401 へ引き渡し）。
- **期待結果:** 進捗が見える。トークン整合。

### 3. 領域3 整理・公開管理（P17/P18/P14）

- **手順:**
  1. P14-publish-settings が2セクション基調 + ラジオ説明文 + QR、単一ノート正本になっているか確認。
  2. P17-trash / P18-tags の行構成（用途別基準）を確認。
- **期待結果:** 公開設定が迷わない。

### 4. 領域4 設定（P21〜P24）

- **手順:**
  1. P21〜P24 が左サイドバー差し替え方式の設定シェルに統一されているか確認。
  2. P24 がモックの多段確認（影響リスト + 同意 + DELETE語 + ユーザー名）になっているか確認。
- **期待結果:** 設定項目へ迷わず到達。破壊操作の確認が強い。

### 5. 領域5 公開・共有（P30〜P33）

- **手順:**
  1. P32 が中央寄せヒーロー + ⌘K ヒントになっているか確認。
  2. P31 にバックリンク + 関連ノートグリッドがあるか確認。
  3. P33 解錠後が P31 遷移方式に差し替わっているか、expired/gone の「トップへ戻る」CTA があるか確認。
- **期待結果:** 回遊導線が機能する。

### 6. 領域6 admin（P40〜P46）

- **手順:**
  1. admin シェルに検索・通知が維持されているか確認。
  2. P40 がダッシュボード=概況の情報設計になっているか確認。
- **期待結果:** 状況把握が素早い。

### 7. 領域7 入口・例外（P01〜P07/P34）

- **手順:**
  1. P34-error がバリアント別アクション出し分け（403→ログイン、500→再読み込み）になっているか確認。
  2. P03 callout の gap が統一されているか確認。
- **期待結果:** エラー時に次の行動が取れる。

## エッジケース・異常系

### 1. レスポンシブ

- **目的:** 横スクロール非発生・カラム切替が各幅で正しいか。
- **手順:** 各モックを 640 / 768 / 1024 / 1280 / 1536px で表示。
- **期待結果:** ページ全体の横スクロールが発生しない。lg でサイドバー常時表示、P11 で右レール sticky 廃止済み。

### 2. 状態網羅

- **目的:** 複数ステートのモックが破綻なく描画されるか。
- **手順:** P02（expired）/ P33（STATE1-3）/ P34（バリアント）/ P14（ラジオ）を表示。
- **期待結果:** 各状態の見本が破綻なく描画。

## 既存機能への影響確認

- index.md / tokens.md の SSOT 変更（§2.1 改訂等）が全モックに整合反映されているか。
- `[Npx]` 任意値が増えていないか（grep で確認、#461 fluid 思想維持）。

## 確認チェックリスト

- [ ] P11 右メタレール廃止 + §2.1 改訂整合
- [ ] P12 折衷エディタ構成
- [ ] P13 カード型 + 進捗取り込み
- [ ] P14 2セクション基調・単一ノート正本
- [ ] P21〜P24 左サイドバー差し替え統一
- [ ] P24 多段確認
- [ ] admin シェル検索・通知維持
- [ ] P33 P31遷移 + expired CTA
- [ ] 全モック横スクロール非発生（5ブレークポイント）
- [ ] トークン整合（`:root` 最終形 / `[Npx]` 非増加）
