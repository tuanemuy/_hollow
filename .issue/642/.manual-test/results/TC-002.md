# TC-002: ソート状態が URL に保持される

**結果**: PASS
**セッション**: verify-tc-002

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `/search?q=p32sort` でソートを新着順に切り替え、アドレスバー確認 | URL に `sort=newest` が付く | URL = `/search?q=p32sort&limit=20&sort=newest` | OK |
| 2 | その URL でリロード | 新着順が維持される | URL 維持、先頭3件 26,25,24（updated_at 降順）、ボタン表示「新着順」 | OK |
| 3 | URL をコピーして新しいタブで開く | 新着順が維持される | 新タブでも先頭3件 26,25,24、ボタン表示「新着順」 | OK |
| 4 | 関連度順に戻し、アドレスバー確認 | `sort` が URL から消える（`sort=relevance` を付けない） | URL = `/search?q=p32sort&limit=20`（`sort` なし）、先頭3件 01,02,03 | OK |
| 5 | `sort` なしの既存 URL（`/search?q=p32sort`）を直接開く（後方互換） | 従来どおり関連度順で表示 | 先頭3件 01,02,03、ボタン表示「関連度順（新着順に切り替え）」 | OK |

## 失敗詳細（FAILの場合）

なし
