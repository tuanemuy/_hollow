# TC-008: spec の更新

**結果**: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
| - | - | - | - | - |
| 1 | `spec/pages/index.md` P32 節を確認 | ソート選択肢の定義あり | 「ソート選択肢（関連度順 = bm25 / 新着順 = `updated_at` 降順）。URL パラメータ `sort` で保持し、省略時は関連度順。ソート切替時は `cursor` をリセット…期間ファセットは公開日基準・新着順は更新日時基準」と記載（L336）。LIKE 経路の新着順も記載（L337） | PASS |
| 2 | `spec/domains/search.md` を確認 | `SearchSort` とカウント系非影響の記載 | L67-69: `sort: SearchSort`（`'relevance' | 'newest'`、デフォルト `relevance`）、`updated_at` 降順 + `note_id` 昇順 tie-breaker、「カウント系（`countByDateRanges`）の結果には影響しない」と明記 | PASS |
| 3 | `spec/usecases/search.md` を確認 | 入力DTOに `sort` の記載 | L91: 入力に `sort?: SearchSort`（省略時 `relevance`）が記載 | PASS |
