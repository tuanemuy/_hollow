# TC-003: ソート切替時に1ページ目へ戻る（cursor リセット）

**結果**: PASS
**セッション**: verify-tc-003

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `/search?q=p32sort` を開く | 26件ヒット、関連度順で表示 | 「26 件」表示、関連度順（ノート01 が先頭）、「次のページ」リンクあり | PASS |
| 2 | 「次のページ」リンクで2ページ目へ | URL に `cursor` が付き、2ページ目（21〜26）表示 | URL: `?q=p32sort&cursor=b2Zmc2V0OjIw&limit=20`、ノート21〜26 を表示 | PASS |
| 3 | ソートトグル（関連度順 → 新着順）をクリック | URL から `cursor` が消え、新しい並びの1ページ目 | URL: `?q=p32sort&limit=20&sort=newest`（cursor 消滅、`q` 維持）。先頭がノート26→25→24（新着順1ページ目） | PASS |
| 4 | `?q=p32sort&tag=p32-tag-c&user=p32-sort-tester` でソートトグル | `q` / `tag` / `user` が維持され `sort=newest` 付与 | URL: `?q=p32sort&tag=p32-tag-c&user=p32-sort-tester&limit=20&sort=newest` | PASS |

## 補足

- agent-browser の `click` コマンドでの「次のページ」リンククリックが SPA 遷移を発火しないケースがあり、`eval` での `element.click()` で代替した（アプリ側の不具合ではなく自動化ツール側の挙動）。
