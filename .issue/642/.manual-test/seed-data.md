# Issue #642 シードデータ — P32 公開検索ソート検証

投入日: 2026-06-13
投入方法: `pnpm db:execute:local .issue/642/.manual-test/seed.sql`（`gen-seed.mjs` で生成、冪等: 固定IDを削除してから再投入）

## 前提整備

- `pnpm db:migrate` 実行済み（適用待ちマイグレーションなし）
- `search_documents` への INSERT はトリガー（`search_documents_ai`）で FTS5（trigram）に自動反映されることを確認済み
- 既存データは削除・変更していない（固定ID `01950642-*` のみ DELETE → INSERT）

## ユーザー

| username | id | 役割 |
| --- | --- | --- |
| `dev-admin` | `01950000-0000-7000-8000-000000000001` | 既存。ノート01〜16の所有者 |
| `p32-sort-tester` | `01950642-0000-7000-8000-0000000000f1` | 新規テスト用。ノート17〜26の所有者（ログイン不要、公開ノート所有のみ） |

## ノート（26件、すべて public / active）

- ID: `01950642-0000-7000-8000-0000000000{01..26}`
- slug: `p32-sort-note-{01..26}`
- タイトル: `P32ソート検証ノートNN p32sort`
- `updated_at`: ノートNN = `2024-04-NNT12:00:00Z`（01が最古、26が最新。全件異なる）
- 本文中の `p32sort` 出現回数 = `27 - NN`（ノート01に26回、ノート26に1回）
- `published_at`: 原則 `updated_at` と同日だが、**ノート24/25/26 は逆転**（24→04-26、25→04-25、26→04-24）。新着順が `published_at` でなく `updated_at` に従うことの検証用

### タグ（ファセット確認用）

| タグ | 所有者 | 付与ノート |
| --- | --- | --- |
| `p32-tag-a` | dev-admin | 奇数ノート 01〜15 |
| `p32-tag-b` | dev-admin | 偶数ノート 02〜16 |
| `p32-tag-c` | p32-sort-tester | 17〜26 |

## テストで使うキーワードと期待結果

### `p32sort`（FTS / bm25 経路、26件ヒット = 2ページ @limit 20）

- **関連度順**: ノート01 → 02 → … → 26（出現頻度が多い＝古いノートほど上位。bm25 スコアを実測し note01 > note02 > note03 を確認済み）
- **新着順**: ノート26 → 25 → … → 01（`updated_at` 降順）。関連度順の**完全な逆順**になるので切替の差分が一目で分かる
- ページネーション: 26件なので2ページ目あり（cursor リセット・連続性の確認に使用）
- 新着順1ページ目末尾はノート07、2ページ目先頭はノート06〜01（重複・欠落なしを確認）
- ノート26 は新着順で先頭だが `published_at` は 04-24（カード表示の `updatedAt` 準拠の確認ポイント）

### `蜻蛉`（CJK 2文字 → LIKE フォールバック経路、3件ヒット）

- ヒット: ノート05 / 15 / 25（`updated_at`: 04-05 / 04-15 / 04-25）
- 関連度順（LIKE時は `note_id` 昇順の安定順）: 05 → 15 → 25
- 新着順: 25 → 15 → 05

### `p32onlyone`（1件のみヒット）

- ヒット: ノート13 のみ。トグル操作で壊れないことの確認用

### `zzz999nohit`（0件）

- シードなし。空状態の確認用

### ファセット

- ユーザー: `dev-admin`（16件）/ `p32-sort-tester`（10件）
- タグ: `p32-tag-a`（8件）/ `p32-tag-b`（8件）/ `p32-tag-c`（10件）
- 期間: すべて 2024-04 内（期間ファセット件数がソート非依存であることの確認に使用）

## 検証クエリ（投入確認済み）

- `search_documents` の `p32sort` ヒット: 26件
- FTS bm25 上位3件: note01, note02, note03（古い順＝高頻度順）
- `蜻蛉` LIKE ヒット: 3件 / `p32onlyone`: 1件
