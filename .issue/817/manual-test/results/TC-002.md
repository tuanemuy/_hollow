# TC-002: /admin/jobs で hydration mismatch warning が出ない

**結果**: PASS
**セッション**: verify-817-jobs

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | dev-admin cookie を CDP 注入し `/admin/jobs` を open | admin として描画 | title「ジョブ監視 — hollow」/ URL は `/admin/jobs` のまま | PASS |
| 2 | `wait --load networkidle` で hydration 完了まで待機 | エラーなく安定 | 安定 | PASS |
| 3 | `console` で全 console ログを確認 | hydration mismatch warning が無い | `hydrat` / `didn't match` / `regenerated on the client` / `SectionErrorBoundary` 一切無し | PASS |
| 4 | SSR HTML を cookie 付き curl で取得し確認 | fallback 文言が無く、ジョブ日時が JST で描画 | HTTP 200 / fallback 文言 0 件 / 「取り込みジョブ」「エクスポートジョブ」描画・`YYYY/MM/DD` 表示 | PASS |

## 備考

`formatDateTime` はジョブ行の「更新」「作成」列に加え、検索インデックス再構築・内部リンクバックフィルの結果表示日時も担う。いずれも同一の整形関数なので TZ 固定で一括解消される。コンソールの hydration 警告なし。
