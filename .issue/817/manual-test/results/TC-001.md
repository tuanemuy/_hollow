# TC-001: /admin/users で hydration mismatch warning が出ない

**結果**: PASS
**セッション**: verify-817-users

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | dev-admin の `__Host-session` cookie を CDP 注入し `/admin/users` を open | admin として描画（ログインに飛ばされない） | title「ユーザー — hollow」/ URL は `/admin/users` のまま | PASS |
| 2 | `wait --load networkidle` で hydration 完了まで待機 | エラーなく安定 | 安定 | PASS |
| 3 | `console` で全 console ログを確認 | hydration mismatch warning が無い | `Hydration failed` / `didn't match` / `SectionErrorBoundary` エラー無し | PASS |
| 4 | SSR HTML を cookie 付き curl で取得し確認 | errorComponent fallback 文言（「…を読み込めませんでした」）が無く、登録日が JST 日付で描画 | HTTP 200 / fallback 文言 0 件 / 「登録日」列に `YYYY/MM/DD` 表示 | PASS |

## 備考

コンソールに出た唯一の warning は `[tanstack-router] These exports ... AppErrorFallback ... will increase your bundle size`（`_app/route.tsx` のコード分割に関する既存の別件・本 Issue と無関係）。hydration 関連の警告・エラーは検出されず。
