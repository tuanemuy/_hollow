# TC-001: /terms ページの表示

- **Status:** PASS (with notes)
- **URL:** http://localhost:3000/terms
- **Screenshot:** `.issue/205/.manual-test/screenshots/tc-001-terms.png`

## 確認結果

- HTTP 200 で表示された（ブラウザ snapshot 取得成功）。
- 警告 blockquote 「本文は雛形です。本番運用前に法務確認のうえ差し替えてください。」が表示されている。
- 利用規約 h1 ほか h2 見出し・段落が `.note-detail-content` 内に描画されている (`document.querySelector('.note-detail-content h1')` で確認)。
- `document.title`: `利用規約 — TanStack Start Template`
  - 「利用規約」は含まれるが、サイト名が `TanStack Start Template`（テンプレートデフォルト）のままで「Hollow」になっていない可能性あり。これは Issue #205 のスコープ外（AppConfig.siteName 設定の問題）。
- `<link rel="canonical">` href: `http://localhost:8787/` （`/terms` を指していない）
  - testing.md の期待 `/terms` を指す と相違あり。Issue #205 の確認ポイントの一つで、後続課題として記録。
