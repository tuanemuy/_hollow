# テスト実行サマリー — Issue #805

**実行日時**: 2026-07-10
**テストソース**: .issue/805/testing.md
**サーバー**: http://localhost:3000（pnpm dev / vite dev workerd）

| TC | テスト名 | 種別 | 結果 | 失敗ステップ |
|----|---------|------|------|-------------|
| TC-001 | アカウント削除フォームのメディア合計サイズ表示 | 正常系 | PASS | - |
| TC-002 | 管理ダッシュボード／メトリクスの上限値・ストレージ表示 | 正常系 | PASS | - |

**合計**: 2 件（PASS: 2 / FAIL: 0）

## 検証で確認した実表示（集約後の共有 formatBytes 出力）

- **account-delete フォーム**: 「2 件 / 3.5 MB」（media 集計 3,670,016 bytes → `formatBytes` = `3.5 MB`）。
  signature が `number` → `number | null` に変わった呼び出し箇所を実データ（非ゼロ）で確認。出力は集約前と一致。
- **admin/Metrics 上限値カード**（非 null 値で可変単位ロジックを網羅的に確認）:
  - 1 日あたりアップロード上限: `1.0 GB`
  - 取り込み最大サイズ: `32.0 MB`
  - ノート最大サイズ: `1.0 MB`
  - エクスポート成果物上限: `256 MB`（`scaled >= 100` により 0 桁 — 桁切替ルールが実表示でも正しく機能）
- **admin/Dashboard・admin/Metrics ストレージ**: R2/DO/合計とも `—`（U+2014）。
  ローカルでは R2/DO バインディング集計が null のため、`formatBytes(null)` の "—" 描画を確認（クラッシュなし）。

## 判定根拠

集約後もアプリが正常にビルド・import され、対象3画面がランタイムエラーなく描画され、
共有 `formatBytes` の全挙動（単位自動選択・`scaled>=100||i===0` の桁ルール・`null→"—"`）が
実表示で集約前と一致することを確認した。表示不変の絶対制約はユニットテスト（verbatim ロック）＋
コピー元との1文字一致 diff ＋本ブラウザ検証の三重で担保。
