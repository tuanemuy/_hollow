# TC-001: _app 配下からログアウト → /login 着地

**結果**: PASS
**実行時間**: 87秒
**セッション**: verify-tc-001

## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | cookie 注入（`__Host-session` = dev-admin-session-token, secure, sameSite Lax）を CDP 経由で設定 | cookie が設定される | `✓ Done` で正常設定 | PASS |
| 2a | `/notes` を開く | 認証済みページ | `/notes` は存在せず 404（「ページが見つかりません」）。手順の代替指示どおり `/` に切替 | -（想定どおり代替） |
| 2b | 認証済みホーム `/` を開く → networkidle → snapshot | サイドバー・ユーザーメニューが見える認証済み UI | サイドバー（ライブラリ／ディレクトリツリー／管理）と「Dev Admin のメニュー（dev-admin@example.com）」を確認。21件のノート一覧表示。ランディングではない | PASS |
| 3 | サイドバー下部のユーザーメニュー（Dev Admin のメニュー, ref=e10）を開く | メニューが開き「ログアウト」項目が出る | TanStack Router Devtools オーバーレイが click point を覆っていたため非表示化後にクリック。`menu` 内に「設定」「ログアウト」(ref=e40) を確認 | PASS |
| 4 | 「ログアウト」(ref=e40) をクリック | ログアウト処理が走る | `✓ Done` | PASS |
| 5 | `wait --url "**/login"` → `get url` → snapshot | `/login` に遷移 | URL = `http://localhost:3000/login`。ログインフォーム描画 | PASS |
| 6-着地点 | URL が `/login`、ログインフォーム表示 | メール・パスワード入力欄＋「ログイン」ボタン | 見出し「ログイン」、メールアドレス入力欄(e5)、パスワード入力欄(e7)、「ログイン」ボタン(e9) を確認 | PASS |
| 6-回帰 | 認証済み UI が残っていない | サイドバー・ユーザーメニューが消えている | サイドバー無し・Dev Admin メニュー無し。banner は「アカウント作成」リンクのみ（未認証ヘッダー） | PASS |
| 7 | ログアウト後の `/login` で `back` → 認証ガード確認 | 認証済みホームに戻らずガードが効く | `back` 後も URL は `/login` のまま、ログイン画面継続（サイドバー・Dev Admin 無し）。ガード有効 | PASS |
| 8 | セッションを閉じる | クローズ | `✓ Browser closed` | PASS |

## チラつき観察
遷移過程でのランディング（未認証トップのヒーロー等）の瞬間描画は捕捉できなかった。agent-browser の snapshot は settled state を撮るため、sub-frame の 1 フレーム瞬間表示は DOM snapshot では捕捉不可。着地点は `/login` で正常に確定しており、ログアウト処理・認証ガードともに正しく動作している。Issue #728 の修正対象である「ランディング一瞬表示の race」については、本ツールの観測粒度では再現有無を確証できないが、最終状態に異常（ランディングへの誤着地・認証 UI 残存）は一切見られなかった。

## 失敗詳細（FAILの場合）
なし（全ステップ PASS）。

## 備考
- 手順 2 の `/notes` は当該ルートが存在せず 404 となったため、手順の代替指示に従い認証済みホーム `/` で認証状態を確認した。`/` 側で認証済み UI（サイドバー＋ユーザーメニュー）が正しく表示されたため、TC の前提（`_app` 配下の認証済みページからのログアウト）は満たしている。
- 手順 3 ではユーザーメニューのクリックポイントを TanStack Router Devtools のオーバーレイが覆っていたため、検証目的に影響しない範囲で devtools パネルを一時的に非表示化してからメニューを開いた。
