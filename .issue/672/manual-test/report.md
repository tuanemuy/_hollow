# ブラウザ検証レポート — Issue #672

**実行日:** 2026-06-13
**テストソース:** `.issue/672/testing.md`
**サーバー:** http://localhost:3000（`pnpm dev`）
**認証:** dev-admin（`pnpm seed:dev-admin`）。`__Host-session` Cookie を CDP 経由で注入。
**テストデータ:** ルート直下ノート（depth0 ルートディレクトリ直下）と階層ありノート（`Research/論文メモ/` 配下）を D1 に投入。

## 結果

| TC | テスト名 | 種別 | 結果 |
|----|---------|------|------|
| TC-001 | ルート直下ノートのパンくず（タイトルのみ） | 正常系 | PASS |
| TC-002 | 階層ありノートのパンくず（Research › 論文メモ › タイトル） | 正常系 | PASS |
| TC-003 | モバイル幅（375px）での折り返し | エッジ | PASS |

**合計: 3件（PASS: 3 / FAIL: 0）**

## 受け入れ基準の充足

- AC-1: パンくず内に「すべてのノート」リンクが**存在しない**（TC-001/002 で確認）。
- AC-2: 階層ありノートで `Research › 論文メモ › タイトル`、先頭区切りなし・区切りは要素間のみ（要素3→区切り2）（TC-002）。
- AC-3: ルート直下ノートはタイトルのみ・区切り0（TC-001）。
- AC-4: 各セグメントが `directoryId` 絞り込みリンクとして機能（TC-002、Research クリックで `/?directoryId=...` 遷移を確認）。

## 既存機能への影響

- サイドバー・プロパティ「場所」の「すべてのノート」表記はパンくずとは別物で、変更されず残存（想定どおり）。
- モバイル幅で横オーバーフローなし。`flex-wrap` + `[overflow-wrap:anywhere]` で折り返し対応。

## 起票したIssue

なし（全PASS）。
