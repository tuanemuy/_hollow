# TC-001: 兄弟名重複エラーの具体メッセージ（最重要）

- 種別: 主要シナリオ
- 結果: **PASS**

## 操作ログ

| # | 操作 | 結果 |
|---|------|------|
| 1 | ログイン後、`existing-dir-1779991904395` の「︙」メニューを開き「子ディレクトリを作成」をクリック | ダイアログ表示（作成先: existing-dir-1779991904395） |
| 2 | 名前に `dup-dir-290` を入力し「作成」 | 成功。ダイアログが閉じ、ツリーに子 `dup-dir-290` が追加された |
| 3 | 同じ親で再度「子ディレクトリを作成」を開き `dup-dir-290` を入力 | ダイアログ表示 |
| 4 | 「作成」をクリック | ダイアログは閉じず、`role="alert"` にエラー文言が表示された |

## 実際に表示された文言（alert）

```
同名のディレクトリが既に存在します
```

## 判定

期待文言「同名のディレクトリが既に存在します」と完全一致。genericなメッセージ（「操作を完了できませんでした」等）ではない。→ **PASS**

## スクリーンショット

- /Users/hikaru/github.com/tuanemuy/hollow2/.issue/290/manual-test/screenshots/tc-001-conflict.png
