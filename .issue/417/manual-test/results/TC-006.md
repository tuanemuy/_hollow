# TC-006（エッジ）: reduced-motion での press scale

**結果**: SKIP（ソース担保・ブラウザ自動検証は対象外）

## 実行ログ
| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|---|---|---|---|
| 1 | reduced-motion での press scale 抑止 | CSS に `motion-reduce:active:scale-100` が含まれる | `app/components/common/styles.ts` の `pillBtn` に `active:not-disabled:not-aria-disabled:scale-[0.985] motion-reduce:active:scale-100` を確認（press scale は active で 0.985、reduced-motion 時は scale-100 で無効化） | ソース担保 PASS |
| 2 | ブラウザでの reduced-motion 挙動 | OS 設定依存 | agent-browser から OS 設定の reduced-motion 制御は困難（`set media reduced-motion` はあるが scale の動的検証は不安定）。指示どおり SKIP | SKIP |

## スクリーンショット
なし（ソース担保のため）

## 失敗詳細
なし
