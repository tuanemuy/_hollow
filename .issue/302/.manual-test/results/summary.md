# Issue #302 マニュアルテスト結果

サイドバー「ディレクトリ」セクションの空状態 — 見出し+に統一する

- 環境: http://localhost:3000 （HTTP 200確認済み）
- アカウント: test302@example.com（verified / ルートディレクトリのみ＝空状態）
- セッション: agent-browser `verify-302`
- 実施日: 2026-05-30

## 結果サマリ

| TC | 内容 | 判定 | 確認できた実際の内容 | スクリーンショット |
|----|------|------|----------------------|--------------------|
| TC-001 | 空状態に CTA ボタンが表示されない | PASS | 「ディレクトリ」セクションに `button "ディレクトリを新規作成"` が1つだけ存在。直下に `paragraph "まだディレクトリがありません。右上の + から作成できます。"` を表示。`+ ディレクトリを作成` という独立ボタンは存在せず、`+` の縦2つ並びもなし。 | screenshots/tc-001-empty-state.png |
| TC-002 | 見出し横の + から作成ダイアログが開き作成できる | PASS | `+`（ディレクトリを新規作成）クリックで `dialog "ディレクトリを新規作成"` が開く。`作成先: （ルート）` 表示。名前欄に `仕事` を入力 → 「作成」ボタンが有効化 → クリック後、サイドバーツリーに `仕事`（treeitem level=1）が表示された。 | screenshots/tc-002-dialog.png, screenshots/tc-002-created.png |
| TC-003 | 階層あり状態の表示 | PASS | 作成後は空状態テキスト・ボタンは非表示。`tree "ディレクトリツリー"` 内に `仕事` ノードと操作メニュー `button "仕事 の操作"`（aria-label「仕事 の操作」）が表示。 | screenshots/tc-003-hierarchy.png |
| TC-004 | 見出し横 + のアクセシビリティ属性 | PASS | 見出し横の `+` ボタンの aria-label が「ディレクトリを新規作成」であることを snapshot で確認。 | （TC-001/003 と同一スナップショット） |

## 総合判定

**全PASS**

- 空状態は見出し横 `+`（aria-label「ディレクトリを新規作成」）とテキスト案内のみに一本化されており、冗長な `+ ディレクトリを作成` CTA ボタンは削除されている。
- ルート直下作成は見出し横 `+` から正常に機能（作成先「（ルート）」、`仕事` 作成成功）。
- 既存のアクセシビリティ属性・操作メニューは維持されている。

## テストデータ

- 作成した `仕事` ディレクトリ（test302@example.com / ルート直下）はテストデータとして残置。削除不要。
