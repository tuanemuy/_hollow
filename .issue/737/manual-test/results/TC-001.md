# TC-001: same-origin 取り込み POST が成功する（403 にならない）

**結果**: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/` を開き `__Host-session` cookie を CDP 経由で注入 | セッション確立 | Dev Admin (dev-admin@example.com) としてログイン状態を確認 | PASS |
| 2 | `/upload` を開き networkidle 後 snapshot | ジョブ一覧表示 | キューに既存ジョブ4件（test-note-2 / test-note-4 / test-note-2 / test-note-3、いずれも「保存済み」）。サイドバーの「アップロード」に未処理バッジなし | PASS |
| 3 | `input[type=file]` に `/tmp/csrf-test-note.md` をアップロード | POST 送信 | コマンド成功（✓ Done） | PASS |
| 4 | networkidle + 3秒待機後 snapshot | 403 alert が出ない / ジョブ追加 | 「権限がありません」alert は**出ず**。status 領域は空。サイドバーが「アップロード（未処理 1 件）」に変化（バッジ 1）。POST 受理を確認 | PASS |
| 5 | 追加待機後 snapshot で新規ジョブ確認 | csrf-test-note ジョブが出現 | `csrf-test-note.md`（text/markdown · 0.1 KB）がキューに出現。タグ `#security #testing #csrf` 付与、ステータス「プレビュー可能」 | PASS |

## 判定根拠

- same-origin（Origin = `http://localhost:3000` = `config.appUrl`）状態で取り込み POST（`uploadFileFn`）が `csrfMiddleware` を通過して受理された。
- 403「権限がありません」alert は一切表示されなかった（最重要確認項目）。
- アップロード前ジョブ件数 4件 → アップロード後、サイドバー未処理バッジ「1」が出現し、キューに `csrf-test-note` ジョブが追加された（件数増加を確認）。
- 追加されたジョブは正常に処理され、ステータス「プレビュー可能」・タグ自動付与まで到達。POST 受理＝CSRF 通過の証拠として十分。

## 失敗詳細（FAILの場合）

なし。
