# TC-009: ToolbarSkeleton の新レイアウト（確認項目 9 / AC-10）

**結果: PASS**（代替手段による確認。DevTools のネットワークスロットリングは agent-browser から利用不可のため、SSR ストリーミング HTML（RSC ペイロード）に含まれる Suspense fallback マークアップの検証で代替）

## 実行ログ

| # | 手順 | 結果 |
| --- | --- | --- |
| 1 | Cookie 注入済みで `curl http://localhost:5183/` を取得し RSC ペイロードを解析 | OK |
| 2 | ToolbarSkeleton fallback の存在確認: 見出しバー `h-[34px] w-[35%]`（mb-3） | OK（1 箇所） |
| 3 | meta-row 構造: `flex justify-between ... flex-wrap mb-5` 内に 左 = 件数ライン `h-3 w-[110px]` / 右 = pill `h-9 w-9` ×2 + segmented バー `h-8 w-[104px] rounded-[9px]` | OK |
| 4 | 旧 CTA / select プレースホルダ: skeleton 内に CTA・select 相当のプレースホルダなし。SSR HTML に `<select` は 0 件 | OK |
| 5 | 件数プレースホルダの二重表示: NoteListSkeleton（`app/components/note/list/skeletons.tsx`）に件数バーなし。件数ラインは ToolbarSkeleton 側のみ | OK |
| 6 | `role="status"` の重複: RSC ペイロード中の status リージョンは「ディレクトリ / 保存したビュー / フィルタ / ノートを読み込み中」のみ。ToolbarSkeleton は `aria-hidden="true"` で status を持たない（設計コメントどおり） | OK |
| 7 | 解決後の実 UI 置換: ページロード後の DOM に `animate-pulse` 要素なし、見出し「すべてのノート」表示、select 0 件 | OK |

## 備考

- スロットリングでの目視確認は環境制約により実施できず、SSR fallback マークアップ + 解決後 DOM の検証で代替した（指示の代替手順に準拠、SKIP 扱いとせず PASS と判定）。
