# TC-006: 表示モード segmented の確定表現（AC-5）

結果: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | ツールバー右端の segmented を確認 | meta-row 右端に配置 | `tablist` の右端 x=1256 が行右端 x=1256 と一致（右端配置） | PASS |
| 2 | マークアップ確認 | `role="tablist"` / `role="tab"` / `aria-selected` / `aria-label` / `title` | tablist `aria-label="表示形式"`、各 tab に `aria-label` / `title`（リスト/タイル/カレンダー）、`aria-selected` 正しい | PASS |
| 3 | 可視ラベル確認 | アイコンのみ | 各 tab の `textContent` 空、SVG アイコンのみ | PASS |
| 4 | デスクトップ寸法 | 32×28px、アイコン 13px | 各 tab 32×28px、アイコン 13×13px | PASS |
| 5 | active の見た目 | ink 濃度差、白カード/shadow なし | active: `color: rgb(29,29,31)`（ink）、非 active: `rgb(134,134,139)`（薄）。`background: transparent`、`box-shadow: none` | PASS |
| 6 | タイル → カレンダー → リストへ切り替え | URL の `display` が replace で更新 | `?display=tile` → `?display=calendar` → `?display=list`。`history.length` 不変（4 のまま）= replace | PASS |
| 7 | iPhone SE で寸法確認 | 36×32px 相当 | 33.2×32px（root font-size が fluid 14.75px のため `w-9`=2.25rem が 33.2px。h は 32px） | PASS |
| 8 | 44px 相当の当たり判定 | 擬似要素で拡張 | `::after` 36.9×43.1px（`-inset-y-1.5 -inset-x-0.5`）。設計コメントどおり「縦優先・横は隣接と干渉しない範囲」の 44px 相当 | PASS |
| 9 | 境界付近のタップ誤爆確認 | 意図したモードに切り替わる | `elementFromPoint` スイープ: 隣接 tab の `::after` 越境は端 2px 以内のみ。中央〜3px 内側は自 tab がヒット。タップで意図どおり切り替え | PASS |

## 備考

- 手順 7/8 の実測値は設計値（36×32 / 44px）より僅かに小さいが、これはルートフォントサイズが fluid（375px 幅で 14.75px）であることによる rem 換算の結果。styles.ts のコメント「44px 相当へ拡張（縦を優先し横は隣接ボタンと干渉しない範囲）」の設計意図どおりであり、FAIL とは判定しない。
