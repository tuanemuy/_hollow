# TC-007: 件数と操作群の 1 行統合 — meta-row（AC-6）

結果: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | デスクトップ（1280px）で見出し直下の行を確認 | 左 = 件数、右 = 操作群の 1 行 | 「11 件のノート」と 選択 / ビュー保存 / segmented が同一 flex 親（`display: flex; flex-wrap: wrap`）内、縦中心 y=176 で完全一致 | PASS |
| 2 | 件数表示の重複確認 | ノート一覧側に重複なし | 「件のノート」を含む末端要素は 1 つのみ | PASS |
| 3 | 幅を 768 / 600 / 480 / 414 / 375px へ段階的に縮小 | 折り返し可・潰れ/はみ出しなし | 全幅で `scrollWidth <= innerWidth`（横はみ出しなし）。375px まで件数と操作群が同一行を維持し要素は潰れない | PASS |

## 備考

- meta-row は `flex-wrap: wrap` を持つため、さらに狭い環境でも折り返しで対応できる構造。今回のコンテンツ量では 375px まで折り返し不要で 1 行に収まった。
