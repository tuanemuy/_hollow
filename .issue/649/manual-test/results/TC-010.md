# TC-010: P30 系モックの segmented 追従（確認項目 10 / AC-12）

**結果: PASS**

## 実行ログ

| # | 手順 | 結果 |
| --- | --- | --- |
| 1 | `spec/design/pages/P30-user-public-top.html` をブラウザで直接表示し computed style を検査 | OK |
| 2 | デスクトップ P30: ボタン 32×28px、アイコン 13px、可視ラベルなし、`role="tablist"`(aria-label="表示形式") / `role="tab"` / `aria-selected` / `aria-label` / `title` あり | OK |
| 3 | active 表現: 白カードなし（background transparent）・box-shadow none・ink 濃度差（active `rgb(29,29,31)` = --color-ink / 非 active `rgb(134,134,139)` = ink-tertiary） | OK |
| 4 | `spec/design/pages/mobile/P30-user-public-top.html`: ボタン 36px × min-height 32px、アイコン 13px、shadow なし・濃度差 active。`title` なし（mobile 慣例どおり） | OK |
| 5 | P10 確定モック（P10-home.html / mobile/P10-home.html）と CSS 比較: `.segmented` / `.segmented button` / `.active` の定義が同一（mobile の transition のみ P10 = `var(--duration-base) var(--ease-standard)` ≒ P30 = `0.18s ease`。180ms で実質同値） | OK |
| 6 | 位置: P30 既存のままツールバー左・`sort-btn` の隣（nextElementSibling = sort-btn） | OK |
| 7 | スクリーンショット /tmp/p30-desktop.png, /tmp/p30-mobile.png で目視確認 | OK |

## 備考

- mobile P30 の transition がリテラル `0.18s ease` で、P10 mobile はトークン変数参照。視覚上は同一（duration 180ms）。静的モック内の表記揺れであり指摘事項程度。
