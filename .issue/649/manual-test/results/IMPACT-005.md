# IMPACT-005 FilterBar 既存チップ挙動（チップ個別解除・内部リンク参照・期間・公開状態）

結果: PASS

## 実行ログ

| # | 操作 | 期待 | 実際 |
| --- | --- | --- | --- |
| 1 | タグチップ `#design` クリック | フィルタ適用・URL 反映・active 状態 | `?tagNames=["design"]`、「1 件のノート」、`aria-pressed=true` / `data-active=true` |
| 2 | 同チップを再クリック（個別解除） | フィルタ解除 | URL クエリ空・全 11 件表示 |
| 3 | 「内部リンク参照」チップクリック | ノート選択ダイアログ表示 | dialog「ノートを選択」（先頭一致コンボボックス）表示、トリガー `expanded=true` |
| 4 | 「テスト用ノートA」を検索・選択 | 参照フィルタ適用 | `?referencingNoteId=019e9845-...`、「参照中:」表示と解除ボタン出現 |
| 5 | 「内部リンク参照フィルタを解除」クリック | 解除 | URL クエリ空に復帰 |
| 6 | 「期間」チップクリック → Escape | dialog「期間フィルタ」（開始日/終了日）表示 → 閉鎖 | 表示・Escape で `expanded=false` |
| 7 | 「公開状態」チップクリック → Escape | menu（すべて/非公開/限定公開/公開 の menuitemradio）表示 → 閉鎖 | 表示（「すべて」checked）・Escape で閉鎖 |

備考: フィルタはすべて解除して終了。データ変更なし。
