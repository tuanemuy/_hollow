# TC-005: 選択 / ビュー保存のアイコンのみ化（AC-4）

結果: PASS

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | ツールバー右群を確認 | 「選択」「ビューとして保存」がアイコンのみ | 両ボタンとも可視テキストなし（`textContent` 空） | PASS |
| 2 | `aria-label` / `title` を確認 | 両属性あり | 選択: `aria-label="選択モード"` / `title="選択モード"`、保存: `aria-label="ビューとして保存"` / disabled 時 `title="条件が設定されていません"` | PASS |
| 3 | デスクトップ実寸を確認 | 36px 角 | 両ボタン 36×36px（getBoundingClientRect） | PASS |
| 4 | 「選択」を押して ON/OFF | `aria-pressed` が切り替わる | false → true → false に変化、選択 UI が表示/解除 | PASS |
| 5 | フィルタ未適用時の保存ボタン | disabled + 従来の title 文言 | `disabled=true`、`title="条件が設定されていません"` | PASS |
| 6 | フィルタ適用（#design）後の保存ボタン | enabled、`title="ビューとして保存"` | `disabled=false`、`title="ビューとして保存"` | PASS |
| 7 | 「ビューとして保存」を押す | SaveViewDialog が開く | dialog「現在のフィルタをビューとして保存」が開く（名前・公開範囲・保存/キャンセル） | PASS |
| 8 | 名前「TC005検証ビュー」で保存 | 保存できる | 保存成功（リロード後サイドバー「保存したビュー」に表示） | PASS |
| 9 | iPhone SE（375×667）で当たり判定 | 44px タッチ床 | 両ボタン 44×44px、`min-width/min-height: 44px` | PASS |
| 10 | モバイルでタップ動作 | 選択モードが切り替わる | `aria-pressed` true → false で正常動作 | PASS |

## 備考

- 検証で作成した「TC005検証ビュー」は検証後に DB から削除し、シード状態（保存ビュー 3 件）へ復元済み。
- 手順 8 で保存直後の SPA 内サイドバーには即時反映されなかったが、ページ再読み込みで反映を確認（保存自体は成功）。受け入れ基準（従来どおり保存できること）は満たす。
