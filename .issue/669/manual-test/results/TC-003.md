# TC-003 ディレクトリ行のコンパクト化（AC-4）

結果: **PASS**

対象: `/notes/{id}/edit`

| 操作 | 期待 | 実際 | 判定 |
| --- | --- | --- | --- |
| 見た目（fieldset 枠線なし） | コンパクトな1行 + pill、fieldset なし | エディター form 内に `fieldset` 要素なし。行は `mb-3 flex flex-wrap items-start gap-x-3 gap-y-2` のコンパクトな1行 | PASS |
| margin-bottom | 12px | `12px` | PASS |
| ディレクトリ選択 | combobox から選択でき、選択後に解除/リネーム/削除ボタンが出る | listbox（`/`, `検証ディレクトリ`）から選択 → 「選択を解除」「リネーム」「削除」が表示。解除クリックでボタン群が消える | PASS |
| 新規ディレクトリ名入力 | 入力できる | 「または新規ディレクトリ名」に「新しいテスト用ディレクトリ」を入力 → 値反映を確認（その後クリアし実データ未変更） | PASS |
| リネームダイアログ | 開いて閉じられる | 「ディレクトリをリネーム」ダイアログ表示（新しい名前 prefill）→ キャンセルで閉じる | PASS |
| 削除ダイアログ | 開いて閉じられる | 「「検証ディレクトリ」を削除しますか？」確認ダイアログ表示 → キャンセルで閉じる | PASS |

特記: 指示どおりリネーム/削除はダイアログの開閉のみで実行せず、実データは変更していない。
