# Plan Review — Issue #669 / Round 2（要件カバレッジ・スコープ整合性）

レビュー対象: `.issue/669/plan.md` / `.issue/669/adr.md`
視点: Issue本文・コメント合意 → 受け入れ基準の網羅性、各基準の検証可能性、基準⇔ステップの紐づけ、スコープ外混入。Round 1（round-1-coverage.md / round-1-arch-risk.md）指摘の反映確認を含む。

## Round 1 指摘の反映確認

- **P-001（seed-once 防御の効果範囲の内部矛盾）— 反映済み・整合確認済み。** AC-9 が「routerInvalidate 経由での state・フォーカス保持 ＋ seed-once 契約の退行防止 pin（生 invalidate の RSC 再マウントへの防御では**ない**）」に修正され、スコープ節（plan.md:37）・設計2・ステップ2・リスク節・adr.md ADR-003（既知の残課題の段落 + Consequences）まで主張が一貫している。矛盾していた旧記述（「3ルール例外に対しても AC-9 で守られる」）は残っていない。rule 2 残存経路は「既知の残課題（スコープ外）」に明示的に倒され、判定基準（手動検証 (f) は記録のみ・不合格としない）も明文化された — Round 1 で求めた「どちらかに倒す」が満たされている。
- **coverage S-001（toolbar `margin-bottom: var(--space-4)`）— 反映済み。** AC-1 に追記され、ステップ3で `mb-4` 直接指定（wrapper 分離時は wrapper 側）として余白戦略と整合。モック実値（P12-editor.html:612 `margin-bottom: var(--space-4)`）と照合済み。
- **coverage S-002（rule 2 経路の手動確認）— 反映済み。** ステップ8 (f) として追加され、AC-10 のスコープ境界（不合格判定にしない）と矛盾しない書き方になっている。
- arch S-001〜S-003 もレビュー履歴どおり設計3・ステップ3/4/5・手動検証 (a) に反映されていることを確認した。

## 検証メモ（コード・モック照合）

- モック余白の実値照合: title `--space-5`（:480）、dir-row `--space-3`（:493）、tags-row `--space-5`（:568）、toolbar `--space-4`（:612）、editor `min-height: 480px`（:708）— ステップ4/5/6/7 の `mb-5 / mb-3 / mb-5 / mb-4 / min-h-[480px]` と全一致。
- モック要素順序（title :468 → dir-row :489 → tags-row :562 → toolbar :600 → editor）— ステップ4の並べ替え方針と一致。
- `routerInvalidate.ts` は現状 `_app` 除外のみ集約 — ステップ1の拡張前提は正しい。
- Issue 受け入れ条件4項目 → AC-1〜6（モック一致）/ AC-5（タグ判断記録）/ AC-10（フォーカス）/ AC-11（品質ゲート）、コメント合意 ①②③ → AC-7 / AC-9 / AC-8。漏れなし。

#### 問題点（要修正）

問題点ゼロ。

#### 改善提案 [S-xxx]

なし（無理な粗探しはしない。Round 1 指摘はすべて正しく反映され、新規の要件漏れ・スコープ混入・検証不能な基準は見つからなかった）。

#### 良い点

- AC-9 の文言が「何を保証し、何を保証しないか」を基準自体に埋め込んでおり、検証時の判定ブレが構造的に排除されている
- rule 2 残存経路の扱いが「スコープ除外の宣言」「手動検証 (f) での現状記録」「後続 Issue 化の判断材料」の3点セットで閉じており、スコープ外に倒しつつ追跡可能性を確保している
- モックの余白・順序・寸法が計画内の具体値（`mb-*` / `min-h-*`）まで落ちており、AC の検証がモックとの突き合わせだけで完結する
- レビュー履歴節に「見送った提案と理由」（arch S-004）まで記録されており、反映状況が監査可能

## 結論

問題点ゼロ。Round 1 の指摘（P-001、S-001/S-002、arch S-001〜S-003）はすべて正しく反映されており、新規問題なし。
