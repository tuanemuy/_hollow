# TC-004 mobile ヘッダー sweep＋cta-bar 廃止

**結果: PASS（注記あり）**

## 実行ログ

| Step | 操作 | 確認内容 | 結果 |
|---|---|---|---|
| 01 | /mobile/P11-note-detail を 390x844 で開く | `cta-bar` クラス 0 件。header-right = アップロード（`pill-btn primary icon`）＋新規作成（`pill-btn icon`）、アバター無し | OK |
| 02 | /mobile/P15-export | 下部固定バー無し（fixed bottom は sidebar のみ）。フォーム末尾に「エクスポートを実行」（`pill-btn primary`）。header-right 確定形・アバター無し | OK |
| 03 | /mobile/P20-views | 下部固定バー無し。page-header 内に「新しいビュー」CTA。header-right 確定形・アバター無し | OK |

受け入れ grep `grep -rln 'cta-bar' spec/design/pages/mobile/` → 0 件。

## 注記

mobile/P11-note-detail には `.bottom-cta`（下部固定の「編集」＋「公開設定」バー、#536 由来のページ局所 CTA）が存在する。#628 ADR-001 が廃止したグローバルヘッダー由来の `cta-bar`（BottomCtaBar）とは別物で、受け入れ grep の対象外だが、「下部固定 CTA バーが存在しない」を字義どおり読むと P11 のみ該当する。設計判断の確認を推奨。

## スクリーンショット

- screenshots/tc-004/step-01.png（P11）
- screenshots/tc-004/step-02.png（P15）
- screenshots/tc-004/step-03.png（P20）
