# TC-003: 保存 round-trip: contenteditable の非漏出とメディア参照の維持

**結果**: PASS
**実行時間**: 45
**セッション**: verify-tc-001

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | TC-002 の編集後、保存完了を待つ | 保存完了 | 「保存しました」表示を確認 | PASS |
| 2 | ノート詳細（読み取り専用ビュー）を開く | 画像とテキスト双方が表示、画像は `/media/<id>` からロード | 本文に "before" / "after" テキストと画像を確認。`imgLoaded: true`（complete && naturalWidth > 0）。`<p>before<img src="/media/019f4ff8-...のid" alt="">after</p>`。読み取りビューに `contenteditable` 要素 0 件 | PASS |
| 3 | ページを再読み込みして再確認 | 画像・テキストが残っている | reload 後も同一構造 `<p>before<img ...>after</p>`、画像ロード成功 | PASS |
| 4 | 再度「編集」→「HTML」タブで生 HTML を確認 | `contenteditable` が一切含まれない / `/media/<id>` 参照が保たれる | 生 HTML: `<p>First paragraph line for testing.</p>\n<p>Second paragraph line.</p>\n<p>before<img src="/media/019f4ff8-eb14-7558-91ba-1723e3df9070" alt="">after</p>` — `contenteditable` 含有なし、メディア参照維持 | PASS |

## 備考

- `<p><img ...>入力テキスト</p>`（正確には `<p>before<img>after</p>`）の構造が保存 round-trip 全体で保たれていることを確認。
- HTML → ビジュアルへ戻した際も decorate が再適用された（モード切替往復の副次確認。TC-005 の前提操作として実施）。
