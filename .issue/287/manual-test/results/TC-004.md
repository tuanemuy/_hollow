# TC-004: 既存 decorate 規則の回帰なし（inline モードの他ブロック）

**結果**: PASS
**実行時間**: 約150秒
**セッション**: verify-tc-004

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | HTML モードで前提データの本文を持つ新規ノートを作成（`/notes/new` → HTML タブ → 貼り付け → 作成） | 保存成功 | 保存成功。sanitizer による構造正規化は**一切なし**（保存後 HTML が入力と完全一致: `<p>先頭の段落</p><ul><li><p>リスト内段落</p></li></ul><blockquote>直下テキスト<p>入れ子段落</p></blockquote><pre><code>code text</code></pre><p></p><table><tbody><tr><td><br></td></tr></tbody></table>`） | PASS |
| 2 | `/notes/{id}/edit` を開く | 「ビジュアル」タブがデフォルト選択 | 「ビジュアル」が selected | PASS |
| 3 | `querySelectorAll('[contenteditable]')` で decorate 対象を列挙 | 期待要素のみに付与 | `P(先頭の段落)` / `P(リスト内段落)` / `BLOCKQUOTE` / `P(入れ子段落)` / `PRE` / `P(空)` / `TD` の 7 要素すべて `contenteditable="true"` | PASS |
| 4 | `<li>` / `<ul>` / `<table>` / `<tbody>` / `<tr>` / `<pre>` 内 `<code>` の contenteditable を確認 | 付与されない | 全て `contenteditable` 属性なし（null） | PASS |
| 5 | `<li><p>` の内側 `<p>` に末尾からテキスト入力（`LI追記`） | 巻き戻らず残る | `<ul><li><p contenteditable="true">リスト内段落LI追記</p></li></ul>` — 残存、構造保持 | PASS |
| 6 | `<blockquote>` 内側 `<p>` にテキスト入力（`BQ追記`） | 巻き戻らず残る | `<blockquote contenteditable="true">直下テキスト<p contenteditable="true">入れ子段落BQ追記</p></blockquote>` — 残存。blockquote 本体と内側 p の両方が editable | PASS |
| 7 | `<pre><code>` にキャレット → `Enter` → `line2` 入力 | 従来どおり編集可、Enter でリテラル改行 | `<pre contenteditable="true"><code>code textline2\n</code></pre>` — リテラル `\n` が `<code>` 内に挿入され、タグ分割（`<p>` 化・`<br>` 化）なし。※Chromium の caret 挙動により末尾 Enter 後の入力が改行の前に入った（`\n` が末尾）が、リテラル改行そのものは確認できた | PASS |
| 8 | 空段落 `<p></p>` の decorate 確認 | `contenteditable="true"` が付く（一般化の副産物・意図どおり） | 付与されている（`<p contenteditable="true"></p>`） | PASS |
| 9 | 通常段落（先頭の段落）に末尾からテキスト入力（`追記OK`） | 従来どおり編集可 | `<p contenteditable="true">先頭の段落追記OK</p>` — 残存 | PASS |
| 10 | 自動保存ステータス観察 | 保存が発火 | 「保存しました」表示に遷移（自動保存成功） | PASS |
| 11 | 編集ページ再読み込み後の decorate 再適用確認 | 再適用される | 全 7 要素に `contenteditable="true"` が再付与、構造保持 | PASS |

## 失敗詳細（FAILの場合）

なし
