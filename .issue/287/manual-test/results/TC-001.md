# TC-001: メディア挿入直後の `<p><img></p>` が編集可能として decorate される

**結果**: PASS
**実行時間**: 67
**セッション**: verify-tc-001

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 前提ノート「TC287 inline media test」（段落2行）を新規作成し `/notes/019f4ff8-74b1-754f-b767-33c6cdd13b75/edit` を開く | 「ビジュアル」タブがデフォルト選択 | tab "ビジュアル" [selected] を確認 | PASS |
| 2 | MediaUploader の hidden な `input[type=file]` に test-image.png をアップロード | 進捗 → 「ノートに挿入しました」表示 | 「ノートに挿入しました」表示を確認 | PASS |
| 3 | 本文末尾に `<p><img></p>` が追記されることを確認 | 本文末尾に `<p><img src="/media/<id>" alt=""></p>` | `<p><img src="/media/019f4ff8-eb14-7558-91ba-1723e3df9070" alt=""></p>` が最終ブロックとして追記（isLastChild: true）。ページ再読み込みなし（resync rebuild 経路） | PASS |
| 4 | DOM で `<p>` の `contenteditable` を確認 | `contenteditable="true"` が付く | `pContentEditable: "true"` | PASS |
| 5 | `<img>` 自身の `contenteditable` を確認 | 付いていない | `imgContentEditable: null` | PASS |
| 6 | `<p>` 内（画像の右側余白）をクリック | キャレットが置ける | selection collapsed、anchorNode=P（offset 1 = img 直後）、activeElement=当該 `<p>` | PASS |

## 備考

- 挿入「直後」・ページ再読み込みなしの resync rebuild 経路で decorate されていることを確認（従来編集不可だった箇所）。
- 他の既存段落 2 つも従来どおり `contenteditable="true"`（decorate 対象一覧: P×2 + P[has img]）。
