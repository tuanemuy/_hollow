# TC-04: SavedView 復元時にも chip タイトルが表示される

**結果**: PASS
**実行**: 約 15 秒

## 実行ログ

| # | 操作 | 期待 | 実際 | 判定 |
|---|------|-----|-----|------|
| 1 | TC-03 の状態（`/?referencingNoteId=<A.id>`）で「ビューとして保存」ボタンをクリック | ダイアログ表示 | dialog "ビューとして保存" 表示、名前入力欄、公開範囲、保存ボタン | OK |
| 2 | 名前を「Issue32 TC04 View」と入力し保存（個人用 既定） | ビュー保存成功 | ダイアログ閉じる、DB に saved_views 1 行 (id `019e3c01-c2f9-7308-bf35-085c67e95c84`) | OK |
| 3 | `/?viewId=<saved-view-id>` に直接遷移 | URL 受理 | URL 受理、ホームのデータが復元 | OK |
| 4 | FilterBar chip を確認 | `参照中: ターゲット`（タイトル）で復元 | StaticText "参照中:" + "ターゲット" 表示 | OK |
| 5 | 一覧表示を確認 | B / C のみ | 「2 件のノート」、リファラ B / C のみ | OK |

## スクリーンショット
- Step 4: screenshots/tc-04/step-01-savedview-restore.png

## 確認ポイント
- SavedView の `visibilityFilter` 経路（Issue #31）と `referencingNoteId`（Issue #8 ADR-007 で SavedView 経路を往復済み）が両立し、復元時にも title resolver が呼ばれる → OK
- `viewQueryToSearch` の復元処理（`app/components/note/list/listSelectors.ts` 行 162 周辺）で `baseSearch.referencingNoteId` 経由で SSR 解決される構造を確認
