# TC-06: 不正な UUID（schema パス通過後、`DomainNoteId.create` で reject）

**結果**: PASS
**実行**: 約 4 秒

## 実行ログ

| # | 操作 | 期待 | 実際 | 判定 |
|---|------|-----|-----|------|
| 1 | `/?referencingNoteId=not-a-uuid-string` を URL 入力（`noteListSearchSchema` の `z.string().min(1)` で通過） | URL 受理、ページ 200 | URL = `http://localhost:3000/?referencingNoteId=not-a-uuid-string&page=1&limit=20`、ホーム表示 | OK |
| 2 | FilterBar chip を確認 | `参照中: not-a-uu`（先頭 8 文字） | StaticText "参照中:" + "not-a-uu" 表示 | OK |
| 3 | 一覧 | 0 件、ページ全体は壊れない | 「0 件のノート」、「該当するノートがありません」、レイアウト崩れなし | OK |

## スクリーンショット
- Step 1: screenshots/tc-06/step-01-invalid-uuid.png

## 確認ポイント
- `DomainNoteId.create` が drop した時の `{ title: null }` フォールバックが効いて UUID 断片表示（8 文字）に落ちる → OK
- 500 系エラーや loader 例外を漏らさない → OK
