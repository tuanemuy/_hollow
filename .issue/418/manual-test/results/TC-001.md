# TC-001: AUTH_FOOTER_LINK（/login フッターリンク）

**結果: PASS**

対象: `/login` フッターの「アカウントを作成」リンク（`<Link to="/signup">`, `AUTH_FOOTER_LINK`）。

## 実行ログ

| # | ステップ | 結果 |
|---|---------|------|
| 1 | `http://localhost:5175/login` を open → `wait --load networkidle` | OK（URL 一致・vite dev 確認・リンク4件描画） |
| 2 | snapshot でフッター「アカウントを作成」リンク特定（ref=e10, href=/signup） | OK |
| 3 | hover 前の computed style 確認 | `className="text-accent hover:underline hover:[text-underline-offset:3px]"`、`color=oklch(0.371 0 0)`、`text-decoration-line=none` |
| 4 | hover → 下線確認 | `:hover=true`、`text-decoration-line=underline`、`text-underline-offset=3px`、color 不変 |
| 5 | スクリーンショット（通常時 + hover 時） | 保存済み |

## 確認ポイントの判定

- accent 色: `text-accent` クラス適用、computed `oklch(0.371 0 0)` = `--color-accent`（tokens.css L3）と一致。**※注記**: 本プロジェクトの accent トークンは chroma 0 のダーク・ニュートラル（青系ではない）。testing.md / 指示の「青系」は想定値で、実デザインはモノクロ accent。判定基準（accent トークン適用・hover 挙動）は満たす。
- hover 前は下線なし → OK
- hover で 3px offset 下線 → OK
- 色・オフセットが従来と同一（`textLink` primitive そのもの）→ OK

## スクリーンショット

- 通常時: `.issue/418/manual-test/screenshots/tc-001/tc-001-normal.png`
- hover 時: `.issue/418/manual-test/screenshots/tc-001/tc-001-hover.png`（下線が視認可能）
