# TC-003: 削除ボタンの有効化ゲート（AC-3/7）

**結果: PASS**

「アカウントを完全に削除する」ボタンの disabled 状態を段階的に確認した。**削除ボタンは押していない（非破壊）。**

| 手順 | 操作 | button.disabled | 期待 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 初期状態 | true | disabled | PASS |
| 2 | 同意 checkbox のみ check | true | disabled | PASS |
| 3 | 確認語 `delete`（小文字） | true | disabled | PASS |
| 4 | 確認語 `DELETE`（creds 未入力） | true | disabled | PASS |
| 5 | username `dev-login` + password 入力 | false | enabled | PASS |

最終確認: `is enabled @e14` → `true`（有効化）を確認後、クリックせず終了。

## 確認ポイント

- 全条件（同意 && 確認語が厳密に `DELETE` && username 一致 && password 入力）を満たして初めて有効化。
- 小文字 `delete` では有効化されず、確認語は大文字 `DELETE` のみ通る。
- **削除ボタンは一度も押していない。**
