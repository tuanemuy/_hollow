# TC-4 — 1MiB 超ファイルの拒否（エッジケース2）

- 結果: **PASS**
- 実行時間: 約1分
- セッション: verify-tc-4

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|----------|------------|------|
| 1 | `http://localhost:8787/notes/new` を開く（cookie 注入済み） | ノート編集画面表示 | 表示 OK | OK |
| 2 | 1.5MB の PNG（/tmp/tc657/big.png, 1,556,218 bytes）を選択 | 従来どおりサイズ制限エラー | presign の serverFn POST が **422** で拒否（`maxNoteBytes` 1 MiB 超過）。`/dev/r2/` への PUT は一切発生せず | OK |
| 3 | UI 表示確認 | エラーが表示される | alert「操作を完了できませんでした。時間をおいて再度お試しください」表示、file input は再選択可能 | OK |

## 判定理由

dev プロキシ（`/dev/r2/`）経由でもサイズ制限は presign 段階で従来どおり機能し、制限バイパスは発生しない。
