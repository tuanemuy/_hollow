# TC-3 — `http://127.0.0.1:8787` アクセス時の失敗挙動（エッジケース1）

- 結果: **PASS**
- 実行時間: 約3分
- セッション: verify-tc-3

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|----------|------------|------|
| 1 | `__Host-session=dev-admin-session-token` を `--url http://127.0.0.1:8787` で注入し `/notes/new` を開く | ノート編集画面が表示される | 新規ノート画面表示、「メディアを追加」file input あり | OK |
| 2 | 制限内画像（/tmp/tc657/small.png, 約300KB）を選択 | アップロードが完走しない（preflight 発生または 403 等） | presign POST 200 → PUT 先 `http://localhost:8787/dev/r2/...` がアプリオリジン `127.0.0.1` とクロスオリジンになり preflight `OPTIONS` 発生 → **405** → PUT 失敗 | OK |
| 3 | 失敗後の UI 状態確認 | エラー表示から再試行可能な状態（ハングしない） | alert「エラーが発生しました」+「再試行」ボタン表示。file input も再選択可能 | OK |
| 4 | 「再試行」ボタンをクリック | 再送される | 新しい presign POST 200 + 新しい presigned URL への PUT（XHR）が再発行。再び preflight 405 で失敗し、エラー + 再試行表示に戻る（ハングなし） | OK |

## 判定理由

ドキュメント化された制約（`localhost` 表記必須、`127.0.0.1` では presign URL オリジンと食い違い preflight が復活して失敗）どおりの挙動。失敗後も UI は再試行可能な状態を維持。
