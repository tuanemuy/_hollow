# TC-003: orphan 化された行が次の tick で purge される（回収チェーンの完走）

**結果**: PASS
**実行時間**: 約60秒
**セッション**: なし（CLI 検証）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | orphan 行（`019f0000-0000-7000-8000-000000468002`、TC-002 で orphan 化済み）の `updated_at` を2日前にバックデート | UPDATE 成功、purge 猶予（24h）経過状態になる | `status='orphan'` / `updated_at=2026-07-08T16:10:41.535Z` | PASS |
| 2 | `curl "http://localhost:8788/__scheduled?cron=0+3+*+*+*"` で tick 再発火 | `Ran scheduled event` | `Ran scheduled event`（200 OK） | PASS |
| 3 | 行の存在確認（`SELECT ... WHERE id='019f0000-...468002'`） | 行が削除されている（0件）。1回の tick で markDeleting → purge 完走 | 0件（`results: []`）。再実行不要で完走 | PASS |
| 4 | blob なし行（put 失敗相当）の delete 冪等性 — `deleting` stall がないこと | `deleting` のまま残らない | 行自体が消えており stall なし。purge ログに failed 0 | PASS |
| 5 | tick ログの purge 件数・エラー有無 | purge 件数が出て error なし | `[prune] purged 1 orphaned media asset(s) (0 failed) { purged: 1, failed: 0 }`、当該 tick に error なし | PASS |

## 補足

- seed 行の id は TC-002 記載の逸脱により UUIDv7 形式（`019f0000-0000-7000-8000-000000468002`）。手順・検証内容は testing.md 確認項目3のとおり。
- purge により seed 行は DB から消えたため、テスト後の手動削除は不要だった（残骸なしを SELECT で確認済み）。
- TC-002 で outbox に載った `media.orphaned` イベント（`processed_at=null`）はシステム生成のため残置。relay の通常処理／outbox prune の保持期間で回収される。
