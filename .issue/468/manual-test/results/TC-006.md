# TC-006: 保持ポリシーと運用ノートが明文化されている（AC-1）

**結果**: PASS
**実行時間**: 約20秒
**セッション**: なし（CLI 検証）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `spec/domains/media.md` に保持ポリシーの記載があるか確認 | TTL なし・Note ライフサイクル連動・orphan 化 → purge 回収の記載 | L5「## 保持ポリシー（source, Issue #468 ADR-001）」以下に、(a) 時間ベース TTL を設けない、(b) Note 存続中は無期限保持（trash 中も保持）、(c) 参照が外れた時点で orphan 化 → 標準 purge 機構が回収（overwrite / note purge / 放棄 intake の3契機）、(d) 二重の猶予（24h+24h）、(e) 再検討トリガー、がすべて明記されている | PASS |
| 2 | `spec/usecases/media.md` に `SweepAbandonedSourceIntakes` と `PurgeOrphans` の pruner tick 起動の記載があるか確認 | 両ユースケースに cron（pruner tick）起動の記載 | L101「## PurgeOrphans（バッチ）」— 入力DTO「Cron 起動 — pruner tick（日次 03:00 UTC）から SweepAbandonedSourceIntakes の後に best-effort で実行」。L116「## SweepAbandonedSourceIntakes（バッチ、Issue #468）」— 入力DTO「Cron 起動 — pruner tick（日次 03:00 UTC）から PurgeOrphans の前に best-effort で実行」。処理フロー・エラーケース（per-row tolerance）も記載 | PASS |
| 3 | `docs/runtime_cloudflare.md` に手動リコンサイル手順の運用ノートがあるか確認 | デプロイ以前に漏れた blob の突合削除手順 | L332「#### One-time manual reconcile for pre-#468 leaked blobs」に、#468 以前のデプロイで漏れた行なし blob を対象とする 3 ステップ手順（`wrangler r2 object list` で source キー列挙 → `media_assets` の `storage_key` と突合 → 差分を `wrangler r2 object delete`）が one-off 運用タスクとして記載。直前には pruner tick の sweep → purge 2 ステップと回収レイテンシ（最大約2日）の説明もある | PASS |

## 備考

- 期待結果どおり 3 箇所すべてに対応する記載が存在し、AC-1（保持ポリシーの明文化）を充足する。
