# TC-005: infra テンプレートに pruner の R2 配線が入っている

**結果**: PASS
**実行時間**: 約90秒
**セッション**: なし（CLI 検証）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `pnpm infra:render:staging` を実行 | エラーなくレンダリングされる | `✓ wrote wrangler.staging.toml (stage=staging)` / EXIT=0 | PASS |
| 2 | `pnpm infra:render:production` を実行 | エラーなくレンダリングされる | `✓ wrote wrangler.production.toml (stage=production)` / EXIT=0 | PASS |
| 3 | レンダリング結果（staging）の `[env.pruner]` に R2 配線があるか grep | `[[env.pruner.r2_buckets]] binding = "OBJECT_STORAGE"` と `R2_OBJECT_BUCKET_NAME` が存在 | L219 `R2_OBJECT_BUCKET_NAME = "hollow-staging-objects"`（`[env.pruner.vars]` 内）、L237-239 `[[env.pruner.r2_buckets]] binding = "OBJECT_STORAGE" bucket_name = "hollow-staging-objects"` | PASS |
| 4 | レンダリング結果（production）の `[env.pruner]` に R2 配線があるか grep | 同上 | L219 `R2_OBJECT_BUCKET_NAME = "hollow-production-objects"`、L237-239 `[[env.pruner.r2_buckets]] binding = "OBJECT_STORAGE" bucket_name = "hollow-production-objects"` | PASS |
| 5 | ローカル `wrangler.toml` の pruner セクションと同等の配線か比較 | 同等の R2 配線（binding 名・vars キーが一致） | `wrangler.toml` L293 `R2_OBJECT_BUCKET_NAME` / L311-313 `[[env.pruner.r2_buckets]] binding = "OBJECT_STORAGE"` と同構成（bucket 名のみ環境別） | PASS |
| 6 | `pnpm deploy:staging:pruner:dry` でビルドが通るか確認（実デプロイなし） | ドライランがエラーなく完了 | `--dry-run: exiting now.` / EXIT=0。バインディング一覧に `env.OBJECT_STORAGE (hollow-staging-objects)`（R2 Bucket）と `env.R2_OBJECT_BUCKET_NAME ("hollow-staging-objects")` を表示 | PASS |

## 備考

- ドライランは Cloudflare 認証なしで完走した（環境制約による代替確認は不要だった）。
- wrangler が「top-level の vars / r2_buckets(TEMP_FILES) / services / ai が env.pruner に継承されない」旨の情報警告を出すが、これは既存構成に対する wrangler 標準の注意喚起であり、エラーではない（EXIT=0）。pruner はこれらのバインディングを必要としない。
- レンダリング成果物 `wrangler.staging.toml` / `wrangler.production.toml` は gitignore 対象（`git check-ignore` で確認）。
