# TC-004: pruner tick の既存刈り込みが退行していない

**結果**: PASS
**実行時間**: 約20秒（TC-002/003 の tick ログ確認）
**セッション**: なし（CLI 検証）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | tick 1（TC-002 手順2、malformed seed あり）のログ確認 | 既存ステップが従来どおり実行される | `[outbox] pruned 163` / `[processed-events] pruned 156` / `[activity] pruned 0/0` / `[llm-call-log] pruned 0` / `[prune] expired 3 export(s)` / `[export-jobs] pruned 4` / `[tag-merge-jobs] pruned 1` — すべて出力 | PASS |
| 2 | tick 1 で新ステップ（sweep）が失敗した際の他ステップへの影響 | 新ステップは best-effort（独立 try/catch）で、失敗しても tick 全体が落ちない | sweep が `DATA_INTEGRITY_ERROR` でログにエラーを出したが、直前の既存7ステップはすべて完了済みで、後続の `[prune] purged 0 orphaned media asset(s)` も実行され tick は 200 OK で完走 | PASS |
| 3 | tick 2（TC-002 正常 sweep）のログ確認 | 既存ステップ + `swept` / `purged` ログが揃う | 既存7ステップ（各0件）→ `swept: 1, failed: 0` → `purged: 0, failed: 0`。エラーなし | PASS |
| 4 | tick 3（TC-003 purge）のログ確認 | 既存ステップ + `swept` / `purged` ログが揃う。export purge（#783）の呼び出し位置も従来どおり | 既存7ステップ（各0件、`[prune] expired 0 export(s)` を含む）→ `swept: 0` → `purged: 1, failed: 0`。エラーなし | PASS |

## 補足

- 新ステップの実行順は既存ステップの後（outbox → processed_events → activity → llm-call-log → export expire → export-jobs → tag-merge-jobs → sweep → purgeOrphans）で、既存ログの形式・件数出力に変化なし。
- tick 1 の sweep 失敗（seed の id 形式不備という試験側要因）が、期待どおり swallow されて他ステップを阻害しなかったことは、best-effort 設計（AC-5 / 確認項目4の確認ポイント）の実地の証拠になった。
