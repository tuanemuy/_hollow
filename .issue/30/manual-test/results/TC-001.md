# TC-001: フィルタなしホームで count = 10

- 状態: PASS
- 期待値: `{count} 件のノート` の N = 10
- 実測値: `10 件のノート`

## 操作ログ

1. `open http://localhost:3000/login`
2. email `test-user-001@example.com` と password `TestPassword123!` を入力
3. ログインボタンをクリック → ホーム `/?page=1&limit=20` へ遷移
4. `document.querySelector(".page-subtitle").innerText` を取得 → `"10 件のノート"`

## スクリーンショット

- `/Users/hikaru/github.com/tuanemuy/hollow2/.issue/30/manual-test/screenshots/tc-001/home.png`

## 備考

- 検証中、ホームの renderHome サーバーファンクションが「Saved view 01938f00-0000-7000-8000-00000000d071 query_json.directoryId is not a string|null」で 500 エラーを返した（Issue #30 とは無関係の事前データ問題）。
- 該当 saved view を `DELETE FROM saved_views WHERE id = '01938f00-0000-7000-8000-00000000d071'` で除去した上で再検証し PASS を確認した。
