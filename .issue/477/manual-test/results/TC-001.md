# TC-001: ピルからモーダルが開く

**結果**: PASS
**セッション**: verify-tc-001

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | cookie 注入 + `/notes/...011` を開く | ログイン済みでノート詳細が表示 | 「公開設定モーダル検証ノート」が表示。アクションバーに `button "公開状態: 非公開" [ref=e23]` を確認（`<a>` ではなく `<button>`） | PASS |
| 2 | 公開設定ピルをクリック | URL 不変、中央にモーダルが開く、背景スクロールロック | URL は `/notes/.../011` のまま（`/publish` 付かず）。`dialog "公開設定"` が body 直下の Portal（ref=e1）に出現。`document.body` の `overflow: hidden` でスクロールロック確認 | PASS |
| 3 | モーダル内容を確認 | 公開ステータスのラジオカード・更新ボタン・限定公開リンクセクション・閉じる手段 | ラジオ（非公開[checked]/限定公開/公開）、「公開状態を更新」ボタン、「限定公開リンク」見出し（非公開のため「非公開ステータスではリンクを発行できません。」）、× ボタン(e10)・「閉じる」ボタン(e12) を確認 | PASS |

## スクリーンショット

- Step 1 (ノート詳細): `screenshots/tc-001/step-01.png`
- Step 2 (モーダル表示): `screenshots/tc-001/step-02.png`

## 確認ポイント

- ピルが `<button>` であること: PASS（snapshot で `button "公開状態: 非公開"`）
- URL に `/publish` が付かないこと: PASS（URL 不変）
- 背景スクロールロック: PASS（`body { overflow: hidden }`）
