# テスト実行サマリー — Issue #358

**実行日時**: 2026-05-31
**テストソース**: .issue/358/testing.md
**サーバー**: http://localhost:3100（`pnpm dev --port 3100`、ライブソース）

| TC | テスト名 | 種別 | 結果 | 失敗ステップ |
|----|---------|------|------|-------------|
| TC-001 | 空フォールバック表示（最頻ケース） | 正常系 | PASS | - |
| TC-002 | 状態バッジの切替（既定を使用中 ⇄ この回だけ上書き） | 正常系 | PASS | - |
| TC-003 | ユーザー上書き + インスタンス既定の出し分け | 正常系 | PASS | - |
| TC-003b | 上書きなしユーザーは両欄インスタンス既定 | 正常系 | PASS | - |

**合計**: 4 件（PASS: 4 / FAIL: 0）

## 検証した3状態（解決優先順位の可視化）

| 状態 | DB 条件 | 構造化欄の表示 | メタデータ欄の表示 |
|---|---|---|---|
| 空フォールバック | instance 未設定・override なし | 出所「プロバイダ組み込み」/「LLM プロバイダの既定指示を使用」 | 同左 |
| インスタンス既定 | instance 設定済み・override なし（admin） | 出所「インスタンス既定」+ 既定テキスト | 同左 |
| ユーザー上書き | User A が structure のみ override | 出所「ユーザー設定で上書き中」+ 上書きテキスト | 出所「インスタンス既定」+ 既定テキスト |

「実際に使われる値（上書き優先）」が表示され、状態バッジも入力の空/非空で正しく切り替わることを確認。

## 環境メモ（後続検証者向け）

- ログインルートは `/login`（`/sign-in` は 404）。テストユーザー `existing@example.com` / `admin@example.com`、パスワード `Password123!`。
- アップロードモーダルは `#upload` ハッシュ単独では開かない。ログイン後ヘッダーの「アップロード」CTA（href=`/#upload`）クリック → `location.hash='#upload'` で確実に開く。
- 「詳細オプション」「既定値を表示」はネイティブ `<details>`。
- Phase 2 シードは `.issue/358/manual-test/seed-prompts.sql`、後始末は `cleanup-prompts.sql`。検証後 DB は pristine（overrides 0 / prompts_json `{}`）に復元済み。
