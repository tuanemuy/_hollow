# クロスフェーズ検証 002

ゼロベース。各層の整合を再確認。

## トレーサビリティ
- シナリオ→ページ→ユースケース→ドメイン→DB→テストケースのチェーンが全シナリオで成立
- 全ドメインエンティティに DB テーブルが対応
- 全ポートに利用ユースケースあり

## アーキテクチャ適合性
- ドメイン層は純粋。インフラ非依存
- 集約境界は ID 参照
- ユースケースはオーケストレーション、ビジネスロジックはドメイン
- イベント駆動の購読対応表が `spec/domains/index.md` に明示

## 実装可能性
- DTO 共通型 / 共通エラー / NoteSnapshot 型などすべて定義済み
- ポートにメソッドシグネチャとエラーケース
- DB スキーマ + 補助制約方針
- usecase 内で `countAdmins → assertNotLastAdmin` のフロー、Outbox payload の中身まで明示

## 判定

クリーン。修正不要。設計完了。
