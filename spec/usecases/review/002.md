# ユースケースレビュー 002

ゼロベース。チェックリストで再評価。

## 観点別チェック結果

### トレーサビリティ
- A1〜A6, B1〜B5, C1〜C6, D1〜D6, E1〜E5, F1〜F5, G1〜G3, H1〜H4, I1〜I5 を逐一突き合わせた結果、すべて対応 usecase が存在する
- H4 自動パージ → PurgeTrashOlderThan 追加済み

### DTO 完全性
- 共通 DTO は spec/usecases/index.md に集約済み
- NoteDTO / NoteListItemDTO / BacklinkDTO / InternalLinkRefDTO / ViewQuerySnapshotDTO / ExportJobDTO / IngestionJobDTO / SearchHitDTO / SavedViewDTO / DirectoryDTO / TagDTO / UserDTO / PublicationStateDTO / ShareLinkDTO / MediaAssetDTO / InstanceSettingsDTO すべて記述
- 共通エラー型も列挙済み

### アーキテクチャ適合性
- ビジネスロジックはドメイン側に配置済み（TagService.renameInBody / NoteService.assembleFromInputs / DirectoryService.deleteSubtree / PublicationService.changeVisibilityAndCascade / MediaService.assertViewableBy）
- usecase 層はオーケストレーションに留まる
- 集約間参照は ID で行われる
- Outbox イベント発火責任は usecase が担う旨が明確

### エラー網羅性
- 共通エラー型のクラス階層を spec/usecases/index.md に明示済み
- ドメイン層が出すエラーと application/presentation 層が出すエラーの境界も明示

### 単一責任
- バルク系（BulkMoveNotes / BulkTrashNotes / BulkChangePublicationVisibility / BulkUpload）に「単一 usecase をループ呼びする薄いオーケストレータ」と注記
- 個々の usecase は基本的に 1 機能に集中している

## 判定

クリーン。修正不要。Phase 3 へ進む。
