{
  "_comment": "Plaintext template — copy to production.enc.json and encrypt with `sops -e -i infra/secrets/production.enc.json`. Keep the keys in sync with `workerSecretSpecs()` in infra/src/secrets.ts.",
  "BETTER_AUTH_SECRET": "<random-base64-32-bytes>",
  "GOOGLE_CLIENT_ID": "<google-oauth-client-id>.apps.googleusercontent.com",
  "GOOGLE_CLIENT_SECRET": "<google-oauth-client-secret>"
}
